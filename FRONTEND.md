# Frontend — What's Been Built

**Status:** Working — public site and admin panel both run against the real backend/database

## How to run it

```bash
cd frontend
npm install
npm run dev
```

Opens at whatever local address Vite prints (usually `http://localhost:5173`). Requires the backend to be running too (see `BACKEND.md`) — the frontend talks to it via `VITE_API_URL` in `.env` (defaults to `http://localhost:4000/api`).

## Branding

Renamed from "Twegle Quiz" to **Twegle** to reflect that it's now more than quizzes. New logo lives in `src/components/Logo.jsx` — a mascot-style SVG icon (rounded gradient square with a friendly face) paired with a "Twegle" wordmark in Baloo 2, used in the header and as the favicon. Page title/meta now read "Twegle — Quizzes, Quotes & Chaos for Everyone."

**Tagline:** "Where Fun Goes Viral" — added directly in `Header.jsx` (small italic text next to the logo, `hidden sm:inline` so it doesn't crowd the header on phone widths) and `Home.jsx`'s hero band (a small uppercase line above the main heading). Deliberately added to these two spots only, not to the shared `LogoWithWordmark` component itself (`Logo.jsx`) — `AdminLayout.jsx` uses that same component for its own header, which is already tight on horizontal space (nav links + user info + logout all fighting for one line, see "Small fixes from a UX pass" below), so baking the tagline into the shared component would have leaked it into the admin panel too.

**Description wording, updated 2026-08-01:** the hero `<h1>` ("Quizzes, Jokes, Quotes, Games, Stories & Horoscope"), the footer's brand blurb, `index.html`'s `<meta name="description">`/`og:description`, and `useDocumentMeta.js`'s default description were all changed from an older "quizzes, jokes, quotes, and more" phrasing to explicitly name Games/Stories/Horoscope — matching the description the owner had already published in the site's own social media bios, so the wording reads the same wherever someone encounters the brand.

## Public site — what it does right now

- **Homepage** (`/`) — the main hub. A compact hero band up top (with a live "🎯 N quizzes / 💬 N+ jokes & quotes" count, fetched from the API rather than hardcoded — see "Known decisions" below), then a language switcher (EN/हिंदी) paired with the Trending/Newest sort toggle right beside it — both are global filters that apply across every content type, not something specific to one tab, so they sit together ahead of the tabs/category chips (moved here 2026-07-31, previously the sort toggle lived at the bottom of the sidebar, below the category chips) — category tabs — as of 2026-08-05, six tabs: **Quizzes → Friendship Quiz → Games → Posts → Stories → Horoscope**, in that order, chosen explicitly through a monetization lens with the owner (Quizzes first — biggest content library and the strongest solo share loop, so the most pageviews/ad impressions; Friendship Quiz second — same growth-loop idea but needs a friend to join, more first-click friction; Games — replayable, own share hook; Posts and Stories/Horoscope last — steadier, less share-driven browsing). The previous 5 separate Jokes/Funny Lines/Quotes/Motivational Quotes/Memes tabs are now one **Posts** tab (see "Merged Posts tab" below) — quiz-category filter chips (All / Beauty / Bollywood / K-pop / Lifestyle / Fun & Random, Quizzes tab only), story-category filter chips (All / Horror / Comedy / Romance / Mystery / Moral Tales / Motivational, Stories tab only), and post-category filter chips (All / Jokes / Funny Lines / Quotes / Motivational / Memes, Posts tab only). The **Trending/Newest sort toggle** (mentioned above, next to the language switcher) works on every tab — Trending sorts by real play/attempt counts for quizzes/friendship-quizzes/games, or by aggregated view+share engagement for posts/stories; Newest sorts by creation date, or by registry order for Games — see "Trending/Newest sort toggle generalized" below. On desktop (`lg:` and up) these sit in a **left sidebar** next to the quiz/post grid instead of stacking above it — this puts the actual content right beside the filters instead of below several rows of them, so visitors see real cards immediately instead of scrolling past controls first. On mobile, the tab row and both category-chip rows are each a single horizontally-scrollable strip rather than wrapping across multiple lines (see "Known decisions" below — with 9 tabs now, wrapped pills were pushing the first real content card down by most of a phone screen). The active tab lives in the URL (`?tab=`), not just local state, so switching tabs is itself bookmarkable/shareable and the browser back/forward buttons move between tabs. All of it browsable from one page without a full reload. No login, nothing to set up, just pick something.
- **Search** (`/search?q=`) — a search box in `Header.jsx`, present on every public page, submits to this page. Searches quizzes, posts, stories, and friendship-quiz templates in one call and groups the results by type, each rendered with the same `QuizCard`/`FriendshipQuizCard`/`StoryCard`/`PostCard` components used everywhere else. See "Search" below.
- **Quiz flow** (`/quiz/:slug`) — fetches that quiz from the API, one question per screen with a progress bar. No login wall — matches the "guest-first" decision from `DEVELOPMENT_PLAN.md`. Two shapes now: **personality** (the original — answers tally toward a result key) and **trivia** (right/wrong questions, scored numerically). See "Non-personality (trivia) quizzes" below.
- **Result page** (`/result/:slug/:resultKey`) — shows the result, share buttons (native share sheet on mobile, WhatsApp, copy link — these now point at a backend `/api/share/...` URL so shared links actually show a real preview card, see below), a **"📸 Share as Image" button**, a "🆚 Compare with a friend" option, a placeholder ad box, the affiliate-link disclosure text, a **"🚩 Report this"** link (see "Report a quiz/post" below), and a "You might also like" section (4 other quizzes — see below). A trivia quiz's result additionally shows "You scored X/N!" when arriving straight from finishing (not on a cold visit to a shared link — see that section for why). Also silently records an anonymous "play" against the backend (no personal data — see `api.js` below).
- **Browse page** (`/browse/:category`) — a directly-linkable feed for one content category (e.g. `/browse/jokes`), for when a category needs its own shareable URL outside the homepage tabs.
- **Post page** (`/post/:id`) — a single joke/funny line/quote/motivational quote as its own shareable card, same share-button, image-share, ad-slot, "🚩 Report this", and "You might also like" treatment as quiz results. Silently records an anonymous "view" the first time it loads, and a "share" whenever any share action is used (native share, WhatsApp, copy link, or "Share as Image") — same anonymous-tracking approach as quiz plays, feeding the admin Analytics page's new Post Engagement table.
- **Privacy Policy / Terms of Service** (`/privacy`, `/terms`) — real content: what data is/isn't collected, the anonymous play-tracking ID, ads (none live yet, non-personalized when added), affiliate link disclosure, children's privacy, cookies/local storage, and a contact email. Updated 2026-08-07 to cover the optional end-user account feature — username/password only, no email/phone, one-time Recovery Code, public Gamer Tag — which didn't exist when these were first written, and both originally said an accounts feature "will update this policy first" before launching; that update was overdue since accounts had already shipped. Still worth a real lawyer review before the site generates meaningful revenue, but no longer a bare placeholder.
- **About page** (`/about`) — brand blurb plus a Contact section (email), linked from the footer's "Company" column alongside Privacy/Terms. Small trust-builder for a first-time visitor.
- **Friendship Quiz** — a new "🤝 Friendship Quiz" homepage tab, separate from regular quizzes since it's a two-person format: "How well do you know me?" One person picks a template and answers questions about themselves (`/friendship/:slug`), gets a shareable link, and any number of friends can open it (`/friendship/play/:code`) to guess what they'd say — each friend gets their own scored reveal page (`/friendship/result/:attemptId`) with a per-question right/wrong breakdown, shareable itself so their score can drive more people back to the original link. No login for either side, same guest-first pattern as everything else. See "Friendship quizzes" below for the full flow.
- **Memes** — a "😹 Memes" homepage tab and `/browse/memes`, alongside Jokes/Quotes/etc. Unlike every other post type, a meme card shows an actual image (with an optional caption below it) instead of a colored gradient+text card — see `PostCard.jsx`. The individual page (`/post/:id`) shows the image full-size with a "⬇️ Download Image" button instead of "📸 Share as Image" (there's no synthetic card to generate — the meme already *is* the image). See "Memes" below for how the image gets there.
- **404 page** — any URL that doesn't match a route (`NotFound.jsx`, registered as a catch-all `*` route) shows a proper "page not found" message with a link home, instead of a blank page.
- **Games** — a new "🎮 Games" homepage tab and `/games/:slug` pages, alongside Quizzes/Friendship Quiz/Jokes/etc. Nine games live today: Tic-Tac-Toe, Rock Paper Scissors, Memory Match, 2048, Word Guess, Guess the Number, Sudoku, Simon Says, and Whack-a-Mole. See "Games" below. Tic-Tac-Toe additionally has a real two-player mode — "🆚 Challenge a friend" on its single-player screen — separate from the games list above. See "Two-player Tic-Tac-Toe" below.
- **Stories** — a new "📖 Stories" homepage tab and `/story/:slug` pages: original short stories across 6 categories (Horror, Comedy, Romance, Mystery, Moral Tales, Motivational), each with a "🔊 Listen to this story" button that reads the full text aloud using the browser's built-in Web Speech API — no recorded voice, no paid TTS service, nothing server-side. See "Stories" below.
- **Horoscope** — a new "🔮 Horoscope" homepage tab (placed right after Stories) and `/horoscope/:sign` pages: light, funny, entertainment-only horoscopes for all 12 zodiac signs, in English and Hindi, with a Day/Week/Month/Year period switcher. Every horoscope is computed on the fly from a deterministic date-based formula — no database content, no cron job. See "Horoscope" below.
- **Feedback page** (`/feedback`, linked from the footer's "Company" column) — a simple textarea + optional email form for visitors to send feedback about the site itself, no account needed. See "Feedback" below.
- **End-user accounts** (`/signup`, `/login`, `/forgot-password`, `/account`) — fully optional, no feature is gated behind them. A header icon (next to 🏆 My Badges) links to `/account` if logged in, `/login` otherwise — a plain 👤 for guests, or a colored badge with the Gamer Tag's first letter once logged in, so it's visually obvious at a glance which state you're in. See "End-user accounts" below.

**25 quizzes** are live — 12 personality-quiz topics (skincare personality, Bollywood era, aesthetic, procrastination style, chai/coffee order, Korean beauty standard, squishy, blind bag, K-pop idol position, K-pop comeback era, texting personality, monsoon mood), each in both English and Hindi (the Hindi versions have their own slugs, e.g. `skincare-type-hi`), plus 1 English-only trivia quiz ("How Well Do You Know Bollywood?") — plus posts across Jokes/Funny Lines/Quotes/Motivational Quotes, plus **8 stories** (6 English across all 6 categories, 2 Hindi). All managed through the admin panel, not by editing code.

Post cards (Jokes/Funny Lines/Quotes/Motivational Quotes) use the same colorful gradient-card treatment as quizzes, one fixed color per category (see `src/postStyles.js`) — previously they were plain white cards with a border, which looked flat next to the quiz cards.

## Shareable images (📸 Share as Image)

A plain link dies in a WhatsApp/Instagram Story — a real image gets reshared. Every quiz result and post now has a "Share as Image" button that generates a branded 1080×1920 (Story-ratio) PNG — emoji, title, description, and a "Twegle" logo badge, colored to match the quiz/category gradient — and either opens the phone's native share sheet (straight to Instagram Story/WhatsApp on mobile) or downloads the file (desktop, or browsers without file-sharing support).

- `src/utils/shareImage.js` — draws the card directly with the **Canvas 2D API** (`fillRect` for the gradient, `fillText` with manual word-wrapping for text, arc/text for the logo badge), then exports it with `canvas.toBlob()`.
- **Why not a DOM-screenshot library:** the first implementation used `html-to-image` (renders the DOM as an SVG `foreignObject`, loads that as an `<img>`, then draws it to canvas). That trick is a well-known source of cross-browser flakiness, and it hung indefinitely with no error at all during testing — confirmed down to a minimal red `<div>` failing the same way, ruling out anything specific to our card's content. Canvas 2D drawing has none of that fragility and is standard, reliable behavior on every browser, including mobile Safari — the primary target for this feature. `html-to-image` was removed from the project entirely.
- Gradient colors for canvas are a small hex lookup (`GRADIENT_COLORS` in `shareImage.js`) keyed by the same Tailwind gradient strings used everywhere else — keep it in sync if a new gradient is ever added to `gradients.js`.

## Link previews

`getQuizShareUrl`/`getPostShareUrl`/`getFriendshipShareUrl`/`getFriendshipResultShareUrl` in `api.js` build URLs pointing at the backend's `/api/share/...` routes rather than the React page itself — the "Copy link" and WhatsApp buttons in `ShareButtons` use whichever one is passed in as `url`. Full explanation of why (client-side apps can't set meta tags in time for link-preview crawlers) is in `BACKEND.md`. The "Share as Image" feature is unaffected by this — it shares an actual image file, not a link.

## SEO

A different problem from link previews above — Google's crawler does execute JavaScript and reads the rendered page, so unlike the WhatsApp/Instagram crawler problem, standard per-page `<title>` and meta description genuinely help here.

- **`src/utils/useDocumentMeta.js`** — a small hook (`useDocumentMeta(title, description)`) that sets `document.title` (as `"<title> | Twegle"`, falling back to the site default while data is still loading) and the `<meta name="description">` tag's content, resetting both back to the default on unmount. Wired into every content page: `Quiz`, `Result`, `PostView`, `Browse`, `FriendshipSetup`, `FriendshipPlay`, `FriendshipResult`, plus static titles on `About`/`Privacy`/`Terms`. No new dependency (no `react-helmet` or similar) — the app only ever needs to touch two tags, so a tiny hook does the job.
- **`Home.jsx`** renders minimal `WebSite` JSON-LD structured data (name, url, description) directly as a `<script type="application/ld+json">` — the one piece of structured data that actually fits cleanly; schema.org has no good native type for a personality-quiz result, so more elaborate structured data per quiz wasn't worth forcing in.
- **`Home.jsx`** also renders an `AdSlot` below the active tab's content grid, once it's non-empty (2026-08-05) — every other `AdSlot` usage sits on a result/detail page, so the homepage (the highest-traffic page) previously had no ad placement at all.
- **`frontend/public/robots.txt`** — a plain static file (allows all crawlers, blocks `/admin/`, points to the sitemap). Its `Sitemap:` line is a `https://yourdomain.com/...` placeholder since no real domain is registered yet — update it once one is (the sitemap directive must be a full absolute URL, not relative).
- **`VITE_SITE_URL`** (new env var, `.env`/`.env.example`) — the frontend's own public URL, used only for the `WebSite` schema's `url` field. Defaults to `http://localhost:5173` for local dev.
- **`sitemap.xml`** itself is generated by the *backend* (`GET /sitemap.xml`, not called by the frontend at all) since it needs live database content — see `BACKEND.md` for the generation logic and the one-time hosting rewrite it'll need once frontend/backend are on separate real domains.

## Friendship quizzes ("How well do you know me?")

The one genuinely new interaction pattern on the site — every other quiz type is single-player, this one is two-sided:

1. **`FriendshipSetup.jsx`** (`/friendship/:slug`) — fetches the question template, then a single-page form: a name field plus every question shown at once with tappable option buttons (not a multi-step wizard like the regular quiz flow — filling in known facts about yourself benefits more from being able to see/edit everything than from a dramatic one-question-at-a-time reveal). On submit, calls `createFriendshipInstance` and — without navigating away — swaps the same page into a "Your link is ready!" screen with `ShareButtons` pointed at the new shareable link.
2. **`FriendshipPlay.jsx`** (`/friendship/play/:code`) — a friend opens the link, sees the subject's name and the same questions (answers withheld by the backend, not just hidden in the UI), fills in their own name + guesses, and submits. On success, navigates to `/friendship/result/:attemptId` using the ID the backend just generated, so the result becomes a real, refreshable, shareable URL rather than transient state.
3. **`FriendshipResult.jsx`** (`/friendship/result/:attemptId`) — the score reveal: big `score/total`, a message that scales with how well they did, `ShareButtons` for posting their own score (which pulls more people back to the same `/friendship/play/:code` link — this loop is the actual growth mechanic), and a full per-question breakdown (✅/❌, what the subject picked, what the friend guessed).

`FriendshipQuizCard.jsx` is the homepage-tab card, linking to the setup page — separate from `QuizCard.jsx` since it shows a different stat ("N friends have guessed" instead of "N took this") and links to a different first step.

4 starter templates are seeded (2 English + 2 Hindi) via `npm run seed:friendship-quizzes` on the backend — see `BACKEND.md`.

## Compare with a friend (regular quizzes)

Brings the friendship-quiz's "share a link, friend plays too" growth loop to the regular single-player personality quizzes, without adding a new quiz-taking flow:

1. **`Result.jsx`** — after seeing their own result, a "🆚 Compare with a friend" button reveals an inline name field (same swap-the-page-in-place pattern as `FriendshipSetup.jsx`'s share screen); submitting calls `createQuizCompare` and shows a "Your link is ready!" panel with `ShareButtons`.
2. **`CompareInvite.jsx`** (`/quiz/:quizId/vs/:code`) — a friend opens the link and sees who invited them and which quiz, but **not** the first person's result (withheld server-side — see `BACKEND.md` — so it can't bias their answers). They enter their own name and tap "Take the Quiz," which navigates into the *existing* `Quiz.jsx` flow with the compare code/name passed via router state (`navigate(path, { state })`), so the actual quiz-taking UI is 100% reused, not duplicated.
3. **`Quiz.jsx`** — reads `location.state.compareCode`/`compareName`; on the last question, if a compare code is present, it submits the result to `joinQuizCompare` instead of navigating to the normal `/result/:quizId/:resultKey` page, then navigates to the reveal page. If the link turns out to be invalid/expired, it silently falls back to the normal result page rather than dead-ending the player.
4. **`CompareResult.jsx`** (`/quiz/:quizId/vs/:code/result`) — the reveal: both people's emoji/title side by side plus a match/no-match banner, `ShareButtons`, and an `AdSlot`. If opened before the friend has played (e.g. the first person checking back on their own link), shows a "Still waiting on your friend!" state instead. Opening an already-completed link's invite page (`CompareInvite.jsx`) auto-redirects straight here.

Reuses `generateUniqueCode`'s exact approach from the friendship-quiz feature (short random `code`, not a database ID) and the same `/api/share/*` link-preview pattern for WhatsApp/Instagram previews (`getQuizCompareShareUrl`).

## Non-personality (trivia) quizzes

Every quiz so far mapped answers to a personality result key, tallying which key got picked most. Trivia quizzes (`quiz.type === 'trivia'`) are right/wrong instead, scored numerically — built to reuse as much of the existing quiz-taking flow as possible rather than a parallel UI:

- **`Quiz.jsx`** — the same per-question `handleAnswer` and `scores` tally used for personality quizzes works unchanged for trivia, because a trivia option's `result` field is just the literal string `'correct'` or `'incorrect'` (see `BACKEND.md`) — so `scores.correct` already holds the right numeric score with zero new tallying code. The only real branch is at the *end*: `pickTriviaResult(quiz, scores)` finds whichever result's `minScore`-`maxScore` range contains the score, instead of `pickWinningResult` picking the most-tallied key. The score and question count are passed to the Result page via router state (`navigate(path, { state: { score, total } })`).
- **`Result.jsx`** — reads that router state to show "You scored X/N!" above the result title. Deliberately **not** load-bearing: a cold visit to a shared result link has no router state (it's only set by an in-app `navigate()` call), so the line simply doesn't render then — the result's own authored title/description still convey the tier either way, same graceful-degradation pattern already used for the compare-with-a-friend `compareCode`/`compareName` state.
- **`QuizForm.jsx`** (admin) — a new "Quiz Type" dropdown (Personality/Trivia) changes two things further down the same form: each answer option's "Pick result..." dropdown becomes a plain "Correct" checkbox (setting `result` to `'correct'`/`'incorrect'` directly), and each Result entry gains Min/Max score number inputs alongside the existing key/emoji/title/description fields. No separate trivia-authoring page — same form, same fields, just relabeled and re-typed based on the selected Quiz Type.

## Games

A new content type, but a different shape from Quiz/Post/FriendshipQuiz — a game isn't admin-authored data, it's code, so there's no admin CRUD and no fetch-by-slug from a database. Instead:

- **`src/games/registry.js`** — a plain static array (`{ slug, title, emoji, gradient, description }`), one entry per game. Adding a new game means adding an entry here, a component in `src/games/`, and a case in `Game.jsx`'s render switch — no backend or admin work required for the game itself.
- **`src/games/TicTacToe.jsx`** — the first game. Human is always X and always goes first; the AI (O) uses a full minimax search (small enough state space — 9 cells — to brute-force with no pruning) so it plays perfectly; the best a human can do is draw. Ties between equally-good AI moves are broken randomly so it doesn't always open the same way. The AI's move fires after a short delay so it doesn't feel instant. Calls `onGameEnd('win'|'loss'|'draw')` once when the board ends, and `onReset()` when "Play again" is clicked (letting the parent page clear its own end-of-game state — see the bug note below).
- **`src/games/RockPaperScissors.jsx`** — the second game. Deliberately uses a uniform-random AI pick rather than any "smarter" logic: a single round of RPS carries no information to exploit (unlike Tic-Tac-Toe, a solved perfect-information game), so random is the fair baseline, not a corner cut. Same `onGameEnd`/`onReset` contract as `TicTacToe.jsx`, so `Game.jsx` didn't need any changes beyond a registry entry and a render-switch case to support it.
- **`src/games/MemoryMatch.jsx`** — flip-and-match card pairs (6 emoji pairs, 12 cards). No adversary and no lose condition (untimed), so it only ever reports `onGameEnd('win')`, once all pairs are matched.
- **`src/games/Game2048.jsx`** — the standard 2048: a 4x4 grid, arrow-key or on-screen-button moves, tiles slide and merge via the well-known "transpose/reverse, then always slide left" trick (four directions reduce to one merge algorithm instead of four separate ones). Win at a 2048 tile, loss when the board is full and no direction changes it.
- **`src/games/WordGuess.jsx`** — a word-guessing game, deliberately named (and drawn) without any hangman/gallows imagery — just a shrinking heart/life counter — to stay consistent with the site's family-friendly tone. A small on-brand word list (TWEGLE, VIRAL, MEME, etc.), 6 lives, win on completing the word before lives run out.
- **`src/games/GuessTheNumber.jsx`** — "the house" picks a secret 1–100, the player gets higher/lower hints, 7 attempts (enough for a binary search over that range). A good example of a game with a real win/loss condition despite having no visible "opponent" to react to your moves.
- **`src/games/Sudoku.jsx`** — a 9x9 grid, medium difficulty (36 given clues). No puzzle database or backtracking solver needed: every puzzle starts from one closed-form solved grid (`((r*3 + Math.floor(r/3) + c) % 9) + 1`), then gets scrambled via the standard Sudoku symmetry operations — relabeling digits 1-9 with a random permutation, shuffling rows within each band of 3 and columns within each stack of 3, shuffling the 3 bands/stacks themselves, and a 50/50 transpose — all of which preserve the "each digit exactly once per row/column/box" property, so the result is always a genuinely valid, varied solved grid without ever needing to verify it. Blanking out 45 of the 81 cells (reusing the existing `shuffleArray` utility from `utils/shuffle.js`) produces the puzzle; solution-checking is a plain equality comparison against the grid used to generate it, not a general Sudoku validator. Live conflict highlighting (a cell turns red if its value duplicates another in the same row/column/3x3 box) uses a straightforward `hasConflict(grid, r, c)` check. No lose condition, same as Memory Match — only ever reports `onGameEnd('win')`, once every cell is filled and matches the solution. Supports both clicking a number-pad button and typing 1-9/Backspace on the keyboard once a cell is selected.
- **`src/games/SimonSays.jsx`** — the classic growing-sequence memory game: 4 colored pads, the app flashes an ever-longer pattern, the player repeats it by clicking. Endless in principle, so it's given a real "win" state by treating reaching round 10 as mastering it (same milestone-framing idea 2048 already uses for hitting the 2048 tile) — a wrong click before then ends the game as a loss, score being however many rounds were reached either way.
- **`src/games/WhackAMole.jsx`** — a fixed-time (20s) reaction game: one mole pops up at a time in a random hole of a 3x3 grid for under a second, click it before it ducks back down. Score is moles whacked in the window; reaching 15 is framed as a win, anything less a loss (same milestone-framing pattern as Simon Says).
- **`Game.jsx`** (`/games/:slug`) — looks up the slug in the registry, renders the matching game component, and once it ends: records the anonymous outcome via `recordGamePlay`, shows `ShareButtons`, and an `AdSlot`. Share text is per-outcome and, since not every game frames itself as "beating the house" (a solo memory game or 2048 has no opponent to beat), per-game: a `SHARE_TEXT_OVERRIDES` map lets Memory Match/2048/Word Guess/Guess the Number/Sudoku/Simon Says/Whack-a-Mole supply their own win/loss phrasing, falling back to the original "beat the house" wording (`DEFAULT_SHARE_TEXT`) for Tic-Tac-Toe/Rock Paper Scissors, which are explicitly framed that way.
- **`GameCard.jsx`** — the homepage-tab card, styled identically to `QuizCard.jsx` ("N played" badge instead of "N took this"), fed by merging the static registry with `GET /api/games/counts` on the homepage.
- **`GameLeaderboard.jsx`** — the score-saving form shown after a scored game ends (Guess the Number's tries, 2048's tile, etc.). Reads `useUserAuth()`: logged-in visitors see "Saving as {Gamer Tag}" instead of a nickname field and submit with one click; guests keep the original manual "Your nickname" input. Either way, the submitted name is just a string on the score record — no account linkage beyond that.

**Bug caught during testing:** clicking "Play again" correctly reset the board, but `Game.jsx`'s own `outcome` state (which controls whether the share buttons show) didn't know the game had restarted, so the *previous* round's share buttons stayed visible over a fresh game. Fixed by having `TicTacToe` also call an `onReset` prop when the board clears, which `Game.jsx` uses to clear its own `outcome` state back to `null`.

## Two-player Tic-Tac-Toe

A real async match against a friend, separate from the single-player-vs-AI `TicTacToe.jsx` above — reached via a "🆚 Challenge a friend to a real match" link shown only on the Tic-Tac-Toe game page (`Game.jsx`, `game.slug === 'tic-tac-toe'`).

- **No accounts, so no server-side notion of "which browser is X."** Filling in the challenge form calls `createTicTacToeGame(name)`, which creates the match (the creator is always `X`) and immediately writes `localStorage['tictactoe-role-<code>'] = 'X'` *before* navigating to `/games/tic-tac-toe/:code` — that's the only place a role ever gets assigned as `X`. A friend opening the invite link has no such key yet, so `TicTacToeMultiplayer.jsx` shows a join form instead of a board; submitting it calls `joinTicTacToeGame` and writes `role = 'O'` the same way. From then on, revisiting that URL in that browser always resumes with the right role, no login needed.
- **Polling, not real-time.** There's no websocket infrastructure in this app, so while it's not your turn (including "still waiting for someone to join at all"), the page polls `GET /api/tictactoe/:code` every 4 seconds and re-renders when the state changes. This is a nice-to-have, not load-bearing — the game is genuinely *async*: closing the tab and checking back an hour later shows the real current state regardless of whether polling ever ran, same philosophy as the quiz-compare and friendship-quiz invite links.
- **Making a move** (`handleCellClick`) is guarded three ways before it even calls the API — a role must exist, the cell must be empty, and it must actually be that role's turn — then `makeTicTacToeMove(code, role, cell)` sends `{ role, cell }`, and the backend is the real authority on whether the move was legal (see `BACKEND.md`). If the request fails (most likely a timing race — the poll hadn't caught up with the opponent's move yet), the page just refetches the real state instead of showing a scary error.
- The board/status UI reuses the same 3x3 grid styling as the single-player `TicTacToe.jsx`, and `ShareButtons` at two different moments: while `status === 'waiting'` (send the invite), and once `status === 'finished'` (share the result, phrased from that browser's own role's perspective — "I beat Bob," "I drew with Bob," or "Bob beat me — think you can do better?").

## Stories

A new content type, modeled closely on Quiz rather than Post — its own model, own slug, own `/story/:slug` page — rather than reusing `Post`, since a story is one long body of prose with a proper shareable slug, not a short card.

- **`StoryView.jsx`** (`/story/:slug`) — fetches the story, shows the category/title in a gradient card matching `storyStyles.js`, the full body text below it, then `ShareButtons`, an `AdSlot`, a "You might also like" section (4 other stories, same-category-first, same shuffle-fallback pattern as `Result.jsx`/`PostView.jsx`), and a `ReportButton`.
- **"🔊 Listen to this story" / ⏸ Pause / ⏹ Stop** — a small `useReadAloud(text, language)` hook inside `StoryView.jsx` wraps the browser's built-in `SpeechSynthesisUtterance` (Web Speech API): `play()` cancels any in-flight utterance and starts a new one (`utterance.lang` set to `en-US`/`hi-IN` based on the story's language), `pause()`/`resume()` call the matching `speechSynthesis` methods, and a cleanup effect cancels speech on unmount so navigating away mid-narration doesn't leave it talking over the next page. If `'speechSynthesis' in window` is false (unsupported browser), the button is replaced with a plain "just read on below" note instead of throwing — voice availability varies by OS/browser, so this degrades gracefully rather than assuming it always works. No recorded voice, no paid TTS API, nothing server-side — the backend's only involvement is serving the story's `body` text (see `BACKEND.md`).
- **Bug fixed 2026-07-31: switching to a different story via "You might also like" left the previous story's narration still playing.** Reported directly. Root cause: React Router renders `/story/:slug` → `/story/:otherSlug` navigations by reusing the same `StoryView` component instance (just with new params/state), not a fresh mount — so the `useReadAloud` hook's cleanup effect, which was keyed only on `[supported]`, never re-ran when the story changed, only on actual unmount (leaving the site entirely). The new story's text loaded and rendered fine, but the old utterance kept talking, and the play/pause/stop button's `status` state didn't reset either, so it could show stale controls for narration that no longer matched the visible story. Fixed by re-keying the effect on `[supported, text]` instead: since `text` changes to the new story's body the moment it loads, React runs the previous render's cleanup (which cancels whatever was playing) before running the effect again for the new story (which resets `status` to `'idle'`) — the exact same cleanup-then-rerun mechanism already used for unmount, now also triggered by a same-component story swap. Verified in the browser: started narration on one story, clicked a different story from its suggestions, and confirmed `window.speechSynthesis.speaking` became `false` immediately and the button reset to "🔊 Listen to this story" rather than showing stale Pause/Resume controls.
- **`StoryCard.jsx`** — the homepage-tab/search-result card, styled like `QuizCard.jsx` (gradient background from `storyStyles.js`, a "🔊 Read & Listen →" pill instead of "Take the quiz →"), with the same `TileShareButton` every other card gets.
- **`storyStyles.js`** — per-category emoji/label/gradient (Horror/Comedy/Romance/Mystery/Moral Tales/Motivational), same pattern as `postStyles.js`.
- Homepage wiring (`Home.jsx`): a `storyCategory` filter (separate local state from `quizCategory`, since the two tabs have completely different category sets) drives its own row of category chips shown only on the Stories tab, and both category filters plug into the same tagged-content/scroll-reset machinery already built for quizzes (see "Known decisions" below).

## Horoscope

A light, funny, entertainment-only horoscope feature — pulled forward from `PENDING_TASKS.md`'s backlog, placed as a homepage tab immediately after Stories per the request. Unlike every other content type, there's no admin-authored content or database model behind it at all — every horoscope is computed fresh on every request from a small, fixed set of pre-written lines (see `BACKEND.md` for the deterministic formula).

- **Homepage tab** (`key: 'horoscope'` in `Home.jsx`'s `TABS`) fetches the 12-sign list via `fetchZodiacSigns(language)` and renders a grid of `ZodiacCard.jsx` — styled like `QuizCard`/`StoryCard` (gradient background, a "Today's Horoscope →" pill, the same `TileShareButton`). The Trending/Newest sort toggle is skipped for this tab (`sortItems` returns the list as-is) — the 12 signs have a fixed traditional order and no play count or creation date that sorting could meaningfully apply to.
- **`HoroscopeView.jsx`** (`/horoscope/:sign`) — the detail page. Its own local EN/Hindi toggle (separate from the homepage's language state, since a visitor can land here directly from a shared link without ever touching the homepage) and a Day/Week/Month/Year period switcher, both of which re-fetch via `fetchHoroscope(sign, period, language)` on change. Below that: the horoscope text itself, a small disclaimer line ("just for laughs — not real astrology, and definitely not medical/financial/legal advice"), `ShareButtons`, an `AdSlot`, and a link back home. No `ReportButton` — there's no admin-authored or user-submitted content here to flag.
- **View/share tracking** — reuses the existing `recordEngagement` call (`contentType: 'horoscope'`, `contentId` = the zodiac sign's key, same "no database row" pattern already used for games). A view is tracked once per sign per page load (not per period/language switch, so switching periods on the same sign doesn't inflate the count); a share is tracked whenever `ShareButtons` fires.
- **Link previews** — `getHoroscopeShareUrl(sign, period, language)` points at a new `/api/share/horoscope/:sign` backend route (see `BACKEND.md`), same reasoning as every other content type's share URL: a link-preview crawler doesn't run JavaScript, so a plain SPA URL never gets a real preview card.
- **Deliberately not wired into search or the sitemap this pass** — there's no database content to search against (all 12 signs are already one click from the homepage), and a sign's horoscope text changes daily/weekly/monthly/yearly depending on the period, so a single static sitemap entry per sign wouldn't represent the page's content particularly well anyway.

## Puzzles

Admin-authored riddles/brain-teasers — a homepage tab placed immediately after Quizzes (not at the end of the tab list) since they share the same daily-streak mechanism, deliberately overriding the earlier "position TBD" placeholder from the Posts-merge/tab-reorder pass.

- **Homepage tab** (`key: 'puzzles'` in `Home.jsx`'s `TABS`) fetches via `fetchPuzzles(language, difficulty)` and renders a grid of `PuzzleCard.jsx` — an image thumbnail (if `imageUrl` is set) or the puzzle's emoji, the question text (line-clamped), a "Solve it →" pill, and the difficulty label. A dedicated set of difficulty chips (`PUZZLE_DIFFICULTIES`: All/🟢 Warm-Up/🟡 Challenge/🔴 Brain Buster) replaces the usual category-chip row for this tab — the same filter-chip pattern already used for Posts/Quiz categories. Display labels only — the underlying `easy`/`medium`/`hard` values stored on `Puzzle` are unchanged; "Easy/Medium/Hard" read flat for a fun-brand site, so every display spot (`PuzzleCard`, `PuzzleView`, admin `PuzzleList`/`PuzzleForm`) was renamed in one pass, 2026-08-06.
- **`PuzzleView.jsx`** (`/puzzle/:id`) — the detail page. Shows the question, the optional full-width image, and a "🔍 Reveal Answer" button; tapping it reveals the answer card and, only if this is today's Puzzle of the Day, records the shared streak via `recordDailyActivityCompletion` and shows a "🔥 {count}-day streak!" line. Below that: `ShareButtons`, an `AdSlot`, a "You might also like" grid of same-difficulty `PuzzleCard`s, `CrossPromo`, and a `ReportButton contentType="puzzle"`.
- **`PuzzleOfTheDayBanner.jsx`** — sits on the homepage right after the existing `DailyQuizBanner`, picking a puzzle via `pickPuzzleOfTheDay(puzzles)` (same deterministic day-of-year pick as `pickQuizOfTheDay`, sorted by `_id` since puzzles have no slug). Deliberately shows **no streak badge** — `DailyQuizBanner` already shows the shared streak count, and showing the same number on both banners would just be a confusing duplicate.
- **Shared streak, not a parallel one**: `dailyQuiz.js`'s `recordDailyQuizCompletion` was renamed to `recordDailyActivityCompletion(finishedId, todaysId)` — same `localStorage` key (`dailyQuizStreak`) written to by either `Result.jsx` (finishing the daily Quiz) or `PuzzleView.jsx` (revealing the daily Puzzle's answer). Verified directly via `localStorage.getItem('dailyQuizStreak')` after a puzzle reveal — confirms both paths write the exact same key.
- **Admin CRUD** — `PuzzleList.jsx`/`PuzzleForm.jsx` mirror `StoryList.jsx`/`StoryForm.jsx` exactly: search/difficulty/language/status filters, pagination, clone/delete, and a form with Question/Answer (both `<textarea>`, max 500 chars), an optional Image URL field (paste-a-URL — no upload pipeline yet), Difficulty/Emoji/Gradient, and Language/Status/Publish-at.
- **Search and sitemap** — `SearchResults.jsx` gained a "Puzzles" results section using the same `PuzzleCard`; puzzles appear in `/sitemap.xml` at priority 0.5, same tier as Posts/Stories.

## Unified view/share engagement tracking

Post was the only content type with real view/share analytics (`recordPostEngagement`). Asked directly whether that gap also applied to Quiz, Friendship Quiz, and Games (it did — each only tracks completions, not views or shares), all four got the same tracking added in one pass via a single `recordEngagement(contentType, contentId, action)` call in `api.js`, rather than four separate near-identical helpers.

- **View** — fires once per page load (a `viewedRef` guard, same pattern `PostView.jsx` already used) on the page where that content is actually opened: `Quiz.jsx` (`recordEngagement('quiz', quiz._id, 'view')`), `FriendshipSetup.jsx` (`'friendshipQuiz'`), `Game.jsx` (`'game'`, using the game's slug as `contentId` since games have no database row), and `StoryView.jsx` (`'story'`).
- **Share** — fires via the `onShare` prop already supported by `ShareButtons.jsx`, wired onto that same page's share buttons: `Result.jsx`'s primary result share (not the separate "compare with a friend" share, which shares a different link entirely), `FriendshipSetup.jsx`'s "your link is ready" share, `Game.jsx`'s post-outcome share, and `StoryView.jsx`'s share. Tile-level shares (`TileShareButton` on `QuizCard`/`GameCard`/`StoryCard`/`FriendshipQuizCard`) aren't tracked — that component doesn't accept an `onShare` callback, same as it never did for Post either.
- **Admin side** — `fetchEngagementSummary(token, contentType)` in `adminApi.js` hits `GET /api/admin/engagement/:contentType`; `Analytics.jsx` renders one `EngagementTable` per type (Quiz/Friendship Quiz/Game/Story Engagement), reusing the exact column layout (title/Views/Shares) the existing Post Engagement table already used.

## Feedback

A public `/feedback` page (`Feedback.jsx`) — a textarea plus an optional email field, submitting to `POST /api/feedback` via `submitFeedback` in `api.js`. Deliberately minimal: no categories, no login, just "what's on your mind" and a thank-you screen on success. Linked from the footer's "Company" column, next to About/Privacy/Terms. On the admin side, `FeedbackList.jsx` (`/admin/feedback`) follows the exact same list pattern as `Activity.jsx`: paginated (`Pager.jsx`), newest first, unread entries visually highlighted with a "New" badge, and Mark read/unread + Delete actions gated to superadmin/editor (analyst sees the list read-only, same split as every other admin list).

## End-user accounts

Fully optional accounts — no email or phone number collected, and nothing on the site is gated behind login. `UserAuthContext.jsx` (separate from the admin panel's `AuthContext.jsx`, its own `userSession` localStorage key) provides `session`/`signup`/`login`/`logout`/`updateSession` and wraps the whole public site in `App.jsx`.

**Session refresh on load (2026-08-07):** the cached session (displayName/avatar) was only ever written at login/signup or by an explicit `updateSession()` call on that specific device — a profile change made on one device never reached an already-open session on another. Found directly: an account logged into both a phone and a desktop browser showed two different avatars, since only the device the change was made on had the new value (`fetchCurrentUser()` existed in `userApi.js` but was never actually called anywhere). Fixed with a one-time `useEffect` in `UserAuthProvider` that refetches `GET /api/users/me` on mount whenever a cached session exists, syncing this device's copy with whatever the account actually looks like server-side right now — no fresh login required. A failed refresh (e.g. offline, expired token) is silently ignored, keeping the stale cache rather than logging the visitor out.

**Cross-device stats sync (2026-08-07):** the same owner then reported the daily streak count and badge progress *also* differing between a phone and a desktop logged into the same account — expected, since both the streak (`dailyQuiz.js`) and badges (`badges.js`) were entirely `localStorage`-based with no account tie-in at all (this gap was already tracked in `PENDING_TASKS.md`). Built out now that a real account exists to attach it to: `EndUser` gained a `stats: Mixed` field (backend, mirrors the frontend's localStorage shape exactly — see `BACKEND.md`), and a new `frontend/src/utils/statsSync.js` bridges the two directions:
- **Push** — `pushLocalStatsToServer()` reads the current `dailyQuizStreak`/`twegleStats`/`twegleBadgesSeen` localStorage keys and `PUT`s them to `/api/users/me/stats` (fire-and-forget, no-op if logged out). Wired into the two single funnel points every mutation already passes through — `dailyQuiz.js`'s `recordDailyActivityCompletion` and `badges.js`'s internal `update()` — so no call site elsewhere in the app (`Result.jsx`, `Game.jsx`, `TileReactions.jsx`, etc.) needed to change.
- **Pull + merge** — `syncStatsOnLogin(token)` runs once whenever `UserAuthProvider` has a session (on load, and right after login/signup), `GET`s the server's copy, and merges it with whatever's already in this browser's `localStorage` rather than blindly overwriting either side: the streak takes whichever entry has the more recent `lastDate` (the one that actually kept the streak alive most recently); every other stat is a monotonic counter or a set, so merging is just "take the higher number" / "union the arrays" per field — no progress from either device is ever lost by logging in on a new one. The merged result is written back to `localStorage` (so every existing read in `dailyQuiz.js`/`badges.js` picks it up with zero changes there) and pushed back up immediately, so both devices normalize to the same numbers right away rather than waiting for the next local mutation.
- `Badges.jsx`'s copy is now conditional on `session` — "Synced to your account — same progress on every device you log into" when logged in, the original "Tracked only in this browser" wording otherwise.

**Sync only ran once per page load (2026-08-07, same-day follow-up):** reported directly — revealing a puzzle on mobile made its "already attempted" tick show up correctly on mobile, but an already-open desktop tab logged into the same account never picked it up, even after refreshing the Puzzles tab (only a full page reload fixed it). Two separate causes, both fixed:
1. `syncStatsOnLogin` only ran inside a `useEffect` with `[]` deps in `UserAuthProvider` — since that provider wraps the whole app and doesn't remount on client-side tab/page navigation, it fired exactly once per full load and never again, so a change made on another device after that point was invisible until the next hard reload. Fixed by also re-running `syncStatsOnLogin` on every `visibilitychange` to `'visible'` (`UserAuthContext.jsx`) — the moment a visitor switches back to the tab is the most likely point they'd actually check something, without needing to poll continuously while the tab stays active. The effect's dependency array is now `[session?.token]` instead of `[]` so it re-attaches correctly if the session itself changes.
2. Even with a fresh sync running in the background, `hasCompletedQuiz`/`hasRevealedPuzzle` (`badges.js`) read `localStorage` directly at render time with no React state behind them — a background write from a completed sync doesn't by itself force an already-mounted `QuizCard`/`PuzzleCard` to re-render. Fixed by having `syncStatsOnLogin` dispatch a `'twegle-stats-synced'` `CustomEvent` once it's written merged data back to `localStorage` (`statsSync.js`) — the same pattern `badges.js` already used for its unlock-toast event — with `Home.jsx` listening and bumping a dummy `useState` counter to force a re-render of the grid. Verified locally by pushing a puzzle-reveal directly to a test account's server-side stats (simulating a second device) and confirming an already-open, non-navigated tab picked up the checkmark automatically after a simulated tab-focus event.

**Follow-up, same day — `visibilitychange` alone wasn't enough:** reported directly — switching in-app between the Games and Quizzes/Puzzles homepage tabs (not switching browser tabs or apps) never picked up a change made on another device, only a full page reload did. Root cause: `visibilitychange` only fires when the tab itself is hidden/shown (minimized, switched away from, backgrounded) — clicking between homepage tabs is plain in-page navigation, the document never becomes hidden, so the event never fires there at all. A reload "worked" only because a reload always re-syncs on mount regardless of any event. Fixed by adding a plain `setInterval(() => syncStatsOnLogin(session.token), 20000)` alongside the existing `visibilitychange` listener in `UserAuthContext.jsx` — re-syncs every 20 seconds while a session exists, independent of any visibility or navigation event, so switching between in-app tabs (or simply leaving the tab open and idle) converges within a few seconds either way, with no reload needed. Verified locally: cleared a fresh test account's state, switched to the Games tab (in-app, not a browser-tab switch), pushed a puzzle-reveal to that account directly via the API (simulating a second device), waited ~25s with zero visibility changes, then switched back to the Puzzles tab and confirmed the checkmark had appeared on its own.

Also fixed while investigating: `PuzzleOfTheDayBanner.jsx`'s eyebrow text was still "🔥 Puzzle of the Day" while `DailyQuizBanner.jsx`'s said "🔥 Daily Streak" — two different labels for what's actually the same shared mechanic, reported as confusing on mobile where both banners sit side by side. Both now say "🔥 Daily Streak"; the puzzle's own identity is already carried by its emoji and question text below the eyebrow line.

- **Signup** (`Signup.jsx`) — username, password, and a Gamer Tag (the public display name, separate from the private username). On success, shows the one-time **Recovery Code** full-screen with a "save this now, it's the only way back in" warning and a required "I've saved this code" checkbox before continuing to `/account` — the code is never shown again after this screen.
- **Login** (`Login.jsx`) — username + password; wrong credentials show one generic error, never revealing which field was wrong. A "Twegle works fine without one" note is shown for anyone who lands here without meaning to sign up. On success, redirects to `/` (the homepage), not `/account` — a regular login has nothing new to show, unlike signup (which has the one-time Recovery Code to display), so landing on Account every time read as being pulled away from whatever the visitor actually came to do.
- **Forgot Password** (`ForgotPassword.jsx`) — username + Recovery Code + new password. The `TWEGLE-` prefix (identical on every code — see `generateRecoveryCode` in `endUserAuthController.js`) is a fixed, non-editable label next to the code input rather than something the user has to type out; only the `XXXX-XXXX` part is collected and the full code is composed on submit. On success, a brand-new Recovery Code is issued (the old one is immediately invalidated) and shown the same way as at signup.
- **Account page** (`/account`, `Account.jsx`) — shows the (read-only) username, an **avatar picker** (14 fixed preset emoji — 🦄🐱🐼🦊🐸🌟🔥😎🐶🐰🦁🐨🎉🌈 — click one to save instantly via `updateAvatar`, no separate save button; the currently-selected one gets a violet ring), an editable Gamer Tag field, a "Generate a new Recovery Code" action (confirmed via `window.confirm` since it invalidates the current one), and Log Out. Redirects to `/login` if there's no session.
- **Header** (`Header.jsx`) — an account icon pointing at `/account` if `session` exists or `/login` otherwise; updates reactively the instant signup/login/logout/avatar-change happens, no reload needed. Logged-out state is a plain 👤. Logged-in state shows the chosen avatar emoji if one's been picked, otherwise a colored badge with the Gamer Tag's first initial — either way, visually distinct from the guest icon so a returning visitor can tell they're logged in at a glance.

`userApi.js` mirrors the admin panel's `adminApi.js` request-helper pattern (Bearer-token support, thrown `Error` on failure) rather than `api.js`'s ad hoc fetch style, since auth calls specifically benefit from consistent error handling.

This build deliberately does not touch the existing anonymous localStorage mechanisms (daily streak in `dailyQuiz.js`, achievement badges in `badges.js`, or the anonymous play-tracking ID in `api.js`) — those keep working exactly as before for every visitor, logged in or not. Migrating that history onto an account is a separate, deliberately deferred follow-up. One small piece of account integration did land in a later session: `GameLeaderboard.jsx` now uses the account's Gamer Tag automatically when submitting a score if logged in (skipping the manual nickname prompt guests still see) — see "Games" below.

### Admin visibility/moderation

`EndUserList.jsx` (`/admin/end-users`, labeled "Members" in the sidebar/page heading — renamed 2026-08-06 from "End Users," a clearer, less clinical term for the audience) — closes the gap where admins could manage each other (`/admin/admins`) but had zero visibility into regular visitor accounts. Search box (username/Gamer Tag), paginated list, a "Disabled" badge on any account an admin has moderated, and Disable/Re-enable + Delete buttons (hidden for Analyst, who sees the list read-only — same write-role split as every other admin list). Disabling is reversible (a `status` flag, not a delete) and takes effect immediately, not just on the account's next login attempt — see `BACKEND.md`. Nav item sits between Feedback and Admins in the sidebar.

Clicking a row (not the action buttons, which `stopPropagation`) opens a detail modal — avatar, Gamer Tag, private username, status, exact join timestamp — with the same Disable/Delete actions repeated inside for convenience. Below the field list, a plain note (not a fake empty field/value row) states that passwords and recovery codes are never visible to admins — added, then deliberately *not* laid out as a `dt`/`dd` pair, after the owner pointed out that a "Password / Recovery Code" field that always reads "Never visible" looks like a broken field rather than an answer to a real question.

## Report a quiz/post

A small "🚩 Report this" link on `Result.jsx`, `PostView.jsx`, `StoryView.jsx`, and `PuzzleView.jsx` (`ReportButton.jsx`, shared between all four) — reuses the exact same `POST /api/feedback` pipeline as the general Feedback page above rather than a separate system, just with `contentType`/`contentId`/`contentLabel`/`reason` attached (see `BACKEND.md`). Clicking it reveals a small inline form (radio-button reason: offensive / incorrect / broken / other, plus an optional details textarea) in place of the link, then a quiet "Thanks — we'll take a look. 🙏" on success — no navigation away from the quiz result or post. `FeedbackList.jsx` shows a red "🚩 Report — {reason}" badge and the reported item's label on any entry that has these fields set, so an admin can tell a report apart from general feedback at a glance.

## Share before attempting

Every share button on the site used to be tied to *after* finishing something — a quiz result, a friendship-quiz score, a game outcome, or opening a post. There was no way to invite a friend to anything before either of you had opened it. Built once as a shared component, then wired into **every** content tile: `QuizCard.jsx`, `GameCard.jsx`, `PostCard.jsx` (jokes/funny lines/quotes/motivational quotes/memes), `StoryCard.jsx`, and `FriendshipQuizCard.jsx` all show the same small 🔗 icon in the card's corner.

- **`TileShareButton.jsx`** (new shared component, `frontend/src/components/`) — takes `{ title, shareUrl, shareText }` and renders the icon plus a floating popover (Share/WhatsApp/Copy link). Every card passes its own item-specific `shareUrl`/`shareText`, so sharing any tile always previews *that specific* quiz/game/joke/meme/friendship quiz, never a generic site-wide link. Each card's `shareUrl` points at a crawler-friendly backend `/api/share/*` route (see `BACKEND.md`) built from that item's own title/description:
  - Quiz → `getQuizIntroShareUrl(slug)` → `GET /api/share/quiz/:slug`
  - Game → `getGameShareUrl(slug)` → `GET /api/share/game/:slug` (new — games have no database row, so the backend keeps a small `GAME_META` map mirroring `games/registry.js`, same duplication pattern as `GAME_SLUGS` already used for play-tracking)
  - Post (joke/funny line/quote/motivational quote) → `getPostShareUrl(post._id)` → `GET /api/share/post/:id` — this route already existed (built for the individual `/post/:id` page's own share buttons) and needed no changes; the tile just reuses it
  - Friendship Quiz → `getFriendshipQuizIntroShareUrl(slug)` → `GET /api/share/friendship-quiz/:slug` (new — shares the *template* before anyone's filled it in, distinct from the existing instance/result share routes)
  - Story → `getStoryShareUrl(slug)` → `GET /api/share/story/:slug`
- **The popover is `absolute`, not part of the card's normal flex flow.** A first version (built for `QuizCard.jsx` only, before this was generalized) rendered it as a plain block between the description and the action pill, which pushed that pill down and made one card visibly taller than its row-mates whenever its popover was open — breaking the equal-card-height invariant documented under "Known decisions" below. Floating it above the content instead (`position: absolute`, anchored to the icon) fixes this: opening/closing the popover never changes the card's own height.
- **The popover's buttons are plain `<button>`s, not `<a>` tags**, even for WhatsApp (which normally is a real link elsewhere in the app, e.g. `ShareButtons.jsx`). Every one of these cards is itself a real `<Link>` (so ctrl-click/right-click "open in new tab" and crawlers still work) — nesting an `<a>` inside another `<a>` is invalid HTML and breaks click behavior, so the WhatsApp button instead does `window.open(waUrl, '_blank', 'noopener,noreferrer')` from a plain button. Every handler also calls both `preventDefault` and `stopPropagation`, since the popover sits inside the card's own `<Link>` and a click needs to reach the button without also navigating into the item underneath it. Closes automatically on an outside click (a `mousedown` listener attached only while open).

## Back navigation

Every content detail page (Quiz, Result, PostView, StoryView, HoroscopeView, Game, TicTacToeMultiplayer, FriendshipSetup/Play/Result, CompareInvite/Result) now shows a "← Back" control at the top, via a new shared `BackButton.jsx` component — requested directly, since previously only some pages had any "back to home" link at all, and only at the bottom of the page.

- **Uses browser history (`navigate(-1)`), not a fixed link to `/`.** This is the key design choice: since the homepage's active tab/filter lives in the URL (`?tab=stories`), going back via actual history returns to the *exact* prior URL — tab and filter included — rather than resetting to a blank `/` (which the homepage always renders as the Quizzes tab). This directly fixes a reported bug: clicking a story from the Stories tab, then clicking Back, used to always land back on Quizzes; now Stories stays selected.
- **Falls back to a plain `<Link to="/">`** when there's no real browser history to go back to — `location.key === 'default'` is React Router's own signal that this is the first entry in the session's history, which happens when a visitor opens a content page directly (a shared link, a bookmark) rather than navigating from within the app. Without this fallback, `navigate(-1)` in that case would either do nothing or leave the app entirely (back to whatever page — if any — was open before Twegle in the browser tab's history).
- Deliberately doesn't touch the existing bottom-of-page links ("Take another quiz," "See more," "Back to Twegle") — those serve a different intent (browse more content, fresh start), so they still point at a plain `/`.

## Social media links (footer)

`Footer.jsx` now shows Instagram/Facebook/YouTube/LinkedIn icons next to the brand blurb (2026-08-01, requested once the actual accounts existed) — plain `<a target="_blank">` links to the handles actually registered: `instagram.com/twegle.official`, `youtube.com/@twegle`, `linkedin.com/company/twegle` (Instagram needed the `.official` suffix since the bare `twegle` handle was already taken there; the other two got the exact handle). Also fixed a real gap found while adding these: the Horoscope tab had no footer link at all in the "Explore" column, unlike every other content type — added `/?tab=horoscope` alongside the rest.

- **Facebook uses the numeric-ID URL, not a vanity one.** `facebook.com/twegle` doesn't resolve — Facebook only lets a Page claim a short custom username once it has roughly 25 followers, so a brand-new Page is only reachable at its long `facebook.com/profile.php?id=...` URL until then. Using the real URL that works today, not the vanity one that'll exist eventually.
- **Real inline SVG icons, not emoji.** The first pass used 📷/👍/▶️/💼 as quick placeholders — replaced with small hand-written `currentColor` SVGs (`InstagramIcon`/`FacebookIcon`/`YoutubeIcon`/`LinkedinIcon`, defined directly in `Footer.jsx`, no new icon-library dependency for just four icons) after feedback that emoji don't clearly read as "this is the Instagram/Facebook/etc. link" — they render as tiny, inconsistent glyphs across platforms/fonts, unlike a recognizable brand mark.

## "You might also like" suggestions

`Result.jsx`, `PostView.jsx`, and `StoryView.jsx` all end with 4 suggested-next cards (`QuizCard`/`PostCard`/`StoryCard`, same components used everywhere else, so they look identical to the homepage grid) instead of just a bare "take another quiz"/"see more" link — 4 rather than 3 so the 2-column grid (see below) fills evenly as a 2×2 block instead of leaving one card alone on its own row. Fetches everything in the same language, prefers items in the same category as the current one, shuffles within each group (`src/utils/shuffle.js`) and falls back to other categories if there aren't enough same-category items, then takes the first 4 — so a small content library (e.g. only 2 posts in one category) never breaks. No new backend endpoint — reuses the existing `fetchQuizzes`/`fetchPosts`/`fetchStories` list calls, filtering out the current item client-side.

**Bug fixed 2026-07-31: cards were badly cramped in this section.** All three of these pages are narrow single-column reading pages (`max-w-xl`, 576px), but the suggestion grid was `sm:grid-cols-3` — the same 3-column count used on the wide homepage grid (`max-w-6xl`, 1152px). Squeezed into the narrower page, each card landed at only ~170px wide, and `QuizCard`/`StoryCard`'s bottom-row badge (`"Take the quiz →"` / `"🔊 Read & Listen →"`) had no `whitespace-nowrap`, so its text wrapped internally across 3-4 lines instead of staying on one line — ballooning card height (up to ~340px) and visually overlapping the category label next to it. Fixed two ways: the grid is now `sm:grid-cols-2` on all three pages (giving each card ~264px, plenty of room), and `whitespace-nowrap` was added to every card's bottom-row badges (`QuizCard`, `StoryCard`, and — proactively, since they have the exact same pattern — `GameCard`/`FriendshipQuizCard` too, even though neither is used in a suggestions section yet) so the wrap can't recur if a card ever ends up in an even narrower spot. `SUGGESTION_COUNT` was also bumped from 3 to 4 right after, once the 2-column grid made 3 items look lopsided (2 cards on one row, 1 alone on the next) — 4 fills a clean 2×2 block instead. Verified via computed `getBoundingClientRect()` measurements in the browser: story suggestion cards went from 332px tall (badge wrapped to 112px) down to 208px tall (badge a clean single-line 32px), same improvement confirmed on quiz and post suggestions; confirmed all three pages render exactly 4 suggestion cards after the count change.

## Search

`Header.jsx` (rendered once for the whole public site, not per-page) now carries a search box alongside the logo. Submitting navigates to `/search?q=<query>` (`SearchResults.jsx`), which calls the new `GET /api/search` endpoint and groups whatever comes back into up to three sections — Quizzes, Friendship Quizzes, and Jokes/Quotes/etc. — reusing `QuizCard`/`FriendshipQuizCard`/`PostCard` so results look identical to browsing the homepage. An `AdSlot` renders below the results (once there are any) — added 2026-08-05 alongside the same addition to `Home.jsx`, since both previously had zero ad placement at all (every other `AdSlot` usage was on result/detail pages only). The header's input syncs itself from the URL's `?q=` (via a `useEffect` on `useSearchParams()`), so landing on a search results page (whether by submitting the form or opening a shared search link) shows the active term and lets you refine it without retyping — and clears itself again once you navigate to an unrelated page.

## Merged Posts tab + homepage tab reorder (2026-08-05)

The post categories (Jokes/Funny Lines/Quotes/Motivational Quotes — Memes was a 5th category until 2026-08-06, see below) share one **"💬 Posts"** tab in `Home.jsx` — no backend/model change (`Post` already had one `category` field), purely a frontend consolidation. Selecting Posts fetches every category at once (`fetchPosts(undefined, language)`) and shows them as one continuous mixed feed; a `POST_CATEGORIES` filter-chip row (All / Jokes / Funny Lines / Quotes / Motivational) — same UI pattern as `QUIZ_CATEGORIES`/`STORY_CATEGORIES` — narrows it back down to one category on demand (`fetchPosts('joke', language)` etc.), tracked in `postCategory` state alongside the existing `quizCategory`/`storyCategory`. `Browse.jsx`'s standalone `/browse/jokes` etc. pages are untouched — they never depended on the homepage tab list, so nothing there needed to change.

`TABS` was also reordered at the same time, chosen explicitly through a monetization lens (discussed directly with the owner — goal: maximize pageviews/ad impressions): **Quizzes → Friendship Quiz → Games → Posts → Stories → Horoscope**. Quizzes stays first — biggest content library and the strongest solo share loop (a quiz result reshares on its own, no second person needed), so it's the strongest driver of pageviews. Friendship Quiz is second, not first — same growth-loop idea, but requires convincing a friend to join, more friction on a first click than the owner's initial draft order assumed. Games next (replayable, own "beat me" share hook), then Posts, then Stories/Horoscope last (longer-dwell, less share-driven). `Footer.jsx`'s "Explore" column gained a `/?tab=posts` link alongside the existing per-category `/browse/*` deep links (kept, since those are still valid standalone pages).

**Memes removed (2026-08-06):** the Memes post category (`category: 'meme'` on `Post`, an image-URL-driven card type with a caption instead of required text) was removed entirely at the owner's direct request ("not looking useful") — full removal, not just hidden: taken out of the `Post.category` enum and every place that branched on it (`postController.js`'s validation, `PostForm.jsx`'s Image URL field, `PostCard.jsx`'s image-rendering branch, `PostView.jsx`'s meme-specific reveal/download UI, `PostList.jsx`'s thumbnail preview, the homepage `POST_CATEGORIES` chip, `Browse.jsx`'s `/browse/memes` route, `Footer.jsx`'s link, and the 6 seeded meme posts, deleted from both local dev and production). See `BACKEND.md` for the backend side of the removal and `PENDING_TASKS.md` for the record of what shipped originally and when it was removed.

## Visual polish

- Custom favicon (gradient circle + "?") and a playful font pairing (Baloo 2 for headings, Nunito for body text) instead of the generic Vite defaults.
- Subtle entrance animations: quiz questions fade/slide in as you progress, the result reveal has a celebratory "pop" animation, and homepage cards stagger in on load.
- Skeleton loading placeholders (pulsing gray blocks) instead of plain "Loading..." text on the homepage, quiz page, and result page.
- Colors on quiz cards come from a fixed, safelisted palette (`src/gradients.js`) rather than free text — see "Known decision" below for why that matters.
- **Credibility pass:** a colorful gradient hero band on the homepage (replacing a plain heading), a real multi-section footer (`Footer.jsx` — brand blurb, category links, legal, copyright, replacing two bare links), and a subtle purple dot-texture background (`.bg-dot-pattern` in `index.css`) instead of flat gray site-wide. Aimed at closing the "this looks like an unfinished demo" gap a first-time visitor notices in the first couple of seconds.
- **"Share Twegle" button:** every quiz result and post already had real share buttons, but there was no way to share the site itself. Built as a persistent floating rail (`ShareSidebar.jsx`) fixed to the left edge of the viewport, vertically centered — the more standard placement on blogs/news sites, and immediately visible rather than requiring a scroll to the bottom of the page — shown on **every page, including the homepage**. Shown at `xl:` and up only (`hidden xl:flex`), not `lg:` — a fixed rail has no room on a phone screen at all, and on the homepage specifically, the rail's fixed 16px-from-viewport-edge position visually collided with `Home.jsx`'s own left filter column (language/tab/category chips) at roughly 1024–1220px-wide screens, since both sit flush against the same edge once the homepage's `max-w-6xl` container can no longer center with real margin. Bumping the rail's breakpoint to `xl` (1280px) — the point at which the centered container reliably has clearance — fixed the collision without hiding the rail from any page. `Footer.jsx` keeps an inline "Share Twegle" section (`xl:hidden`) to cover every width below that on every page, and it's always visible on mobile (no floating rail fits on a phone). No new backend route needed for any of this: `index.html`'s existing static `og:title`/`og:description` tags (added as the generic fallback preview for links that aren't a specific quiz/post) already give the bare homepage link a real preview card.
- **Quiz categories** (Beauty & Skincare, Bollywood & Pop Culture, K-pop, Lifestyle & Personality, Fun & Random) with homepage filter chips under the Quizzes tab — added because a flat, uncategorized list stops working once there are dozens of quizzes across unrelated topics. (A "Trending on Social Media" category briefly existed on 2026-07-30 — built, then removed the same day at the owner's request; see `PENDING_TASKS.md`.)
- **Play-count social proof** on quiz cards (e.g. "15 took this") — pulled from the same `PlaySession` data the admin analytics dashboard already used, now also exposed on the public `GET /api/quizzes` response.
- **Engagement badges (2026-08-06):** a round of UX polish from direct owner feedback. Raw low counts ("2 took this") read as unpopular rather than early, so `src/utils/engagementLabel.js` (a small shared pure function, used by `QuizCard.jsx`, `GameCard.jsx`, and `FriendshipQuizCard.jsx`) swaps counts of 0-4 for "🆕 New" (0 included — otherwise a fresh friendship-quiz tile with no attempts yet showed a blank badge slot next to tiles that always show something, which read as broken rather than intentionally quiet) and 5-14 for "⭐ Popular"; counts of 15+ fall back to the real number, since a big number is stronger social proof than a badge. Games always show a static "🎮 Instant" alongside their badge (all games are quick, no per-game variation needed). Deliberately did **not** add a separate "Trending"/"Editor's Pick" static badge — the homepage's existing "🔥 Trending" sort toggle already owns that word, and a second, different meaning of "Trending" as a per-tile badge risked confusing the two; a manual "Editor's Pick" curation flag was also deliberately deferred since it's a real new admin workflow, not just UI, and wasn't asked for. Quiz cards briefly also showed a "⏱ ~N min" time estimate the same day, computed from a new `questionCount` field on `GET /api/quizzes` — **removed later the same day** at the owner's request: combined with the engagement badge, the two-part string ("⏱ ~2 min · 🆕 New") was long enough to wrap onto a second line on narrow tiles via the row's `flex-wrap`, visibly inflating every quiz card's height by an extra row versus every other card type. Reverted `QuizCard.jsx` back to a single badge (no time estimate) and removed `questionCount` from the backend response entirely (nothing else used it) — the button pill + badge now reliably sit on one line, matching Game/Friendship-Quiz cards' height.
- **Hero copy + banner height (2026-08-06):** the H1 previously listed content types ("Quizzes, Puzzles, Jokes, Quotes, Games, Stories & Horoscope") — told visitors what Twegle has, not why to stay. Changed to lead with the benefit: "Your Daily Dose of Fun — Play, Laugh & Discover Something New," with the content-type list moved into the subtext line instead. This only changes the visible `<h1>` on `Home.jsx` — `index.html`'s static meta tags and `useDocumentMeta.js`'s `DEFAULT_DESCRIPTION` (both still list every content type, for SEO/link-preview purposes) are untouched, since `Home.jsx` never calls `useDocumentMeta()`. The purple hero band itself was also shrunk further (`py-3`→`py-2.5 sm:py-3`, heading `text-xl`→`text-lg` on desktop, roughly a 20-25% height cut) so an extra row of real content is visible without scrolling.
- **Search bar + tagline (2026-08-06):** the header search input got a 🔍 icon, a stronger border, and a `focus:ring-4` glow on focus (previously easy to miss as an actual search field). The "Where Fun Goes Viral" tagline next to the logo was bumped from `text-xs italic text-gray-400` (nearly invisible) to `text-[13px] font-medium text-gray-500` — still secondary to the logo, but now legible.
- **Instant tooltips on the share rail (2026-08-06):** `ShareSidebar.jsx`'s three icons (📤 native share, 💬 WhatsApp, 🔗 copy link) previously relied on the native `title` attribute for a label — delayed and easy to miss, and the likely cause of direct feedback that the 📤 icon's meaning wasn't obvious. Replaced with a `group relative` + `group-hover:opacity-100` custom tooltip that appears instantly on hover, positioned to the right of each icon (`left-full ml-2`) so it isn't clipped by the viewport edge the rail sits against. `title` was replaced with `aria-label` so screen readers still get the label.
- **"Already attempted" mark on Quiz/Puzzle tiles (2026-08-07):** the homepage grid had no way to tell which quizzes/puzzles a visitor had already done, so a repeat visitor couldn't tell fresh content from things they'd already tried. Scoped down from the original three-content-type idea to just Quiz and Puzzle — Friendship Quiz tiles were left out since clicking one *creates your own instance* to send to friends (there's no real "have I already done this" question for a template you'd fill in fresh each time), so the "attempted" concept doesn't map cleanly there. Reuses `badges.js`'s existing anonymous stats blob rather than adding a new tracking mechanism: quizzes already recorded a `quizzesCompleted` array (used by the Quiz Explorer badge); puzzles gained a matching `puzzlesRevealed` array, written by a new `recordPuzzleRevealed(puzzleId)` called from `PuzzleView.jsx`'s `handleReveal` for *every* puzzle (not just the daily one, unlike the streak recording right next to it). Both feed the account-sync merge in `statsSync.js` for free, so the mark is consistent across devices for logged-in visitors too. **The hard constraint was that tile height must not change** — a checkmark that pushed content down would make that one card taller than its row-mates (the same invariant `TileShareButton.jsx` was built around). Solved by making the mark a small `absolute`-positioned emerald circle (`✓`) overlapping the bottom-right corner of the tile's emoji (or thumbnail image, for picture puzzles) — never a flex sibling, so it can't add height. Verified directly: measured the exact same tile's `getBoundingClientRect().height` with and without the mark present (`localStorage` toggled between calls) and confirmed it's pixel-identical either way.
  - **Bug found right after shipping**: the mark never appeared for personality-type quizzes (the majority of quizzes on the site — only 1 of the current library is trivia-type), reported directly ("played Which Squishy Are You, green tick not displayed"). Root cause: `Result.jsx`'s completion-tracking effect (which calls `recordQuizCompleted`) was gated on `location.state.score`/`total` being present, to distinguish "this browser just finished it" from a cold visit to a shared result link (same URL either way). But `Quiz.jsx` only ever passed `score`/`total` in that router state for trivia quizzes — `state` was `undefined` entirely for personality quizzes, so the gate silently blocked `recordQuizCompleted` for every one of them; puzzles had no such gate (`recordPuzzleRevealed` runs unconditionally), which is why puzzles worked immediately but quizzes didn't. Fixed by having `Quiz.jsx` always pass `{ finished: true }` in router state regardless of quiz type (with `score`/`total` added on top for trivia), and re-pointing `Result.jsx`'s gate at `finished` instead of `score`/`total` — the trivia-only perfect-score check still reads `score`/`total` separately. Verified by actually playing both a personality quiz and the trivia quiz through the real UI end-to-end and confirming `quizzesCompleted` picked up both slugs and the homepage tile mark appeared for each.
- **Layout width fix:** Header, Footer, and Home were capped at `max-w-3xl` (768px), which on any normal desktop screen left huge empty gutters on both sides (roughly 23% of a 1440px-wide screen, each side) — a big part of why the site read as "not a proper website." Widened those three to `max-w-6xl` (1152px) and added a third grid column on large screens (`lg:grid-cols-3`) for quiz/post cards, cutting the gutters to about 11% each side and using the extra width to show more content instead of empty space. Quiz-taking/result/post pages intentionally stay narrower (`max-w-xl`) since those are single-column reading flows, not grids — widening them would hurt readability rather than help.
- **Daily streak visibility fixes (2026-08-06, two passes):** `DailyQuizBanner.jsx`'s streak indicator ("🔥 Day N") used to live inside the same `truncate`-clamped title line as "Quiz of the Day" — on the compact 2-column mobile row (this banner shares a row with Puzzle of the Day), the combined text got clipped and the streak silently disappeared. First pass: moved the streak into its own `shrink-0` chip that can't be truncated away, reworded from a bare number ("🔥 3") to "🔥 Day 3." Second pass, after the owner reported this still didn't make it clear the daily quiz *is* a streak mechanic (nothing communicated that at zero streak, before a visitor's first completion): reworked the eyebrow label itself from "Quiz of the Day" to "🔥 Daily Streak" — always visible regardless of streak count, so the mechanic is signaled up front rather than only appearing once a streak already exists. `PuzzleOfTheDayBanner.jsx`'s eyebrow got a matching "🔥" prefix (no count — `DailyQuizBanner` already shows the one shared number, repeating it would be redundant) so it's clear revealing today's puzzle also counts toward the same streak, which wasn't visible anywhere before; confirmed in code that this was already true (`PuzzleView.jsx`'s `handleReveal` calls the same `recordDailyActivityCompletion`, gated on `isDailyPuzzle`), just never surfaced in the UI.
- **Admin logo icon invisible bug fix (2026-08-06):** `AdminLayout.jsx` mounts `LogoMark`/`LogoWithWordmark` up to 3 times simultaneously (mobile top bar, mobile off-canvas drawer, desktop sidebar), and `Logo.jsx`'s inline SVG used a hardcoded `id="twegle-logo-g"` on its gradient `<defs>` — with duplicate IDs across multiple SVGs on one page, and one copy sitting inside a hidden (`lg:hidden`, `display:none`) container, the browser could fail to resolve `fill="url(#twegle-logo-g)"` elsewhere and paint the icon square as unfilled/transparent — leaving only the white inner shapes, invisible against a white sidebar. Fixed with React's `useId()`, generating a unique gradient id per component instance so there's never a collision regardless of how many copies are mounted at once. Also added the logo to `admin/pages/Login.jsx`, which never had one at all (a separate, pre-existing gap, not a regression).
- **Admin sidebar logout button unreachable fix (2026-08-06):** the desktop `<aside>` in `AdminLayout.jsx` is a fixed `lg:h-screen` box with no scroll of its own; once the nav item count grew (10 base items + up to 3 role-gated extras), total sidebar content could exceed a shorter laptop's viewport height, pushing the Log out button below the visible screen with no way to scroll to it — the page's own scrollbar doesn't affect a sticky, fixed-height sidebar. Fixed by adding `overflow-y-auto` to the desktop `<nav>` only, so the nav list scrolls internally while the logo (top) and Log out button (bottom) stay pinned in view. Verified directly by forcing a 500px-tall viewport and confirming the button's bounding box stays within it.
- **Hero shrunk:** the original hero stacked heading, subtext, and a stats row across ~280px of vertical space, pushing the actual quiz grid below the fold. Rebuilt as a single compact horizontal band (heading+subtext on one side, stats on the other, stacking only on mobile) at ~128px tall.
- **Hero + daily banners compacted further for mobile (2026-08-06)**, after the Puzzle of the Day banner (added alongside Puzzles) made the combined stack of hero + two full-width banners noticeably tall on a phone before any real content appeared. The stats row (`🎯 N quizzes · 💬 N jokes · 🌐 EN/HI`) is now `hidden` below `sm` — decorative/secondary info a mobile visitor doesn't need before scrolling. `DailyQuizBanner` and `PuzzleOfTheDayBanner` render side by side in a `grid-cols-2` row instead of stacked full-width — each is now a single compact line (icon + title, with the streak folded into the small label line instead of a separate pill off to the side, which used to occasionally wrap the quiz title onto a second line). Net effect: the hero+banners block is roughly half its previous height on a phone.
- **Filters moved to a sidebar on desktop:** the language switcher, content-type tabs, quiz-category chips, and Trending/Newest toggle used to stack as four separate centered rows above the grid — on a desktop screen that pushed the first real quiz/post card down by roughly 250-300px of pure filter UI before any content appeared. Now, at the `lg` breakpoint (1024px+), they live in a `220px`-wide sticky sidebar (`lg:grid-cols-[220px_1fr]` on the content wrapper) to the left of the grid, with the tabs/category-chip groups switching from wrapped horizontal pills to a real vertical list (`lg:flex-col`) so they read like a proper sidebar nav. The grid now starts at the same vertical position as the sidebar, immediately below the hero. Below `lg`, everything falls back to the original stacked/centered pill rows — there isn't enough width for a sidebar on a phone.

## Admin panel — what it does right now

Reachable at `/admin/login` (no link from the public site — admins go there directly). `AdminLayout.jsx` uses a **left sidebar nav** (logo, Quizzes/Puzzles/Posts/Stories/Friendship Quizzes/Analytics/Activity/Feedback/Members/Admins links — each paired with an emoji, e.g. "🎯 Quizzes" — user info + logout) at `lg:` and up — sticky, full-height, room to grow the nav list without recrowding. **Collapsible** on desktop: a small circular toggle button straddles the sidebar's right edge (`‹`/`›`), shrinking it to a 64px icon-only rail (emoji only, labels hidden) or expanding it back to the full 224px version with labels — the choice persists across page loads via `localStorage`. Collapsing only exists at `lg:` and up (the whole point is reclaiming a *permanent* sidebar's width; the mobile version below has no fixed-width sidebar to reclaim space from in the first place).

**Mobile nav (below `lg`)**: a fixed top bar (logo + hamburger only) plus an off-canvas drawer that slides in from the left with a backdrop, rather than an inline dropdown that pushed page content downward. Left, not top or right — spatially consistent with where the sidebar already lives on desktop, and the more conventional pattern for an admin panel. Background scroll is locked while the drawer is open. Uses an inline `style={{ transform: ... }}` for the slide animation rather than Tailwind's `translate-x-*` utility classes — those weren't resolving reliably in this project's Tailwind v4 setup for a class toggled via template-literal interpolation.

- **Login** — email + password, JWT stored in the browser, role-based like the backend (`superadmin` / `editor` / `analyst`).
- **`/admin/quizzes`** — list all quizzes (draft and published), with a **search box (by title) and category/language/status filter dropdowns** above the list — added once the list grew past 20 items and scrolling through everything stopped being practical. Filters re-query the backend (debounced ~250ms so typing doesn't fire a request per keystroke) rather than filtering client-side, so it scales as more quizzes get added. **Paginated** (`Pager.jsx`, 20 per page) — changing a filter resets back to page 1 automatically, so a filter can never leave you stranded on a now-empty page. Each row also has a **Clone** button (copies the quiz as a new draft, so making a Hindi version of an existing quiz — or any variant — doesn't mean retyping everything) and shows **"scheduled for &lt;date&gt;"** instead of a flat "published" label when a quiz has a future publish date (see `StatusLabel.jsx`). Editors and Super Admins get "New Quiz" / "Clone" / "Edit" / "Delete"; Analysts see the same list read-only.
- **`/admin/quizzes/new`** and **`/admin/quizzes/:id/edit`** — a form to build a quiz without touching code: title/description/emoji/status/**Quiz Type**/category/language/card color, an optional **"Publish at"** date/time (leave blank to go live immediately once Published; set a future date to keep it hidden from visitors until then, even though Status already says Published), a dynamic list of **results**, and a dynamic list of **questions**. The Quiz Type dropdown (Personality/Trivia) changes what the results and question-options sections ask for: **Personality** — each result gets a short key/emoji/title/description, and each answer option picks its outcome from a dropdown of the keys you've defined; **Trivia** — each result additionally gets a Min/Max score, and each answer option gets a plain "Correct" checkbox instead of the key dropdown (see "Non-personality (trivia) quizzes" below). Restricted to Super Admin/Editor.
- **`/admin/posts`** — list all jokes/funny lines/quotes/motivational quotes, with the same search (by text or author) + category/language/status filter bar, pagination, Clone button, and scheduled-status label as the Quizzes list. Same read/write split by role as quizzes.
- **`/admin/posts/new`** and **`/admin/posts/:id/edit`** — a much simpler form than the quiz builder: category, language, status, an optional "Publish at" date/time, the text itself, and an optional author (for attributed quotes).
- **`/admin/friendship-quizzes`**, **`/admin/friendship-quizzes/new`**, **`/admin/friendship-quizzes/:id/edit`** — manage the *question templates* only (title/description/emoji/status/language/card color/publish-at + a dynamic list of questions, each with 2+ plain-text answer options — no "results" step like the regular quiz builder, since there's no personality-outcome concept here). Same Clone button, scheduled-status label, and pagination as the other two lists (no search/filter here — the template count is small enough that it hasn't needed one yet). What real visitors generate (their answer instances and friends' scored attempts) isn't admin-managed content, same as quiz plays aren't editable rows either.
- **`/admin/analytics`** — six tables: total plays/unique players per quiz, total views/shares per post, and total views/shares per quiz, friendship quiz, game, and story (see "Unified view/share engagement tracking" below). Visible to all roles.
- **`/admin/activity`** — a simple audit-trail table: who created/edited/deleted each quiz, post, or friendship quiz, and when. Paginated (`Pager.jsx`, 50 per page) rather than a flat 200-entry cap — added 2026-08-06 after the owner asked whether admin list pages would keep working once there's a lot more data; every other content list already had real pagination, this was the one gap. Visible to all roles, read-only for everyone (there's nothing to "manage" here, just a log). See `BACKEND.md` for what triggers an entry.
- **`/admin/admins`** — create/remove other admin accounts and set their role. Super Admin only. Laid out as a two-column grid (the "Add a new admin" form in a fixed ~360px column, the existing-admins list taking the rest) instead of one column capped at `max-w-2xl` — the old layout left roughly half the page empty on any normal screen, unlike every other admin page which already used the full content width.
- **`/admin/feedback`** — paginated list of visitor feedback, newest first, unread entries highlighted with a "New" badge. Mark read/unread and Delete are Super Admin/Editor only; Analysts see the list read-only. See "Feedback" above.

All of this was tested directly in the browser end-to-end: logging in, creating a quiz, editing one, confirming an Analyst can't see write buttons or the Admins page, confirming a play recorded from the public quiz flow shows up in Analytics, cloning a quiz and confirming it appears as a draft copy, scheduling a quiz for an hour in the future and confirming it's genuinely invisible to the public API until then, confirming create/delete actions show up correctly in the Activity log, confirming the sidebar stays sticky on scroll and stacks correctly on mobile with no horizontal overflow, confirming pagination (page counts, Next/Previous, and the page-resets-on-filter-change behavior) on all three lists, confirming the sidebar collapses to a 64px icon rail and back (a stray `transition-[width]` utility caused the width to visibly get stuck mid-toggle — removed rather than debugged further, since animating this wasn't essential to the feature), confirming the Admins page's form+list now sit side by side using the full content width instead of leaving half the page empty, submitting real feedback through the public `/feedback` form and confirming it appears correctly in `/admin/feedback` with an unread badge, confirming the `TileShareButton` popover on quiz/game/post/meme/friendship-quiz tiles opens with the correct item-specific WhatsApp link and never changes the tile's height (checked via `getBoundingClientRect()` before/after toggling), confirming all three new/reused `/api/share/*` routes (game, friendship-quiz, and the existing post route) render correct crawler previews via curl, and playing a full round of Sudoku (selecting a cell, filling it via both the number pad and the keyboard, confirming a duplicate value in the same row is highlighted in red, and confirming "New Puzzle" generates a fresh, differently-shaped board).

## How data flows now

- `src/api.js` — public-site API calls (`fetchQuizzes`, `fetchQuizBySlug`, `recordPlay`, `fetchPosts`, `fetchPostById`, `recordPostEngagement`, `fetchStories`, `fetchStoryBySlug`, `recordEngagement`, `submitFeedback`, plus the friendship-quiz set: `fetchFriendshipQuizzes`, `fetchFriendshipQuizBySlug`, `createFriendshipInstance`, `fetchFriendshipInstance`, `submitFriendshipAttempt`, `fetchFriendshipAttempt`), all language-filter aware. Also generates and stores a random anonymous ID in `localStorage` for play/engagement tracking — never tied to any personal information.
- `src/admin/adminApi.js` — authenticated admin API calls (quizzes, posts, stories, friendship quizzes, admins, analytics), all requiring the JWT from login.
- `src/admin/AuthContext.jsx` — holds the logged-in admin's session (token + role), persisted in `localStorage` so a refresh doesn't log you out.
- `src/admin/ProtectedRoute.jsx` — redirects to `/admin/login` if not logged in, or shows a permission message if the role doesn't match what a route requires.

There is no more local mock data file — everything the public site or admin panel shows comes from the real API.

## Folder structure

```
frontend/
  vite.config.js            build config + VitePWA() manifest/service-worker setup
  scripts/generatePwaIcons.mjs  one-off PWA icon PNG generator (see "PWA support")
  public/icon-*.png, apple-touch-icon.png  generated PWA icons (checked in, not source-drawn)
src/
  main.jsx, App.jsx        entry point + all routes (public site + /admin/*)
  api.js                    public-site API calls
  gradients.js              safelisted quiz card colors (see "Known decisions")
  postStyles.js             per-category color/emoji for post cards
  storyStyles.js            per-category color/emoji for story cards
  utils/shareImage.js        canvas-based shareable-image generator
  utils/useDocumentMeta.js   per-page title/meta description hook (see "SEO")
  pages/                    Home (tabs + language switcher), Quiz, Result,
                             Browse (category feed), PostView, StoryView, Privacy, Terms, About,
                             FriendshipSetup, FriendshipPlay, FriendshipResult,
                             CompareInvite, CompareResult, SearchResults, Game,
                             TicTacToeMultiplayer, Feedback, NotFound
  components/               Header, Footer, Logo, QuizCard, FriendshipQuizCard,
                             PostCard (jokes/quotes/etc.), StoryCard, ProgressBar,
                             ShareButtons, ShareSidebar, TileShareButton,
                             ReportButton, AdSlot, GameCard
  games/                    registry.js + one component per game (TicTacToe,
                             RockPaperScissors, MemoryMatch, Game2048, WordGuess,
                             GuessTheNumber, Sudoku, SimonSays, WhackAMole)
  admin/
    adminApi.js              authenticated API calls
    AuthContext.jsx          login/logout/session state
    ProtectedRoute.jsx        role-gated route wrapper
    AdminLayout.jsx           left sidebar nav + logout header for all /admin pages
    components/StatusLabel.jsx  shared "published"/"draft"/"scheduled for..." label
    components/Pager.jsx      shared pagination controls
    utils/datetimeLocal.js    <input type="datetime-local"> ↔ ISO string conversion
    pages/
      Login.jsx, QuizList.jsx, QuizForm.jsx, PostList.jsx, PostForm.jsx,
      StoryList.jsx, StoryForm.jsx,
      FriendshipQuizList.jsx, FriendshipQuizForm.jsx, Analytics.jsx, Activity.jsx,
      FeedbackList.jsx, Admins.jsx
```

## Known decisions worth knowing about

- `react-router-dom` has open security advisories in npm's audit database, but every one is about server-rendering / server-action features this app does not use (we're a plain client-side Vite app — only `BrowserRouter`, `Routes`, `Route`, `Link`, `Navigate`). Worth re-checking `npm audit` occasionally as new versions ship.
- **Quiz card colors must come from `src/gradients.js`, never free text.** Tailwind generates CSS by scanning literal class name strings in the source code at build time — it cannot see class names that only exist as data from the API/database at runtime. This caused a real bug (cards showing no color at all) once quiz data moved from a source file to the live database. The fix: a fixed palette lives in `gradients.js` as literal strings Tailwind can see, and the admin panel's "Card Color" dropdown only ever picks from that list — never accepts typed-in values. When adding new quiz topics needs more colors than are safelisted, add the new gradient string to this file first — see `BACKEND.md`'s bug #3 for the same lesson applied to the `language` field (new fields need backfilling on old data, new gradients need safelisting before use). Post category colors in `postStyles.js` reuse gradients already safelisted here, so no changes were needed there.
- **`ShareButtons` takes an optional `shareText` prop now**, instead of always deriving share copy as `` `${title} — take the quiz!` ``. Needed once the component started getting reused for friendship-quiz links and score reveals, where "take the quiz!" read oddly tacked onto "How well do you know Ashish?" or "I scored 6/8...". Passing `shareText` overrides the default; every other caller (quiz results, posts) is unaffected since the prop is optional.
- **The Clone feature's generated title needs a unique tag, not just a static `"(Copy)"`.** Found while testing Clone on a Hindi-titled quiz: the backend's slug generator only keeps Latin/numeric characters, so a Hindi title contributes nothing to the slug — only the English `"(Copy)"` suffix survived, meaning every clone of every Hindi quiz collided on the same slug (`copy`). Fixed on both sides: the clone title now includes a short random tag (`(Copy a1b2)`), and the backend's slug generator has its own fallback for titles that reduce to nothing at all — see `BACKEND.md`'s bug #5 for the full story.
- **`ShareButtons`'s "Copy link" handler must not let a failed clipboard write silently swallow the share-tracking call.** Found while testing post-share analytics: `navigator.clipboard.writeText()` can throw ("Write permission denied") in contexts without clipboard permission — notably in-app browsers like Instagram/TikTok's, which is exactly where this app's traffic is expected to come from. Since the original code did `await navigator.clipboard.writeText(url)` before firing the `onShare` callback, a denied clipboard permission meant the share was never counted at all, with no visible error. Fixed by wrapping the clipboard call in its own try/catch so `onShare?.()` always fires on click, regardless of whether the copy itself succeeded.
- **`QuizCard`/`FriendshipQuizCard` need `h-full flex flex-col` on the card itself, not just on the grid wrapper.** Found from a real report: card tiles in the same row were visibly different heights. `Home.jsx` wraps each card in a `<div>` that (correctly) stretches to fill its CSS Grid row, but the `<Link>` inside — the actual colored card — had no explicit height, so it only grew to fit its own content, leaving an unstyled gap below shorter cards while the wrapper silently stretched around it. Fixed by making the `Link` itself `h-full flex flex-col` with `mt-auto` on the bottom action row, so every card in a row now fills the same height and the "Take the quiz"/"Fill it in" pill lines up at the same vertical position regardless of how long the title/description is. The inline post cards (jokes/quotes/etc.) in `Home.jsx` didn't have this bug — they're the direct grid item with no extra wrapper, so CSS Grid's default `align-items: stretch` already sized them correctly.
- **Admin list pages must not debounce their very first load.** Found from a real report of the admin panel "flickering" when switching between Quizzes/Posts/etc. `QuizList.jsx` and `PostList.jsx` fire `load()` inside `setTimeout(load, 250)` so typing in the search box doesn't send a request per keystroke — but that same 250ms delay was also applied to the initial mount, meaning every single navigation to either page guaranteed at least 250ms of a bare "Loading..." state before the real content ever appeared, even though the request itself resolves in well under 50ms locally. Fixed with an `isFirstRender` ref: the first run of the effect calls `load()` immediately, and only subsequent filter/search changes get debounced.
- **The hero band's quiz/post counts are fetched, not hardcoded.** They used to be a literal `"24 quizzes"` / `"85+ jokes & quotes"` string in `Home.jsx`, manually edited (and easy to forget) every time new content shipped. Now a one-time `useEffect` fetches `GET /api/quizzes` and `GET /api/posts` with no language filter (both endpoints return every language when the param is omitted) and counts them client-side (non-meme posts only, matching the "jokes & quotes" wording), with the old hardcoded numbers kept only as a same-value fallback shown for the brief moment before the fetch resolves.
- **When rendering content that depends on which tab/filter is active, tag the fetched data with what it was fetched for — don't just clear-and-refetch and trust that timing works out.** `Home.jsx` used to call `setItems(null)` in a `useEffect` after `activeTab` changed, but React still renders once with the *new* `activeTab` and the *old* `items` before that effect runs. Switching from Quizzes to Jokes could momentarily render quiz objects through the post-card code path (which reads `post.category`, undefined on a quiz) — a full crash if a `.gradient` property gets read off that undefined lookup. Fixed by storing fetched results tagged with the tab/language that produced them (`{ tab, language, items }`) and only trusting `items` when that tag matches the current `activeTab`/`language` — otherwise it shows the loading state instead of stale data. Worth reusing this pattern anywhere content is fetched per-tab/per-filter.
- **A `transition-[width]` utility on a `position: sticky` flex item can leave the computed width visibly stuck at the old value after a class change, in this dev environment at least.** Found while building the collapsible admin sidebar: toggling between `lg:w-16`/`lg:w-56` updated the element's `className` correctly (confirmed via DevTools) and worked fine on a fresh page load, but a same-session React state toggle left `getComputedStyle(...).width` reporting the *previous* value indefinitely — not a transition timing issue (waited well past any reasonable duration). A plain test element with the same class, added fresh to the page, picked up the correct width immediately, meaning the utility itself wasn't broken — something about applying it to *this specific* sticky flex item was. Removed the transition entirely rather than chasing the exact cause further, since an instant width snap was an acceptable trade for a working toggle. Worth avoiding `transition-[width]` on sticky elements here until/unless this gets properly root-caused.
- **Bug fixed 2026-07-31, then reworked the same day: clicking a homepage filter visibly yanked the sticky sidebar out of place, not just scrolled the content.** First attempt clamped `Home.jsx`'s scroll-reset to never go past the results grid's top (`Math.min(currentScrollY, contentTop)`, via a `preScrollRef` captured synchronously in every filter's `onClick`, applied through `setTimeout(fn, 0)` rather than `requestAnimationFrame` since this environment's headless preview tab doesn't reliably fire rAF callbacks — confirmed separately via a failed screenshot and a timed-out rAF polling loop). That technically kept the sidebar's on-screen position unchanged for content-rich tabs, but landed at a *different* offset per tab depending on how much content that tab had (a content-sparse tab like Memes never let the sidebar get "stuck" in the first place) — reported back as still not fixed, since the inconsistency across tabs read as more confusing than a single predictable behavior. Reworked to a plain, unconditional `window.scrollTo(0, 0)` on every filter/tab change instead — every click now lands at the exact same place (hero, sidebar, and content all visible together from the top) regardless of which tab or how much content it has, trading "sidebar never visually moves" for "behavior is always identical and predictable," which is what was actually being asked for. The `preScrollRef`/`selectFilter`/`contentRef` machinery from the clamped attempt was removed entirely.
- **Newest is now the default sort, and listed first** (2026-08-01, requested directly) — `sortMode`'s initial state changed from `'trending'` to `'newest'`, and the two toggle buttons swapped order so Newest reads left-to-right first, next to the language switcher.
- **Sidebar spacing tightened** (2026-08-01, requested directly, "so space can be created for Menu items") — the EN/हिंदी and Trending/Newest toggle pills shrank (smaller padding and font size), and the vertical gaps between the language row, sort row, and tabs list in `Home.jsx`'s sidebar were all reduced, leaving more room for the tab list itself without changing anything structurally.
- **Trending/Newest sort toggle generalized to every homepage tab, not just Quizzes.** Previously the sort toggle only rendered (and only worked) on the Quizzes tab. Now every tab — Friendship Quiz, Memes, Games, Stories, Jokes, Quotes, Funny Lines, Motivational — shows the same toggle and sorts real data: quizzes/games sort "Trending" by `totalPlays`, friendship quizzes by `totalAttempts`, and everything else (posts, stories) by a new `totalEngagement` field (views+shares aggregated server-side from the existing `PostEngagement`/`Engagement` models, mirroring the aggregation already used for quiz play counts) as a popularity fallback since those content types have no "completion" concept. "Newest" sorts by `createdAt` for everything with a real creation date; Games (which are code, not database rows, so they have no `createdAt`) approximate it by reversing the static games registry, whose order roughly matches when each game actually shipped.
- **Trending/Newest moved next to the language switcher.** Requested directly: since Trending/Newest is a "global filter" like language (applies the same way regardless of which tab is active), it didn't make sense to bury it at the bottom of the sidebar, below the tab-specific category chips. Moved it to sit immediately after the EN/हिंदी toggle, before the tabs row — the sidebar now reads as "global filters first (language, sort), then content-type-specific filters (tabs, category chips)" instead of mixing the two.
- **Homepage tab order: Funny Lines now immediately follows Jokes**, ahead of Quotes and Motivational Quotes — grouping the two quick one-liner formats together. New order: Quizzes, Friendship Quiz, Memes, Games, Stories, Jokes, Funny Lines, Quotes, Motivational Quotes.

## Small fixes from a UX pass

- **Admin panel header showed plain text "Twegle Admin" instead of the real logo** — inconsistent with the public site's branded header. Fixed by reusing the same `LogoWithWordmark` component (`components/Logo.jsx`) in `AdminLayout.jsx` that the public `Header.jsx` already uses, so the brand mark is identical everywhere.
- **Friendship Quiz had no link anywhere in the footer's "Explore" column.** Since the homepage's tab state (`Home.jsx`) was purely local component state with no URL, there was no link to point at. Added minimal URL support — `Home.jsx` now reads an optional `?tab=` query param once on mount (via `useSearchParams`) to pick the initial tab — and added a "Friendship Quiz" footer link to `/?tab=friendship`.
- **Client-side navigation left the new page scrolled to wherever the old page had been scrolled to.** Found from a real report: clicking "Take the quiz" while scrolled down on the homepage opened the quiz mid-scroll instead of at the first question, and footer Explore/Company links had the same problem. Root cause: React Router's client-side navigation, unlike a normal full page load, never resets scroll position on its own. Fixed with a small `ScrollToTop` component in `App.jsx` (calls `window.scrollTo(0, 0)` on every `pathname`/`search` change) rendered once at the top of the app, so it covers every route change site-wide — including same-path query-only navigations like the homepage's own `?tab=` tab switches, not just full page changes.
- **Contact email was still the personal placeholder address.** `About.jsx`, `Privacy.jsx`, and `Terms.jsx` all hardcoded `tiku.tukur@gmail.com` (both the `mailto:` link and its display text) from before the real business email existed. Updated all three to `twegle.official@gmail.com`.

## PWA support (installable, offline-capable for viewed pages)

Not a native app — a plain web app that can be installed to a phone's home screen and keeps working (for content already viewed) without a connection, via `vite-plugin-pwa` (Workbox under the hood).

- **`vite.config.js`** — the `VitePWA()` plugin config. `registerType: 'autoUpdate'` means a new deployed version takes over silently, no user-facing "update available" prompt (kept simple for now). `devOptions: { enabled: true }` lets the service worker actually run under `npm run dev`, not just a production build — otherwise "does this work" could only ever be answered after a full build+preview cycle.
- **Update detection was silently broken for months (fixed 2026-08-07)**: `registerType: 'autoUpdate'` only controls how the *service worker itself* behaves once a newer one is found (`skipWaiting`/`clientsClaim`, baked in automatically) — it says nothing about how often the *browser* actually checks for a newer one. Browsers only check for service worker updates on a fresh navigation, and an SPA tab that's just left open across a deploy never triggers one. The app was relying entirely on `vite-plugin-pwa`'s auto-injected registration script (the default when `injectRegister` isn't set), which registers the worker exactly once and never checks again — so a visitor who kept a tab open (or even just didn't force-reload) across a new deploy could keep running stale JS indefinitely, with no error and no visible sign anything was wrong. This surfaced repeatedly as real features appearing to "not work" that were actually already fixed and deployed — confirmed multiple times by reproducing the exact same steps in a fresh browser session and having them work correctly, while the reporter's own tab kept showing the old behavior until manually unregistering the service worker in DevTools. Fixed by registering explicitly in `main.jsx` via `virtual:pwa-register` instead (`vite.config.js` sets `injectRegister: false` to avoid double-registering): checks for an update on every tab focus (`visibilitychange`) plus hourly as a baseline, and reloads the page exactly once as soon as a new worker takes control (`controllerchange`, with a guard flag to prevent a reload loop). `workbox.cleanupOutdatedCaches: true` was also added so a previous deploy's precached files get evicted once the new worker activates, instead of accumulating.
- **The manifest** (`name`, `short_name`, `theme_color`, icons, `display: 'standalone'`) is generated by the plugin, not hand-written as a static file — it's injected into `index.html` automatically at build time. `index.html` also manually carries an `apple-touch-icon` link and a `theme-color` meta tag, since iOS Safari's "Add to Home Screen" doesn't pick those up from the web manifest the way Android/Chrome does.
- **Two runtime-caching rules** (`workbox.runtimeCaching` in `vite.config.js`) are what make "offline-capable for viewed pages" literally true, not just an installable icon: (1) public `GET /api/*` requests — `NetworkFirst`, so a visitor online always gets fresh data, but a cached response from a previous visit serves instead of failing once offline. Explicitly excludes `/api/admin/*` and `/api/auth/*` by URL pattern so nothing authenticated ever lands in a cache a service worker could replay later. (2) image requests (`request.destination === 'image'`) — `CacheFirst`, since a meme's image rarely changes once posted. The precache (`globPatterns`) separately covers the built JS/CSS/HTML/icons, so the app shell itself opens offline too — together, a quiz/post/etc. a visitor already opened once will keep rendering with real data the next time they're offline, not just a blank shell.
- **PWA icons are hand-encoded PNGs, not drawn by an image library.** No image-processing tool was available in this dev environment (no `sharp`/`canvas` installed, and this Windows machine's `convert` on `PATH` resolves to the OS's NTFS-conversion utility, not ImageMagick — unsafe to invoke blind). `scripts/generatePwaIcons.mjs` renders the brand mark (gradient + two eyes + a smile, matching `favicon.svg`) pixel-by-pixel into a raw RGBA buffer, then encodes valid PNG files using only Node's built-in `zlib` (for the DEFLATE-compressed pixel data) plus a small hand-rolled CRC32 — no new dependency needed. Produces `icon-192.png`/`icon-512.png` (rounded corners, `purpose: 'any'`), `icon-maskable-512.png` (full-bleed, no transparent corners — required for Android's adaptive-icon masking), and `apple-touch-icon.png` (180x180, also full-bleed since iOS renders transparent corners as solid black). Re-run with `node scripts/generatePwaIcons.mjs` if the icon design ever changes.

## Mobile responsiveness

Checked and fixed at a 375px-wide viewport (both public site and admin panel). Found and fixed three real overflow bugs in the admin panel — all were rows using a plain `flex` container with fixed-width children and no wrapping: the quiz form's "Results" and "Questions/Options" rows, the admin header (logo + nav + user info + logout all fighting for one line), and the "Add a new admin" form. All now use `flex-wrap` with `min-w`/`shrink-0` so they stack instead of overflowing.

**The homepage's own mobile layout did eventually need a fix, once enough tabs existed.** With 8 homepage tabs (up from the original 4-5) plus 6 quiz-category chips, both rows were wrapping across 2-3 lines each below `lg`, pushing the first real quiz/post card down by most of a phone screen before any actual content appeared. Fixed by making both rows single-line horizontal scroll strips on mobile only (`overflow-x-auto` + `shrink-0 whitespace-nowrap` on each pill, `-mx-4 px-4` so the touch-scroll area reaches the true screen edge) — desktop is unaffected, since `lg:flex-col lg:overflow-visible` restores the original vertical sidebar list at that breakpoint. Also shrunk the hero band itself (smaller padding/font sizes, ~136px → ~115px tall) for the same reason — more of the actual content grid visible without scrolling, same information shown.

**2026-08-01: language and Trending/Newest toggles merged onto one row on mobile.** Both used to be their own separate stacked row above the tabs, each one taking the full row height on its own — reasonable on the `lg` sidebar (plenty of vertical room), wasteful on a phone screen where every row of filter chrome pushes real content further down. Wrapped both toggle pills in one shared flex row (`flex items-center justify-center gap-2`) that only applies below `lg` — `lg:flex-col lg:items-stretch` restores the original separate-stacked-rows layout on desktop, where the sidebar's fixed 220px width means side-by-side would look cramped rather than helpful.

## Dark mode

Added 2026-07-31 on request ("fast, visible, broad appeal") — a manual light/dark toggle across the public site (admin panel is out of scope for this pass, since it was requested as a user-facing/GenZ-appeal feature).

- **Tailwind v4 class-based dark mode** — this project has no `tailwind.config.js` (CSS-only config via `@tailwindcss/vite`), so class-based dark mode needs a v4-specific `@custom-variant dark (&:where(.dark, .dark *));` line in `index.css` rather than the classic `darkMode: 'class'` config option. Every `dark:` utility class site-wide reads off whether `<html>` carries a `.dark` class.
- **`ThemeToggle.jsx`** (new) — a small sun/moon icon button, wired into `Header.jsx`. Toggling flips `.dark` on `document.documentElement` and persists the choice to `localStorage['twegle-theme']`.
- **No flash of the wrong theme on load** — a small inline `<script>` in `index.html`'s `<head>`, which runs synchronously before any CSS/JS loads, reads the saved preference (falling back to the OS-level `prefers-color-scheme: dark` media query if nothing's saved yet) and toggles `.dark` on `<html>` immediately — so the page never paints light and then snaps to dark a moment later.
- **Coverage** — every public page and shared component that renders plain gray-scale UI now carries `dark:` variants: `Header`/`Footer`/`ThemeToggle`, `Home`, `Quiz`/`Result`, `PostView`/`StoryView`, `FriendshipSetup`/`FriendshipPlay`/`FriendshipResult`, `CompareInvite`/`CompareResult`, `Game`/`TicTacToeMultiplayer`, `SearchResults`/`Browse`/`NotFound`, `About`/`Feedback`/`Privacy`/`Terms`, and shared components like `ProgressBar`/`AdSlot`/`ShareButtons`/`TileShareButton`/`ReportButton`/`PostCard` (meme branch). The colorful gradient tiles — `QuizCard`/`StoryCard`/`GameCard`/`FriendshipQuizCard`, quiz/story/compare-result gradient panels, and `PostCard`'s non-meme branch — needed no changes at all, since white text on a bright gradient already reads fine in either theme; `ShareSidebar` was already a dark `bg-gray-800` panel by design, likewise unaffected.
- **Admin panel is intentionally untouched** in this pass — it's an internal tool, not part of the "kids/teens/GenZ appeal" surface the toggle was requested for.
- Verified live: toggling actually switches every checked page with no console errors, the choice survives a full reload with no flash, and it persists via `localStorage`.
- **Bug fixed 2026-07-31: adding `ThemeToggle` shifted the search box's position.** `Header.jsx` originally laid out logo/search/(nothing) with a plain `flex justify-between`, where the search form (`flex-1 max-w-xs`) simply took whatever space was left after the logo — adding `ThemeToggle` as a third flex item changed how much space that was, visibly moving the search box. Fixed by switching the header row to a `grid grid-cols-[auto_1fr_auto]` layout (logo / search / toggle) with the search form centered (`flex justify-center`) inside its own middle track — the search box's position now only depends on the logo and toggle's own widths staying fixed, not on whichever elements happen to be neighbors, so it won't shift again if another icon is ever added to either side.
- **Bug fixed 2026-07-31: the initial dark-mode pass missed the 7 game engines entirely, breaking the site on any phone with system dark mode enabled.** Reported as "application not working properly in mobile browser, earlier it was working fine." Root cause: the no-flash script in `index.html` applies `.dark` based on the OS's `prefers-color-scheme` for anyone who's never explicitly toggled the switch — so a visitor whose phone defaults to dark mode (common on both Android and iOS) got the whole site's `.bg-dot-pattern` background switched to near-black (`#0f1115`) automatically, without ever touching the toggle. Every public page and shared component had been given `dark:` variants in the original pass, but the 7 actual game components in `src/games/` (`TicTacToe`, `RockPaperScissors`, `MemoryMatch`, `Game2048`, `WordGuess`, `GuessTheNumber`, `Sudoku`) were overlooked — only their `Game.jsx` wrapper page got dark treatment. These games render plain gray-scale UI (`text-gray-800` status lines, `bg-white` boards/tiles/buttons) with no `dark:` fallback, so on a dark-mode phone the status text became near-invisible (dark gray on near-black) and game boards looked visually broken against the dark page background — while the rest of the site (which does have `dark:` coverage) looked fine, making the games specifically read as "not working." Fixed by adding `dark:` variants to all 7 game files, matching the same treatment already given to `TicTacToeMultiplayer.jsx`. Verified via an automated contrast scan (comparing each leaf text node's color against its resolved background luminance) on Tic-Tac-Toe and Sudoku with `.dark` forced on — zero low-contrast text found afterward (previously flagged on every status line). This is also a good reminder that any future public-facing component needs the same `dark:` pass, not just top-level pages — a plain component with zero `dark:` classes doesn't error, it just silently renders in light-mode colors regardless of the page's theme, which reads as "half-fixed" rather than an obvious bug.

## What's next

Every item from the original "strengthen before launch" list (`DEVELOPMENT_PLAN.md` section 12) is now done. Launch is still intentionally on hold (owner's call) until there's an appetite to go live. Ongoing, not "done" in the same sense as the engineering items:
- More quiz/post/friendship-quiz content variety — always a valid next step, never really finished.

Once you decide to launch:
- Real ad code (Google AdSense) replaces the placeholder box in `AdSlot.jsx`.
- Real affiliate links get added per result once you've signed up for relevant affiliate programs.
- Deploying both frontend and backend somewhere public (Vercel + Render/Railway + MongoDB Atlas, per the plan).
- A third language, whenever that's requested — the `language` field pattern already supports it, it's a content task, not an engineering one.
