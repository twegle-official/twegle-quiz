# Pending Tasks

**This is a living checklist** — both your business/account tasks and the development backlog, in one place. Updated every time something completes, and every time something new comes up in conversation. Check here first for "what's left."

For deeper detail on any development item, see `DEVELOPMENT_PLAN.md` / `APPLICATION_FLOW.md` — `APPLICATION_FLOW.md`'s change log has the full narrative for each entry below (what was tried, what broke, how it was verified); this file keeps the condensed checklist version.

---

## Your Tasks (business/account setup — only you can do these)

*Pending first, completed below.*

- [ ] **LinkedIn Company Page** — retried `linkedin.com/company/setup/new`, now blocked with "You don't have enough connections to create a Page" (a different requirement than the earlier 24h-account-age block, which has since cleared). Need to add a handful of LinkedIn connections first, then retry.
- [ ] **Folder reorganization** (`Twegle-Quiz` → nested under a `Twegle` company folder) — recommended to hold off until there's an actual second project to organize alongside it.
- [ ] **Decide final Privacy Policy / Terms wording** — real content already written and live on the site; just needs your sign-off (or a lawyer's, once real money is involved) before it's "final."
- [ ] **Apply to affiliate programs** (e.g. Amazon Associates) — can start anytime, some approve faster with real traffic.
- [ ] **Company/trademark registration** — low priority, revisit once there's real sustained revenue. (A quick trademark search already came back clean for "Twegle.")

- [x] **Dedicated business email** — `twegle.official@gmail.com` ✅ done
- [x] **Domain name** — purchased `twegle.in` via Namecheap (~$9.98/yr, auto-renew off — remember to manually renew before it expires). WHOIS email verification completed same day. DNS connected to Vercel (A record + CNAME) — `https://www.twegle.in` is the live site, `twegle.in` redirects to it. ✅ done.
- [x] **Social media handles** — Instagram (`@twegle.official`), Facebook (Page created, logo + cover uploaded, launch post published), YouTube (`@twegle`, channel art uploaded) ✅ done. Launch-day posts published on Instagram, Facebook, and LinkedIn (personal profile, since the Company Page isn't set up yet). YouTube Community post skipped for now — that feature isn't unlocked on a brand-new channel (only "Upload video"/"Go live" show up), revisit after uploading a first video or gaining subscribers. TikTok skipped (not available in India).
- [x] **GitHub account + organization** — personal account created (username `twegle`), organization `twegle-official` created, repo `twegle-quiz` created (private) and this project's code pushed to `main`. ✅ done. `credentials.txt`, `.env` files, and local MongoDB dev-data were kept out of the repo via `.gitignore`. Authenticated via GitHub CLI (`gh auth login`) for future pushes.
- [x] **Hosting & deployment — live** — MongoDB Atlas (free M0 cluster) for the database, Render (free tier, backend) for the API, Vercel (free/Hobby, frontend) for the site, connected to `twegle.in`. Production database seeded with all existing content (quizzes/posts/stories/friendship quizzes) and 3 admin accounts (superadmin/editor/analyst) created — see `credentials.txt` (local-only, not in Git) for all logins/connection strings. Render's free tier sleeps after 15 min of inactivity (~30-50s cold-start on the next request) — upgrade to the $7/month Starter plan anytime from the Render dashboard for always-on, no code change needed. ✅ done.
- [x] **Apply for Google AdSense** — application submitted 2026-08-05 (account created, payments profile filled in with Ritika Saxena as payee, site connected via the verification script, `ads.txt` added, site requested for review). Now waiting on Google's approval decision (days to a few weeks) — nothing further to do until then; check `adsense.google.com` → Sites for status, or the email on `twegle.official@gmail.com`.

---

## Development Tasks

### Suggested — Site Polish & Growth, not started
*Added 2026-08-17 at the owner's direct request: "how to improve my site so it looks more attractive, what small features attract more people, what makes people spend more time." A new bucket, separate from Phase 2 (which is specifically about what end-user accounts unlock) — this one is about visual polish, virality, and time-on-site, and doesn't require accounts to start most of it. Ordered highest priority first — cheapest-to-build, highest-leverage ideas on top. Each item is tagged with its primary goal: **[Look]** = makes the site feel more polished/alive, **[Attract]** = brings new people in, **[Retain]** = keeps existing visitors around longer.*

- [ ] **[Attract] Referral rewards** — "Invite a friend, you both get a bonus" via a referral code/link on signup. Turns the existing account + points system into an actual growth lever instead of a closed loop; straightforward to bolt onto the existing signup flow with a `?ref=` param.
- [ ] **[Look] Festive/seasonal homepage banner** (Diwali, Holi, New Year, Republic Day, etc.) — a lightweight, date-triggered banner (+ a matching seeded quiz/post around the festival), reusing the exact "deterministic by date, no cron job" pattern Horoscope and Daily Quiz already use. Gives the site a "someone's actively running this" feel around real Indian calendar moments, at near-zero engineering cost.
- [ ] **[Retain] "Random content" button** in the header/footer — one click to a random quiz, post, game, story, or puzzle, for pure serendipitous browsing. Cheap (client-side pick from an already-fetched or one-shot-fetched list) and a direct pages-per-session lever.
- [ ] **[Attract] Embeddable mini-quiz widget** — a stripped-down, chromeless `<iframe>`-embeddable version of a quiz that other blogs/sites can drop in, crediting/linking back to Twegle. Reuses the existing public quiz-play flow; the actual new work is just a minimal layout variant plus embed-friendly headers.
- [ ] **[Retain] First-visit onboarding tour** — a brief 3-4 step tooltip walkthrough pointing a brand-new visitor at Quizzes/Games/Puzzles/Streaks, shown once (localStorage-gated, same pattern as other one-time UI). Today a first-time visitor who takes one quiz has no signal that Games/Puzzles/Stories/Horoscope even exist unless they notice the tabs themselves.

### Suggested (Phase 2), not started
*Phase 1 (MVP + all its polish/growth work) is complete — the owner confirmed 2026-08-17 there's nothing left to pull forward from it, so the old "Suggested (Phase 1)" section and its now-moot framing are gone; everything left in the backlog lives here. Phase 2 in `DEVELOPMENT_PLAN.md` §4 is end-user accounts plus what accounts unlock — end-user accounts are already live (2026-08-05), so all of this is genuinely buildable, not blocked on a prerequisite that doesn't exist yet. Ordered highest priority first within each column.*

**User-side**
- [ ] **Bookmarks / Favorites** — save a quiz, post, or story to revisit later, tied to the account (guests keep working exactly as they do today; this is additive, same as everything else account-related). Highest priority here: small, self-contained, no external dependency, and a clear return-visit hook now that accounts exist.
- [ ] Push notifications (if PWA installed) — the account/subscription piece now exists to make this worth more than a generic broadcast; still needs real push infrastructure (web push + a service worker handler) to be built.
- [ ] **"Find the Difference" as a second puzzle type** — requested 2026-08-05, for later (not part of the first Puzzles build). Two near-identical images side by side, 5-10 deliberate differences to spot. Fits the same admin-authored-content model as the riddle puzzles (each puzzle = 2 image URLs + a list of difference coordinates + difficulty, editable via the admin panel, unlimited going forward once built) — but needs one upfront engineering build for the interactive click-to-find/highlight mechanic (comparable in scope to how Sudoku needed its own one-time build among the Games). The real bottleneck is **content sourcing, not code**: unlike a riddle (write it in 30 seconds), a spot-the-difference pair needs an actual edited image pair — licensed image sets or manual editing — which is slower/costlier to produce than text puzzles. Do this after a plan exists for where the image pairs come from; also benefits from real image uploads (below) rather than paste-a-URL.
- [ ] **Friends-only leaderboards** — game leaderboards are currently global by nickname; a lightweight "share your profile link, see just each other's scores" friend view would need some kind of friend-linking, but no follow-graph infrastructure beyond that. Lowest priority here: narrowest audience of the four.

**Admin-side**
- [ ] Basic automated test suite (login, quiz-taking flow, friendship-quiz flow) — currently everything is verified by hand each session. Highest priority here: an engineering safety net that protects every other feature above, and the risk of a manual-only test process grows with the codebase, not shrinks.
- [ ] **Real image uploads via cloud storage** (Cloudinary recommended — 25GB free tier) — picture Puzzles currently use paste-a-URL since no persistent storage exists yet (Render's server disk is ephemeral, wiped on every redeploy, so a naive server-disk upload would silently break). Explicitly deferred by the owner (2026-08-05) until after Puzzles shipped with paste-a-URL (it has) — **requires the owner to create the Cloudinary account themselves first** (Claude can't sign up for third-party services on their behalf) and share the API credentials (Cloud Name/API Key/API Secret, ideally as Render env vars, not pasted in chat) before this can be built. Ranked below the test suite/comments since it's blocked on an owner action, not something that can be picked up and finished end-to-end right now.
- [ ] **Content approval workflow** — Editor submits, Superadmin approves before it goes live. Meaningful once there are more than a couple of active admins; today Editor can publish directly, which is fine at the current team size.
- [ ] **Community-submitted content with a moderation queue** — e.g. visitor-submitted jokes/quotes, reviewed before publishing. Genuinely needs accounts (to attribute submissions and rate-limit spam) plus a real moderation UI, not a quick add-on.
- [ ] **Cohort/retention analytics** — weekly active users, day-7 retention, etc. — meaningfully needs real accounts to track a "user" across sessions; the current anonymous-ID analytics can count plays/views but can't answer "did the same person come back." Lowest priority here: needs real usage growth to be meaningful, and nothing today is blocked on not having it.

### Deferred on purpose (not needed now)
- [ ] Native mobile app — a real App Store/Play Store app, not the current web app. Only worth considering later, if the web version proves traction first (see `DEVELOPMENT_PLAN.md`'s Phase 3).
- [ ] Third language — no rebuild needed when requested, purely a content task
- [ ] Real ad code / real affiliate links — blocked on the AdSense/affiliate account tasks above, not code work

---

### Completed till date

*Chronological, oldest first — everything shipped since the very first prototype.*

- [x] **Frontend prototype** — homepage, quiz-taking flow, result page, and privacy/terms placeholder pages, built against mock data before any backend existed. Done 2026-07-26.
- [x] **Backend + admin panel** — Node/Express/MongoDB backend, JWT-based admin auth with 3 roles (superadmin/editor/analyst), quiz CRUD, and the frontend wired to the real API instead of mock data. Quiz content rewritten with a comedic tone. Fixed two real bugs: editing a quiz used to change its shareable slug, and "Published" quizzes saved as "Draft." Done 2026-07-26.
- [x] **Security hardening + visual polish (v1)** — helmet, rate limiting (login/plays), NoSQL-injection protection, input validation, payload size limits; custom favicon, Baloo 2/Nunito fonts, question-transition and result-reveal animations, skeleton loading states; 2 more quizzes for variety (5 total); fixed mobile-layout overflow bugs in the admin panel. Done 2026-07-26.
- [x] **Twegle rebrand** — renamed from "Twegle Quiz" to Twegle, new mascot-style logo + favicon. Done 2026-07-27.
- [x] **Post content type** — Jokes, Funny Lines, Quotes, Motivational Quotes as a new shareable-card content type, separate from quizzes, with its own admin CRUD and browse/share pages. Done 2026-07-27.
- [x] **Hindi language support (initial)** — `language` field added to Quiz/Post with a homepage language switcher; found and fixed a bug where the 5 original quizzes briefly disappeared (needed a one-off backfill since a schema default doesn't retroactively apply to existing documents). Done 2026-07-27.
- [x] **5 new quiz topics** — Korean Beauty Standard, Squishy, Blind Bag, K-pop Idol Position, K-pop Comeback Era — plus 36 English + 29 Hindi posts written natively (not translated). Done 2026-07-27.
- [x] **Hindi versions of all 10 quizzes** — 20 quizzes total; colorful gradient treatment for post cards matching quiz cards; fixed a real crash where quickly switching homepage tabs could briefly render one content type's data through another's rendering logic. Done 2026-07-27.
- [x] **"📸 Share as Image"** — generates a branded, shareable image of a quiz result or post (native share sheet on mobile, download elsewhere). First attempt used `html-to-image` and hung indefinitely — replaced with direct Canvas 2D drawing, which is standard and reliable everywhere. Done 2026-07-27.
- [x] **Quiz categories + play-count social proof** — Beauty & Skincare / Bollywood & Pop Culture / K-pop / Lifestyle & Personality / Fun & Random, with homepage filter chips; real "N took this" counts on quiz cards; homepage credibility pass (colorful hero, real footer, background texture); fixed a gap where the admin quiz form had no Language dropdown at all. Done 2026-07-27.
- [x] **Homepage layout widened** — Header/Footer/Home grew from `max-w-3xl` to `max-w-6xl` (was leaving ~23% empty margin per side on a normal desktop screen); hero band rebuilt as a compact single row. Done 2026-07-27.
- [x] **Link previews + Trending/Newest sort** — `/api/share/*` routes so WhatsApp/Instagram/Facebook show a real title/description card instead of nothing (those crawlers don't run JavaScript); a Trending/Newest sort toggle on the homepage quiz grid. Done 2026-07-27.
- [x] **Homepage filters moved to a sidebar (desktop)** — language/tabs/category chips/sort toggle moved from four stacked rows above the grid into a sticky left sidebar, so real content appears right away instead of below ~250-300px of filter UI. Done 2026-07-27.
- [x] **Real legal pages + About/Contact** — replaced Privacy Policy/Terms of Service placeholders with real content; new About/Contact page; admin search + category/language/status filters added to the Quizzes and Posts lists. Done 2026-07-27.
- [x] **Post view/share analytics** — anonymous view/share tracking for posts, mirroring quiz-play tracking; found and fixed a real bug where a denied clipboard permission (common in Instagram/TikTok's in-app browser) silently dropped a share from the count entirely. Done 2026-07-27.
- [x] **Friendship Quizzes ("How well do you know me?")** — the first genuinely two-person format on the site: one person answers about themselves, any number of friends guess and get scored. Full public flow + admin CRUD; `ShareButtons` gained an optional custom share-text override to support it. Done 2026-07-27.
- [x] **SEO groundwork** — per-page titles/meta descriptions, a live `sitemap.xml` generated from the database, `robots.txt`, minimal homepage structured data. Done 2026-07-27.
- [x] **Duplicate/Clone, scheduled publishing, activity log** — one-click copy of any quiz/post/friendship-quiz as a new draft; an optional future "Publish at" date/time enforced at query time (no cron job needed); an admin audit trail of who created/edited/deleted what, and when. Plus a content batch: 2 new quiz topics, 20 more posts, 1 more friendship-quiz template (24 quizzes / 85 posts / 4 friendship templates total). Found and fixed a real bug cloning a Hindi-titled quiz (the slug generator only kept Latin/numeric characters, so repeated Hindi clones collided on the same slug). Done 2026-07-28.
- [x] **UX fixes pass** — inconsistent card-tile heights fixed (`h-full flex flex-col`); admin panel header now shows the real logo instead of plain text; fixed a real "flicker" where admin lists debounced even their very first load; added a Friendship Quiz footer link (which needed minimal `?tab=` URL support on the homepage first). Done 2026-07-28.
- [x] **Memes** — a new shareable content type (`category: 'meme'` on the existing Post model, not a new model), image + optional caption, paste-a-URL for now (no upload pipeline yet); 6 starter memes seeded. Done 2026-07-28. **Removed 2026-08-06** at the owner's request ("not looking useful") — full removal (code + the 6 seeded posts, both local and production), not just hidden. See `APPLICATION_FLOW.md`.
- [x] **Quiz "compare with a friend" mode** — take a regular quiz, share your result, a friend takes the same quiz blind, both results reveal side by side with a match/no-match banner. Done 2026-07-29.
- [x] **"You might also like" suggestions** — 3 related quizzes on Result pages, 3 related posts on Post pages. Done 2026-07-29.
- [x] **Public search bar** — search box in the header, `/search?q=` results page across quizzes/posts/friendship quizzes. Done 2026-07-29.
- [x] **Live homepage hero stats** — real counts from the API instead of a hardcoded string. Done 2026-07-29.
- [x] **404 page** — proper "page not found" screen for unmatched URLs. Done 2026-07-29.
- [x] **Bookmarkable homepage tabs** — active tab lives in the URL, back/forward buttons work between tabs. Done 2026-07-29.
- [x] **Scroll-to-top on navigation bug** — client-side page changes (taking a quiz, footer Explore/Company links) used to leave the new page scrolled to wherever the old page was scrolled to. Fixed with a site-wide scroll-reset on every route change. Done 2026-07-29.
- [x] **Contact email updated everywhere** — About & Contact, Privacy Policy, and Terms of Service now all point at `twegle.official@gmail.com` instead of the placeholder personal email. Done 2026-07-29.
- [x] **Games — Tic-Tac-Toe & Rock Paper Scissors** — new "🎮 Games" homepage tab and `/games/:slug` pages. Both single-player against the house (no accounts, no new backend infra beyond an anonymous play-count ping): Tic-Tac-Toe vs. an unbeatable minimax AI, Rock Paper Scissors vs. a random AI. Done 2026-07-29.
- [x] **"Share Twegle" button** — sharing the site itself rather than one specific quiz/post. A persistent floating rail fixed to the left edge of the viewport, shown on every page including the homepage (at the `xl` breakpoint, to clear a real collision with the homepage's own left filter column below that width) — the footer version covers every narrower width. Done 2026-07-29, placement corrected 2026-07-30.
- [x] **Tagline — "Where Fun Goes Viral"** — added next to the logo in the public header and in the homepage hero band. Deliberately not added to the admin panel's header, which is already tight on horizontal space. Done 2026-07-29.
- [x] **4 more games — Memory Match, 2048, Word Guess, Guess the Number** — Games tab grew from 2 to 6 titles, all single-player vs. "the house," all reusing the existing `GameSession` backend with no changes beyond adding their slugs to the allowlist. Word Guess is deliberately not called/drawn as "Hangman" (life counter instead of gallows imagery, to match the site's family-friendly tone). Done 2026-07-29.
- [x] **Admin panel left-sidebar nav** — `AdminLayout.jsx` moved from a single top nav bar to a left sidebar (sticky, full-height on desktop; stacks as a simple top bar on mobile). Done 2026-07-29.
- [x] **Admin list pagination** — quizzes, posts, and friendship quizzes all paginate server-side now (20/page), via a shared backend pagination helper and a shared `Pager.jsx` component; changing a filter resets to page 1 automatically. Done 2026-07-29.
- [x] **Collapsible admin sidebar** — a toggle button collapses the sidebar to a 64px icon-only rail (desktop only, persisted via `localStorage`) to reclaim width. Done 2026-07-29.
- [x] **Admins page layout fix** — was capped at `max-w-2xl`, leaving roughly half of any normal screen unused; now a two-column form+list layout using the full content width like every other admin page. Done 2026-07-29.
- [x] **Feedback section** — a public `/feedback` page (linked from the footer) where any visitor can send a message plus an optional email, no account needed. Backend is a simple rate-limited `Feedback` model; an admin `/admin/feedback` page lists entries paginated, newest first, with unread highlighting and mark-read/delete actions for superadmin/editor (analyst is read-only, matching every other admin list). Done 2026-07-30.
- [x] **Share before attempting** — a 🔗 share icon on every content tile (quizzes, games, jokes/funny lines/quotes/motivational quotes/memes, friendship quizzes), each opening a floating popover (Share/WhatsApp/Copy link) that never affects the tile's height, via a shared `TileShareButton` component. Every tile shares a preview specific to that item — quizzes and friendship-quiz templates via crawler-friendly `/api/share/quiz/:slug` and `/api/share/friendship-quiz/:slug` routes, games via `/api/share/game/:slug` (backed by a small hardcoded title/description map since games have no database row), and jokes/quotes/memes via the pre-existing `/api/share/post/:id` route. Done 2026-07-30, initially quizzes-only (moved from the quiz-taking screen to the tile), generalized to every content type the same day per feedback.
- [x] **Sudoku** — a 7th game. A 9x9 grid generated from one closed-form solved pattern, scrambled via standard Sudoku symmetry operations (digit relabeling, row/column/band/stack shuffling, optional transpose) rather than a full backtracking generator — always valid, no puzzle database needed. Live conflict highlighting, no lose condition (matches Memory Match's win-only pattern). Done 2026-07-30.
- [x] **Smaller homepage hero** — shrunk padding/font sizes on the hero band (~136px → ~115px tall) so more real content is visible without scrolling, keeping the same tagline/heading/subtext/stats. Done 2026-07-30.
- [x] **Homepage tab reorder** — reordered by expected usage: Quizzes → Friendship Quiz → Memes → Games → Jokes → Quotes → Funny Lines → Motivational Quotes (Memes moved up since images reshare better on WhatsApp/Instagram than plain text; the four post categories ordered broadest-to-narrowest appeal). Done 2026-07-30.
- [x] **Mobile filter overflow fix** — the homepage's tab row (8 tabs) and quiz-category chip row used to wrap across 2-3 lines each on mobile, pushing the first real content card down by most of a phone screen. Both are now single-line horizontally-scrollable strips below `lg` (with the scrollbar itself hidden via CSS so it reads as a plain swipeable carousel), unchanged as a vertical sidebar list on desktop. Done 2026-07-30.
- [x] **PWA support** — installable (manifest + service worker via `vite-plugin-pwa`), with the app shell precached and viewed pages' API responses runtime-cached (`NetworkFirst`, so it still works offline after being viewed once) — admin/auth endpoints explicitly excluded from caching. Icons generated as real PNGs via a small hand-rolled encoder (no image libraries were available in this environment). Done 2026-07-30.
- [x] **"Trending on Social Media" quiz category** — a new category (🔥, alongside Beauty/Bollywood/K-pop/Lifestyle/Fun & Random), added to the backend enum, the homepage filter chips, and both admin category dropdowns. Seeded with its first 2 topics — "Which Viral Reel Trend Are You?" and "Which Meme Format Are You?" — in English and Hindi (24 → 28 quizzes total). Done 2026-07-30. **Removed 2026-07-31** at the owner's request — see below.
- [x] **Report a quiz/post** — a 🚩 "Report this" link on Result/Post pages (radio-button reason: offensive/incorrect/broken/other, plus optional details), reusing the `Feedback` model/admin-list rather than a separate system — exactly as originally suggested. Admin Feedback list shows a red "Report — {reason}" badge on flagged entries. Done 2026-07-30.
- [x] **Non-personality (trivia) quizzes** — `Quiz.type` (`personality`/`trivia`) added without a parallel model or quiz-taking flow: a trivia option's existing `result` field just holds `'correct'`/`'incorrect'` instead of a personality key (reusing the exact same per-option score tally with zero new code), and results gained `minScore`/`maxScore` so the right tier is picked by score range instead of by vote count. Admin `QuizForm.jsx` branches on the selected Quiz Type. One example seeded: "How Well Do You Know Bollywood?" (5 questions, 3 score tiers). Done 2026-07-30.
- [x] **True two-player async Tic-Tac-Toe** — a real turn-based match against a friend via a shareable link (new `TicTacToeGame` model + `/api/tictactoe/*` routes), separate from the existing single-player-vs-AI game. Creator is always X and goes first; a friend joining the link becomes O. No accounts — each browser remembers its own role in `localStorage`; the backend is the real authority on whose turn it is regardless of what the client claims. Frontend polls every 4s while waiting on the opponent. Done 2026-07-30.
- [x] **Removed "Trending on Social Media" quiz category** — taken back out at the owner's request, one day after it was added. Reverted the `trending` category value from the enum in `Quiz.js`/`quizController.js`, removed the 🔥 filter chip from `Home.jsx` and the dropdown entry from both admin forms, and deleted its 2 seeded topics (4 quiz documents, EN+HI) from both `seedQuizzes.js` and the live database. 29 → 25 quizzes live (back to the pre-Trending count). Done 2026-07-31.
- [x] **Stories** — a new content type: original short stories across 6 categories (Horror, Comedy, Romance, Mystery, Moral Tales, Motivational), readable on-page or read *aloud* via a "🔊 Listen to this story" button using the browser's built-in Web Speech API — no recorded voice, no paid TTS service, nothing server-side, so no new cost or copyright exposure from the narration itself. New `Story` model (own immutable slug, like `Quiz`), full admin CRUD, a "📖 Stories" homepage tab with its own category chips, search/sitemap/report/share-preview integration. 8 original stories seeded (6 English across all categories, 2 Hindi). Done 2026-07-31.
- [x] **Quiz/Friendship Quiz/Game/Story view & share engagement tracking** — asked whether Stories' missing view/share analytics gap also applied elsewhere; it did (Quiz/Friendship Quiz/Games each only tracked completions, not views/shares), so all four got the same tracking Post already had (`PostEngagement`) in one pass via a shared `Engagement` model. 4 new tables in admin Analytics. Done 2026-07-31.
- [x] **Sticky filter sidebar scroll-jump fix, reworked to a simple full reset** — reported that a first fix (clamping scroll so the sidebar's position never visibly moved) still wasn't right, since it landed at an inconsistent offset per tab depending on content length. Replaced with a plain `window.scrollTo(0, 0)` on every filter/tab click — every click now lands at the same place every time, prioritizing predictability over the sidebar literally never moving. Done 2026-07-31.
- [x] **Trending/Newest sort generalized to every homepage tab** — previously Quizzes-only; now Friendship Quiz/Memes/Games/Stories/Jokes/Quotes/Funny Lines/Motivational all sort too, via play/attempt counts where they exist and a new aggregated `totalEngagement` (views+shares) field for posts/stories. Done 2026-07-31.
- [x] **Funny Lines tab reorder** — now immediately follows Jokes, ahead of Quotes/Motivational, grouping the two one-liner formats together. Done 2026-07-31.
- [x] **Dark mode toggle** — manual light/dark toggle (`ThemeToggle.jsx`) across the public site, persisted via `localStorage` with a no-flash inline script so the right theme paints on first load; covers every public page and shared gray-scale component (admin panel intentionally out of scope). Done 2026-07-31.
- [x] **Story read-aloud not stopping on story switch** — reported: opening a different story from "You might also like" while one was narrating left the old story's voice still playing. Root cause: React Router reuses the same `StoryView` component across `/story/:slug` navigations rather than remounting, so the speech-cancel cleanup (keyed only on mount/unmount) never fired on a story switch. Fixed by keying it on the story text too. Done 2026-07-31.
- [x] **Games broken in dark mode on mobile** — reported as "application not working properly in mobile browser, earlier it was working fine." Root cause: the dark-mode pass covered every page but missed the 7 actual game engine components (`src/games/*.jsx`), so a visitor whose phone defaults to system dark mode (switches automatically, no toggle needed) saw those games' text/boards rendered with no dark fallback — barely readable against the now-dark page background. Fixed by adding `dark:` variants to all 7 games. Done 2026-07-31.
- [x] **Header search box position fix** — reported: adding the dark/light toggle shifted the search box's placement. `Header.jsx` switched from a plain 2-item flex row to a `grid grid-cols-[auto_1fr_auto]` (logo/search/toggle), with the search form centered in its own column — position is now stable regardless of what's on either side. Done 2026-07-31.
- [x] **Trending/Newest moved next to language switcher** — requested since it's a "global filter" like language, not tied to one tab. Moved from the bottom of the homepage sidebar (below category chips) to right after the EN/हिंदी toggle. Done 2026-07-31.
- [x] **Light/funny astrology (Horoscope) feature** — new "🔮 Horoscope" homepage tab, placed after Stories, with a picker grid of all 12 zodiac signs and a `/horoscope/:sign` detail page (Day/Week/Month/Year period switcher, EN/Hindi toggle, share buttons, disclaimer). Entirely computed, no database or cron job: each horoscope is built from a per-sign "trait" one-liner plus a per-period "action" one-liner, picked deterministically from a date-derived index (`utils/horoscope.js`) — 8 traits × 10 actions gives 80 distinct combinations per sign per period (in the ~60-90 range originally suggested) while only needing 8+10 lines written per language. Wired into the existing view/share `Engagement` model (`contentType: 'horoscope'`, keyed by zodiac sign) and admin Analytics gained a 5th table. Not wired into search or the sitemap for this first pass (nothing to look up — see `FRONTEND.md`). Content rewritten the same day after feedback that it read too generic — replaced with India-specific humor in both languages (family WhatsApp groups, "shaadi kab karoge," chai stalls, autorickshaws, EMI reminders, wedding season, etc.), same 8×10 engine, only `horoscopeContent.js` changed. Done 2026-07-31.
- [x] **Consistent "← Back" button on every content page** — new `BackButton.jsx` (uses browser history via `navigate(-1)`, falling back to a plain link home if there's no real history) added to all 12 content detail pages. Directly fixes the reported bug where clicking Back from a story always reset the homepage to the Quizzes tab — since the active tab lives in the URL, going back via real history now restores whichever tab/filter was active. Done 2026-08-01.
- [x] **Sidebar polish** — EN/हिंदी and Trending/Newest toggles shrunk and share one row on mobile now (previously two separate stacked rows), freeing a full row of vertical space for content; Newest is now the default sort and listed first (was Trending). Done 2026-08-01.
- [x] **Social links in footer, fixed up** — Instagram/Facebook/YouTube/LinkedIn icons added, then two follow-up fixes: swapped emoji placeholders for real inline SVG icons (emoji didn't clearly read as platform links), and corrected the Facebook URL to the numeric `profile.php?id=...` link since the `facebook.com/twegle` vanity URL isn't claimable yet (needs ~25 followers first). Done 2026-08-01.
- [x] **Site tagline/description updated to mention Games/Stories/Horoscope** — homepage hero heading, footer blurb, `index.html` meta description/`og:description`, and the default SEO description all changed from "quizzes, jokes, quotes, and more" to explicitly list Games/Stories/Horoscope too, matching the wording already used in the site's social media bios. Done 2026-08-01.
- [x] **Site deployed live at twegle.in** — MongoDB Atlas (free M0), Render (backend), Vercel (frontend), domain connected via A/CNAME records at Namecheap. Production database seeded with all existing content and 3 admin roles created. Along the way: fixed a MongoDB Atlas database-user typo (`tweglofficial_db_user`, not `twegleofficial_db_user`) that caused the first Render deploy to fail with a bad-auth error; discovered this local machine's network can't complete a direct MongoDB TLS handshake to Atlas (works fine over plain HTTPS, so likely a TLS-inspecting network layer) — worked around it by adding a temporary superadmin-only HTTP route to trigger the existing seed scripts over the live API, then removed that route once seeding was done. Done 2026-08-02.
- [x] **Post-launch bug fixes** — fixed a real bug reported after a friend opened a shared friendship-quiz link cold (never having visited the site before): Vercel returned a raw 404 instead of the app, since a static host has no file at a client-side-routed path on first load; added `frontend/vercel.json` with a catch-all rewrite to `index.html` so React Router can take over. Also fixed the score card/share buttons visually overlapping on the Friendship Quiz result page (missing top margin). Removed a redundant "Back to Twegle" button from Games, Horoscope, and two-player Tic-Tac-Toe pages (the `← Back` button at the top already covers it) and added the missing `← Back` button to About, Feedback, Privacy Policy, and Terms of Service (they'd been missed in the original Back-button rollout). Fixed the footer's LinkedIn icon, which pointed at a Company Page that doesn't exist yet — now points to the founder's personal profile until the Company Page is set up (blocked on LinkedIn requiring a few connections first). Done 2026-08-03.
- [x] **Reaction emojis on posts + Game leaderboards** — picked from "Suggested, not started" as the two features reusing the most existing backend patterns. Reactions: 😂🔥😭👍 buttons on the Post detail page, one pick per anonymous visitor per post (re-tapping a different emoji replaces the old pick rather than stacking), new `Reaction` model + `/api/posts/:id/reactions` routes. Leaderboards: anonymous nickname + score, scoped to the 4 games with a natural numeric result — 2048 (score), Memory Match (moves, fewer is better), Word Guess (lives left), Guess the Number (tries, fewer is better) — new `GameScore` model + `/api/games/:slug/leaderboard` routes. Tic-Tac-Toe (unbeatable minimax AI) and Rock Paper Scissors (pure luck) deliberately excluded — neither produces a meaningful score to compete on. Both features are anonymous/session-based by design, with an eye toward attaching to real accounts later without a rebuild, once end-user accounts (Phase 2) exist. Done 2026-08-04.
- [x] **Daily streak / "Quiz of the Day"** — a Wordle-style habit hook, entirely client-side (no backend changes). A new `dailyQuiz.js` util picks one quiz per calendar day via the same date-derived-index pattern Horoscope already uses (sorted by slug, `dayOfYear % length` — no cron job, no server round trip), and tracks a streak in `localStorage`, incrementing only when the visitor actually completes that exact quiz (checked in `Result.jsx` against the same day's pick, keyed off `quiz.language` so the pick is consistent between the homepage banner and the result page). New `DailyQuizBanner.jsx` shows the pick + current streak at the top of the homepage; `Result.jsx` shows a "🔥 N-day streak" line when the just-finished quiz was the actual daily pick. Verified end-to-end in browser: banner picked "Which Monsoon Mood Are You?", completing it showed the streak line, and the homepage reflected the updated streak afterward. Done 2026-08-04.
- [x] **Content freshness flags + Mobile-friendly Quick Add (admin)** — picked as the next two, both admin-side. Freshness flags: a "⚠️ Needs a refresh" badge on Quizzes/Posts admin list rows, computed entirely client-side from data the existing Analytics endpoints already return (`fetchAnalytics`/`fetchPostAnalytics`) — no backend changes. A published item is flagged once it's been live over 14 days with fewer than 3 plays/views+shares (loose thresholds by design — a nudge to glance at, not a hard rule); play/view counts are now also shown on every row, not just flagged ones. Quick Add: a new `/admin/quick-add` page (superadmin/editor only, matching Posts' own write permissions) — category buttons (Joke/Funny Line/Quote/Motivational; Meme excluded, it needs an image not text), EN/HI toggle, one big textarea, optional author field for quotes, publishes straight to 'published' via the same `createPost` endpoint the full Posts form already uses (no new backend route). Shows a running "Just added" list so rapid-fire adding has visible confirmation without leaving the page. Both verified end-to-end in browser (freshness logic unit-verified since no local content is old enough yet to trigger it live; Quick Add tested by actually publishing a post and confirming it in the database). Done 2026-08-04.
- [x] **Reactions inline on homepage post tiles** — pulled forward from "Suggested, not started" per direct feedback that clicking into a post just to react was unnecessary friction. Added a compact `TileReactions.jsx` row (😂🔥😭👍, tap to react, tap again to change) to every post tile (Jokes/Funny Lines/Quotes/Motivational/Memes) on the homepage — every button stops the click from bubbling to the tile's own link, same technique `TileShareButton` already used, so reacting never navigates. Backend gained one new endpoint, `GET /api/posts/reactions?ids=a,b,c`, returning counts for many posts in a single call; `Home.jsx` fetches it once per visible page of tiles (keyed off the raw fetched list, not the re-sorted display order, so toggling Trending/Newest doesn't re-fetch). Verified in browser: reacting on a tile tapped its count up live without leaving the homepage or changing the URL, on both meme tiles (white background) and text-post tiles (colored gradient background, `dark` styling variant). Done 2026-08-04.
- [x] **"This week" digest + Bulk Import (admin)** — the last two items from "Suggested, not started." Digest: a 3-line summary card at the top of admin Analytics (total plays/attempts, total views/shares, new content published — all counted over the last 7 days), one new endpoint (`GET /api/admin/digest`) that counts across every relevant model (`PlaySession`/`GameSession`/`FriendshipAttempt`/`PostEngagement`/`Engagement`/`Quiz`/`Post`/`Story`/`FriendshipQuiz`) rather than adding six separate mini-summaries. Bulk Import: a new `/admin/bulk-import` page with two tabs — Posts (paste a list, one joke/quote/etc. per line, same category+language for all, each line becomes its own published post) and Quizzes (paste a JSON array in the same shape the regular Quiz form already saves, useful for quizzes drafted elsewhere). Both reuse the existing `createPost`/`createQuiz` endpoints looped from the frontend rather than adding new backend write routes, and both report per-item success/failure afterward rather than failing the whole batch on one bad row. Verified end-to-end in browser: digest showed real counts (matching what the tables below it independently added up to); bulk-imported 3 posts and 1 quiz, confirmed all 4 in the database afterward. Done 2026-08-04.
- [x] **Achievement badges** — 7 badges (Beat the House 3x, Tried Every Game, Quiz Explorer, Perfect Score, 7-Day Streak, Reaction Fan, Super Sharer), entirely `localStorage`-based, no accounts, no backend. New `badges.js` accumulates lightweight stats (games played/won, quizzes completed, perfect trivia, reactions given, shares given — plus reusing the existing daily-streak count from `dailyQuiz.js`) from hooks dropped into the existing action points (`Game.jsx`, `Result.jsx`, `TileReactions.jsx`/`PostReactions.jsx`, `TileShareButton.jsx`/`ShareButtons.jsx`) rather than one new page per content type. Badges are computed fresh from current stats every time (not stored as "unlocked" flags), so raising a threshold later never needs a migration. A new `/badges` page (linked from the footer) shows all 7 with locked/unlocked state and progress ("2/3"); a `BadgeToast.jsx` mounted once in `App.jsx` shows a brief bottom-corner popup the moment a badge is newly earned, deduped via a separate "seen" list so it only fires once per badge ever, not once per page load. Verified end-to-end in browser: played Rock Paper Scissors to 3 wins, confirmed "Beat the House 3x" flipped to unlocked (🏆, 3/3) on the badges page and the unlock event fired (recorded in the seen-list). Done 2026-08-04.
- [x] **My Badges made more discoverable** — the badges page was only linked from the footer; added a 🏆 trophy icon to the header (next to the dark/light toggle) so it's one click away from every page, not just a footer scroll-down. Done 2026-08-04.
- [x] **Make Friendship Quiz results more share-focused** — `FriendshipResult.jsx` previously showed the score + a generic share-buttons row, not framed as a direct "beat this" challenge. Added "Think your friend can do better?" + a standalone "🏆 Challenge Your Friend" button above the existing share row. The button deliberately points at the *original invite link* (`getFriendshipShareUrl(instanceCode)`), not the score-result link — so it invites a new friend to guess about the same subject, not just re-share this one result. That required one small backend addition: `buildAttemptResult()` in `friendshipInstanceController.js` now also returns `instanceCode` (the `FriendshipInstance.code`), which wasn't previously exposed to the frontend. Verified end-to-end: created a real instance + attempt via the API, opened the result page in browser, confirmed the button's WhatsApp link correctly points at `/api/share/friendship/{instanceCode}`, not the result page. Done 2026-08-04.
- [x] **Added ad placement to the homepage and search results** — `AdSlot` (the ad placeholder, real ads swapped in once AdSense approves) previously only appeared on result/detail pages (quiz results, posts, stories, games, horoscope, friendship-quiz/compare results) — a visitor who only ever browsed tabs on the homepage, or used search, never saw an ad slot at all, even though the homepage is the highest-traffic page. Added one `AdSlot` below the content grid on `Home.jsx` (after whichever tab's tiles are showing, not above — so it doesn't push real content below the fold on first load) and one below the results on `SearchResults.jsx`. Verified in browser: the "Advertisement" placeholder box now renders on both pages once content has loaded. Done 2026-08-05.
- [x] **Search Console set up + fixed two real SEO bugs found along the way** — walked through adding `https://www.twegle.in` as a URL-prefix property (ownership auto-verified via the GA4 tag already on the site, no extra code needed) and submitting the sitemap. First submission came back "1 error — Sitemap is HTML," which turned up two genuine bugs, not just something to wait out: (1) Vercel's catch-all SPA rewrite (`{"source": "/(.*)", "destination": "/index.html"}`) was intercepting `/sitemap.xml` before it could reach the backend that actually generates it, so Google (and anyone) was getting the React app's HTML shell instead of real sitemap XML — fixed by adding a `frontend/vercel.json` rewrite that proxies `/sitemap.xml` to the Render backend, ordered *before* the catch-all (Vercel rewrites are first-match). (2) `frontend/public/robots.txt` still had its `Sitemap:` directive pointing at a `yourdomain.com` placeholder from before `twegle.in` was even registered — updated to the real domain. Verified the fix directly via `curl` (confirmed `Content-Type: application/xml` and real sitemap content) before having the site resubmit; second submission came back **Success, 140 pages discovered**. Done 2026-08-05.
- [x] **Google Analytics (GA4) wired in** — walked through creating the GA4 property together (account/property setup, data-sharing settings, business objectives, Web data stream for `https://www.twegle.in`), then added the resulting Measurement ID (`G-KYTM35VQCT`) to `frontend/index.html`. Loads only on the public site — explicitly skipped on `/admin/*` routes (same pattern as the dark-mode script's admin exclusion) so admin/editor CMS usage doesn't pollute visitor analytics. Verified in browser: `gtag.js` loads and `window.dataLayer` populates on public pages, confirmed absent on `/admin/login`. Search Console (separate, still the owner's task) split out into its own pending item since it covers something GA4 doesn't — how people find the site via Google search itself. Done 2026-08-05.
- [x] **Fixed admin login card off-center + unnecessary vertical scroll on mobile** — `Login.jsx` used `min-h-screen` (`min-height: 100vh`), and mobile browsers size `100vh` against the *largest possible* viewport (address bar hidden), not what's actually visible on first load (address bar showing) — so the centering box was taller than the real visible area, causing both a scrollbar that shouldn't exist and the centered card visibly sitting above where it should be. Switched to `min-h-dvh` (`100dvh`, dynamic viewport height — tracks the real visible viewport, supported natively by Tailwind v4), same fix pattern browsers now recommend generally for this exact "100vh mobile" problem. Verified in browser at a mobile viewport: `document.documentElement.scrollHeight` now exactly equals `clientHeight` (no overflow) and the container height exactly matches the viewport. Done 2026-08-04.
- [x] **Fixed Dashboard causing a page-wide horizontal scrollbar on mobile** — the Day/Week/Month/Year/All range pills had no wrap safety (`flex` with no `flex-wrap`), and the Top Content boxes used `overflow-hidden`, which clips a too-wide row instead of giving that individual widget its own scrollbar — on a narrow phone this let content push the whole page wider rather than staying contained. Fixed: the range pills now wrap to a second line (`flex-wrap`) instead of forcing the row wider; each Top Content box now uses `overflow-x-auto` (so if a single very-long title still needs more room, that one box scrolls internally, exactly as requested) plus `min-w-0` added through the `TopList` widget/grid/list-item chain so nothing forces its container wider than the viewport; the page root also gained a defensive `overflow-x-hidden`. Verified in browser: `document.body.scrollWidth` now exactly equals the mobile viewport width (375px, zero page-level overflow), confirmed via a full-tree scan for any element wider than the viewport. Done 2026-08-04.
- [x] **Dashboard upgrades: date-range selector, Top Content beyond quizzes, logo links to Dashboard, login lands on Dashboard** — four follow-up requests on the new Dashboard. Confirmed login already lands on `/admin/dashboard` by default (shipped with the Dashboard itself) and didn't need further changes. **Range selector**: Day/Week/Month/Year/All pills at the top now control both the "This week"-style digest card (heading changes to "Today"/"This week"/"This month"/"This year"/"All time") and which content counts as "top" — `computeWeeklyDigest` in `digestController.js` was generalized to `computeDigestForRange(since)` (`since: null` means no date filter, for "All"). Published-content tile counts and the unread-feedback count deliberately stay current-state, not time-windowed, since "how much do we have live right now" has no date range. **Top Content beyond quizzes**: `GET /api/admin/dashboard` now returns `topContent: { quizzes, friendshipQuizzes, games, posts }`, each the top 5 for the selected range — friendship quizzes ranked by `FriendshipAttempt` count per template, games by `GameSession` count per slug (game titles resolved client-side from the existing static `GAMES` registry, since games have no database row to `$lookup`), posts by `PostEngagement` count (views+shares combined). Rendered as 4 small ranked lists in a 2-column grid via a shared `TopList` component. **Logo → Dashboard**: the sidebar logo (previously a plain non-interactive div) is now a `Link` to `/admin/dashboard` on both mobile and desktop, collapsed or not. Verified end-to-end in browser: logged in and landed on Dashboard automatically, all 4 Top Content lists showed real ranked data, switching Week → All correctly changed both the digest numbers and every Top Content list, and clicking the logo navigated to Dashboard. Done 2026-08-04.
- [x] **Fixed Activity page still looking cropped on mobile** — follow-up to the Analytics table fix: that fix made tables horizontally *scrollable* on narrow screens rather than clipped, but mobile browsers hide scrollbars by default, so a 640px-wide, 5-column table on a 375px screen still just looked cut off with no visible hint there was more to see. Activity's table specifically (the worst offender — 5 columns) now switches to a stacked card per entry below the `sm` breakpoint (same layout the Dashboard's Recent Activity widget already uses), with the original table kept for `sm` and up where there's actually room for it. No horizontal scrolling needed on mobile at all now. Also fixed a small pre-existing gap where `RESOURCE_LABELS` had no entry for `story`, so story entries showed the raw value instead of "Story." Verified in browser at a 375px mobile width: page has zero horizontal overflow and every entry reads as a clean stacked card. Done 2026-08-04.
- [x] **Admin Dashboard + fixed cropped Analytics tables on mobile** — two requests together. Cropped tables: every admin table (Analytics' 7 tables, Activity's log) wrapped its `<table>` in a div with `overflow-hidden`, which clips anything too wide for the screen instead of letting it scroll — on mobile, wide tables like Post Engagement lost content off the right edge with no way to reach it. Changed every wrapper to `overflow-x-auto` with a `min-w-[...]` floor on the table itself, so columns keep a sane minimum width and the table scrolls horizontally instead of squeezing or clipping. Dashboard: admin login now lands on a new `/admin/dashboard` page (also the `/admin` index route) instead of jumping straight to the Quizzes list — one new backend endpoint (`GET /api/admin/dashboard`, `dashboardController.js`) aggregates everything in one round trip: the same "this week" digest Analytics shows, published-content counts per type (4 clickable tiles), an unread-feedback callout (only shown when count > 0), the top 5 quizzes by all-time plays, and the 5 most recent activity-log entries. `computeWeeklyDigest()` was extracted out of the existing digest endpoint so both reuse the exact same numbers instead of duplicating the aggregation. New "🏠 Dashboard" nav item added as the first sidebar link. Verified end-to-end in browser: logged in, landed on the dashboard with real counts/top quizzes/recent activity matching the database, and confirmed Analytics tables now scroll horizontally instead of cropping on a simulated mobile viewport. Done 2026-08-04.
- [x] **Fixed blank search/filter fields + mobile nav always expanded (admin panel)** — reported alongside the login bug: search boxes and filter dropdowns showed blank text on mobile (same forced-dark-mode root cause as login, just not yet patched on every admin page), and the left sidebar's nav items always rendered inline on mobile instead of behind a toggle, pushing page content down. Fixed both: (1) every admin `<input>`/`<select>`/`<textarea>` across QuizList/PostList/StoryList/QuizForm/PostForm/StoryForm/FriendshipQuizForm/QuickAdd/BulkImport/Admins now has explicit `bg-white text-gray-900`, same belt-and-braces fix as the login page; (2) `AdminLayout.jsx` gained a hamburger toggle (☰/✕) shown only below `lg` — nav items and the logout footer are now hidden by default on mobile and only render when tapped, auto-closing after following a link; desktop sidebar behavior is unchanged. Verified in browser with the color scheme forced to dark and viewport at mobile width: search/filter text renders correctly, and the sidebar starts collapsed behind the hamburger, expands on tap, and closes after navigating. Done 2026-08-04.
- [x] **Fixed invisible text on admin login (mobile dark mode)** — reported: on a mobile browser, typing into the admin login email/password fields showed nothing on screen, but clicking "Log in" worked (the value was being captured, just not rendered visibly). Root cause: `index.html`'s no-flash theme script sets the `dark` class on `<html>` based on system/saved theme with no awareness of route — so a phone with system dark mode enabled got `color-scheme: dark` (`index.css`) applied to `/admin/login` too, even though the admin panel has zero `dark:` styling anywhere (deliberately out of scope). The browser then rendered the unstyled native `<input>` text using its dark form-control color on the input's actual white background — invisible. Fixed in 3 layers: (1) the inline script now skips setting `dark` at all when `location.pathname` starts with `/admin`; (2) `Login.jsx` and `AdminLayout.jsx` each strip the `dark` class on mount too, covering the edge case of navigating into admin client-side without a full page reload; (3) the login inputs now also set explicit `bg-white text-gray-900` as a belt-and-braces guard. Verified in browser with the color scheme forced to dark: `color-scheme` computed as `light` on `/admin/login`, and typed text showed white background + dark text as expected. Done 2026-08-04.
- [x] **Fixed Back not scrolling to top** — reported: clicking a `CrossPromo` link then Back left the page scrolled down instead of at the top, unlike every other navigation on the site. Root cause: the browser's own "auto" scroll restoration on Back/Forward races the existing `ScrollToTop` effect (`App.jsx`) and wins, restoring the old scroll position asynchronously after `scrollTo(0, 0)` already ran. Fixed by setting `window.history.scrollRestoration = 'manual'` once, so the app is the only thing managing scroll position on every navigation, Back included. Verified: scrolled down on a Result page, followed a `CrossPromo` link, hit Back — landed at the top both times. Done 2026-08-04.
- [x] **Cross-content "next thing" recommendations** — Result/Post/Story pages already showed "You might also like," but only same-type suggestions (quiz→quiz, post→post, story→story). Added a new `CrossPromo.jsx` component — a small pill strip below the existing same-type suggestions offering one other content type each time: a random game (static list, no fetch) always shown, plus a random post on Result/Story pages ("😂 Quick fun") or a random quiz on Post pages ("🧠 Take a quiz" — deliberately not another post, since that page's own suggestions already cover posts). No backend changes, aimed at raising pages-per-visitor. Verified end-to-end in browser on all three pages, including a Hindi story (language correctly passed through so the suggested post/quiz matched). Done 2026-08-04.
- [x] **Shareable image for "Compare with a friend" results** — a new `drawCompareCard()` in `shareImage.js`, reusing the same Canvas 2D story-image pipeline (1080x1920, no DOM-screenshot library) `shareOrDownloadImage` already uses for quiz/post results, but laid out as two vertical halves — one person's name/emoji/result per side, a "VS" badge on the dividing line, a "🎉 They Matched!" / "🔀 Different Results!" headline up top. New `shareOrDownloadCompareImage()` export, and the share/download-vs-native-share-sheet logic was factored out into a shared `shareOrDownloadBlob()` helper so both image types reuse it instead of duplicating it. Wired into `CompareResult.jsx` as a "📸 Share as Image" button next to the existing text-link share buttons. Verified end-to-end in browser: created a real compare instance via the API, opened the reveal page, clicked the button, confirmed no errors and the button correctly returned to its normal state after generating (headless test environment has no `navigator.share`, so it exercises the download-fallback path). Done 2026-08-04.
- [x] **End-user accounts** — username + password only, no email/phone collected anywhere (sidesteps child-data-protection exposure by design). Password reset uses a one-time Recovery Code (`TWEGLE-XXXX-XXXX`, shown once, rotates on every reset/regenerate) since there's no email/SMS channel. Login is entirely optional — every existing feature still works fully for guests; nothing is gated behind an account. Public Gamer Tag is separate from the private login username. Verified locally end-to-end (signup, login, wrong-password, forgot-password/reset, rate limiting, logout, account page) with zero impact on any existing feature. Done 2026-08-05.
- [x] **Show Gamer Tag "up front" in the header + game leaderboards** — the smallest, safety-first slice of the original "Gamer Tag up front everywhere" request (the full-avatar-badge/picker part shipped separately, below). The header's account icon shows a distinct logged-in state: a colored circular badge with the Gamer Tag's first initial (e.g. "Q" for "Quiz Wizard"), replacing the plain 👤 guest icon, so a returning visitor can tell at a glance they're logged in without hovering. `GameLeaderboard.jsx` checks the account session when submitting a score: logged-in visitors see "Saving as {Gamer Tag}" and submit with one click, no typing; guests still get the manual nickname field, unchanged. Verified locally: created a test account, confirmed the header badge showed the correct initial, played a full round of "Guess the Number," and confirmed the leaderboard entry saved under the account's Gamer Tag with no nickname prompt. Done 2026-08-05.
- [x] **Avatar picker for end-user accounts** — 14 preset emoji avatars (🦄🐱🐼🦊🐸🌟🔥😎🐶🐰🦁🐨🎉🌈, expanded from an initial 8 per follow-up request the same day; no image uploads, no file storage needed) selectable on the Account page. New `avatar` field on `EndUser` (nullable — defaults to no avatar, falling back to the Gamer Tag's initial letter), validated server-side against the fixed list (`isValidAvatar` in `validators.js`) so the choice can't be tampered with client-side. The header's account icon shows the chosen avatar in place of the initial-letter badge once one is picked. Verified locally: picked an avatar, confirmed it updated instantly in the header, persisted after a full page reload and was actually saved server-side (checked via `/api/users/me`), and confirmed the backend rejects any emoji outside the preset list (tested with 💀 directly against the API). Done 2026-08-05.
- [x] **Merge the 5 post categories into one browsable feed + reorder all homepage tabs** — Jokes/Funny Lines/Quotes/Motivational Quotes/Memes collapsed from 5 separate homepage tabs into one "💬 Posts" tab (continuous scroll feed, same `Post` model/`category` field as before) with category filter chips (All/Jokes/Funny Lines/Quotes/Motivational/Memes), same pattern the Quizzes tab already used. Discussed the resulting tab order explicitly with the owner through a monetization lens (goal: maximize pageviews/ad impressions) — agreed order: **Quizzes → Friendship Quiz → Games → Posts → Stories → Horoscope** (Puzzles later slotted in right after Quizzes, see below — grouped together since they share the same daily-streak mechanism). Quizzes stays first over Friendship Quiz specifically because it's the proven volume/share driver (26 quizzes, solo shareable result cards, no second person needed to go viral) — a deliberate override of the owner's initial draft order, which had Friendship Quiz first. Homepage cut from 10 tabs to 6. Verified locally: tab order renders correctly, Posts shows a mixed feed by default, each category chip filters correctly (spot-checked Memes-only), `Browse.jsx`'s standalone `/browse/jokes` etc. pages are untouched and still work (they never depended on the homepage tab list). Done 2026-08-05.
- [x] **Add Puzzles as a new content type** — requested 2026-08-05, scope refined and approach decided the same day. **Approach: simple admin-authored text puzzles** (riddles/brain-teasers — a question + reveal-the-answer card), same admin CRUD/content pattern as Quiz/Post, deliberately **not** a coded puzzle-generator engine — decided purely on a monetization basis: individual admin-authored puzzles each become their own shareable page with their own ad slot, unlimited/cheap to keep adding. **Includes picture puzzles** (optional `imageUrl` field on the same `Puzzle` model, paste-a-URL for now, same as Memes) — not a separate category, just a puzzle with an image attached. Three difficulty tiers (displayed as "Warm-Up / Challenge / Brain Buster") is the only filter dimension. Reuses the existing "daily streak" mechanism: `recordDailyQuizCompletion` generalized to `recordDailyActivityCompletion` in `dailyQuiz.js`, so completing either the daily Quiz or the new "Puzzle of the Day" (via `pickPuzzleOfTheDay`) keeps the same shared `localStorage` streak alive. Wired into every shared system: `Engagement`/`Feedback` content-type enums, search, sitemap, share-preview OG tags, admin dashboard/Analytics. Seeded 16 starter puzzles (14 English, 2 Hindi, 1 picture puzzle). Verified end-to-end locally: homepage banners (Quiz of the Day + Puzzle of the Day), Puzzles tab with working difficulty-chip filtering, `/puzzle/:id` detail page (Reveal Answer, shared streak confirmed via `localStorage` inspection — same `dailyQuizStreak` key as quizzes), admin create/edit/delete, search surfacing puzzles, and sitemap including all published puzzle URLs. Done 2026-08-06.
- [x] **Admin visibility/management of end-user accounts (Members)** — New `EndUserList.jsx` (`/admin/end-users`, page titled "Members"), same list/pagination/search pattern as `Admins`/`FeedbackList`, showing username, Gamer Tag, avatar, signup date, and status; clicking a row opens a detail modal with the same fields plus exact join timestamp. `EndUser` gained a `status` field (`active`/`disabled`) — reversible, not a delete — plus a Delete action for permanent removal. Disabling takes effect immediately, not just on next login: `requireUserAuth` checks live status on every request, not just at login, so an already-issued token can't keep working after an admin disables the account. Read access for Super/Editor/Analyst, moderation actions (disable/delete) for Super/Editor only — same read/write split as every other content type. No password or recovery-code data is ever exposed (the modal states this directly). Found and fixed two small pre-existing bugs from the Puzzles build while in this area: the Report button 400'd on every puzzle page (a hardcoded content-type whitelist in `feedbackController.js` was never updated), and Activity/Dashboard's resource-type labels fell back to the raw lowercase string for `puzzle`/`endUser` entries. Also deleted 18 leftover test accounts found in production during review, and paginated the Activity log (the one admin list page that was still a flat 200-entry cap). Verified end-to-end: created/disabled/re-enabled/deleted temp accounts, confirmed an already-issued token was rejected after disabling, confirmed search/pagination/modal all work. Done 2026-08-06.
- [x] **Migrate localStorage stats (streak, badges) to the account** — reported directly: the same account logged into a phone and a desktop showed a different streak day-count and different unlocked badges on each. `EndUser` gained a `stats: Mixed` field plus `GET`/`PUT /api/users/me/stats`; a new `frontend/src/utils/statsSync.js` pushes local changes up (wired into `dailyQuiz.js`'s and `badges.js`'s single mutation funnel points) and merges the server copy in on load/login (max of every counter, union of every set, most-recent-`lastDate` wins for the streak) so switching devices never loses progress on either side. Game leaderboard nicknames needed no migration — they already read the account's Gamer Tag directly when logged in. Verified end-to-end locally: two simulated "devices" with different local progress on the same account converged to the same merged numbers after each loaded once. Done 2026-08-07.
- [x] **"Already attempted" indicator on quiz/puzzle tiles** — a small "✓" mark on any Quiz/Puzzle tile the visitor has already completed/revealed, so browsing the homepage grid doesn't require remembering what you've already tried. Scoped down to Quiz + Puzzle only — Friendship Quiz tiles were left out since clicking one *creates your own instance* rather than repeating a fixed piece of content, so "already attempted" doesn't map cleanly there. Reused `badges.js`'s existing `quizzesCompleted` array for quizzes; added a matching `puzzlesRevealed` array (written for every puzzle revealed, not just the daily one) that also flows through the cross-device sync built the same day. Hard constraint: the mark must never change tile height (cards in the same row must stay equal-height) — solved with a small `absolute`-positioned badge overlapping the tile's emoji/thumbnail corner, never part of the flex layout. Verified directly: measured the same tile's height with and without the mark present and confirmed it's pixel-identical. Done 2026-08-07.
- [x] **Cross-device stats sync — closed two real gaps found right after shipping** — reported directly on real accounts. First: revealing a puzzle on one device didn't show up on an already-open tab of the same account on another device, even after that tab's own initial sync had run — root cause was the sync only firing once per full page load; fixed with a `visibilitychange`-triggered re-sync (`UserAuthContext.jsx`) plus a `'twegle-stats-synced'` event so already-mounted tiles actually re-render on new data (`statsSync.js`, `Home.jsx`). Second, reported right after: switching in-app between homepage tabs (e.g. Games → Quizzes) never picked it up either, only a full reload did — because `visibilitychange` only fires when the tab/app itself is hidden or shown, not on in-page navigation. Closed with a plain 20-second interval re-sync alongside the visibility listener, so it converges automatically regardless of how the tab is used. Verified both fixes locally and the first on real production accounts (`shash_saxena`) by pushing a change from a simulated second device and confirming an already-open tab picked it up with zero reload, zero visibility change. Done 2026-08-07.
- [x] **Dark mode for the admin panel** — queued at the owner's request on 2026-08-06 (deliberately deferred at the time, since it was originally scoped as public-site-only), picked up this session. Reused the exact same mechanism the public site already had — same `.dark` class, same `ThemeToggle.jsx` component, same `twegle-theme` localStorage key, so a preference set on one side carries over to the other. Removed two guards that previously forced admin routes to always stay light (`index.html`'s no-flash script had an admin-route exception; `Login.jsx`/`AdminLayout.jsx` each had a `useEffect` actively stripping the `dark` class on mount) — both existed only because admin had zero `dark:` styling at the time, so they became counterproductive rather than protective once every admin page was actually styled. Added `dark:` variants across all 22 admin components/pages (layout, login, every content-type list/form pair, Analytics, Activity, Feedback, Admins, Members, Quick Add, Bulk Import) and a `ThemeToggle` in both the mobile top bar and the desktop sidebar footer. Verified in the browser: toggled from both mobile and desktop chrome, confirmed it persists across a full reload including on the login page, ran an automated contrast scan across 5 representative pages in dark mode (zero real issues, only the expected emoji/white-on-gradient false positives), confirmed form inputs render with visible dark backgrounds and light text (the exact bug the old light-only guard was protecting against), and confirmed light mode still works correctly afterward. Done 2026-08-07.
- [x] **Add 2 more games** — Simon Says (classic growing-pattern memory game, 4 colored pads, "win" framed as reaching round 10) and Whack-a-Mole (20-second reaction game, 3x3 grid, "win" framed as 15+ moles whacked) — both wired into the existing `src/games/registry.js` pattern, `Game.jsx`'s render switch, the backend `GAME_SLUGS`/`GAME_LEADERBOARDS`/`GAME_META` allowlists, and the leaderboard system. 7 → 9 games live. Verified end-to-end locally: played both to a real win and loss, confirmed hit detection was pixel-accurate on Whack-a-Mole (14/14 clicks landed via a direct JS click loop, avoiding tool-latency races against the 850ms mole window), confirmed share buttons/leaderboard both render correctly on game-over, and confirmed both tiles render correctly on the homepage Games tab. Done 2026-08-07.
- [x] **Sound effects toggle for Games** — three short tones (click/win/lose) synthesized on the spot with the Web Audio API rather than shipping recorded audio files — no assets to host, same zero-cost reasoning as Stories' Web Speech API read-aloud. Muted by default; a new 🔊/🔇 toggle in `Game.jsx` (same pattern as the site's `ThemeToggle`) opts in, persisted to `localStorage`. Win/lose wired once into the existing `handleGameEnd` funnel (covers all 9 games with no per-game changes); click wired once via a click listener around the whole game area that checks for a `<button>` target (covers every game's actual interactions the same way). Verified in the browser across two structurally different games (Guess the Number, Simon Says) with zero console errors, and confirmed the mute preference persists and survives switching games. Done 2026-08-08.
- [x] **Footer link fixes** — Puzzles was missing from the "Explore" column entirely (real gap — every other content type had a link, Puzzles didn't), and "🏆 My Badges" was sitting under "Company" alongside About/Privacy/Terms even though it's a feature, not company/legal info — moved to the end of "Explore." Verified both by extracting the rendered footer link list directly. Done 2026-08-08.
- [x] **FAQ / Help page with FAQ schema markup** — a new `/faq` page (`Faq.jsx`), linked from the footer's "Explore" column, answering 10 common questions (accounts, cost, privacy, streaks, badges, languages, kid-appropriateness, reporting content) in a click-to-expand accordion. Ships a `FAQPage` JSON-LD block (same pattern Home.jsx already uses for its `WebSite` schema) so Google can show the FAQ rich-snippet treatment directly in search results — a cheap SEO win with no new backend work, since the content is static/hand-written rather than pulled from a database. Added to the sitemap alongside About/Privacy/Terms. Verified in the browser: all 10 questions render and expand/collapse correctly, the JSON-LD parses with the right `@type` and question count, dark mode text is legible, and the new footer link resolves to `/faq`. Done 2026-08-08.
- [x] **Personal history page ("My Activity")** — a new section on `/account`, between the Gamer Tag form and Recovery Code, surfacing the account stats that already sync server-side (see `statsSync.js`) but previously had no view beyond the small Badges page. Four count tiles (quizzes taken, puzzles solved, games played, day streak) plus three titled lists — quizzes completed and puzzles solved are resolved from bare slugs/ids (all that's actually stored, see `badges.js`'s `stats` shape) into real titles by fetching the full public quiz/puzzle lists client-side and filtering, since there's no per-user, per-play history stored server-side (only aggregate id lists, no titles or dates). Games played reads from the static `GAMES` registry the same way. Links out to `/badges` rather than duplicating the badge grid. `getStats()` in `badges.js` was exported (previously module-private) since this is its second caller. Verified in the browser: signed up a test account, seeded realistic stats via localStorage, confirmed all 4 counts and all 3 lists rendered with correct titles/links, confirmed dark mode text is legible, confirmed empty-state sections correctly don't render when a list is empty. Done 2026-08-08.
- [x] **My Activity polish — 5 fixes from direct feedback right after shipping** — (1) The page's container was `max-w-md` (448px, inherited from Login/Signup's tight auth-form width) which read as "mobile-only" on a wide desktop browser once the richer My Activity content was added; widened to `max-w-2xl`, matching the Badges page it links to. (2) The quizzes/puzzles lists had `max-h-40 overflow-y-auto`, creating a nested scrollable region — reported as focus/scroll "jumping" when a page-scroll transitioned into the inner list's own scroll; removed the cap so the whole page scrolls as one, no nested region to capture the wheel. (3) Section headers (`QUIZZES YOU'VE TAKEN`, etc.) were `text-gray-400`, too low-contrast to read comfortably — darkened to `text-gray-600 dark:text-gray-300` and confirmed already-bold. (4) Added a matching "Streak" detail section (previously the only stat tile with no explanatory list below it) clarifying that it's one shared counter — completing either the daily Quiz or the daily Puzzle keeps it alive, not two separate streaks. Also confirmed for the owner that Friendship Quizzes and Posts are intentionally not included — neither is tracked as a completion list in `badges.js`'s `stats` shape (only aggregate counters like `reactionsGiven`), so there's nothing to resolve into titled entries. (5) Made the 4 count tiles clickable — each now smooth-scrolls to its matching detail section below (`scrollIntoView`), disabled/inert when its count is 0, so clicking "Quizzes taken" jumps straight to "QUIZZES YOU'VE TAKEN" instead of leaving the visitor to scroll and find it themselves. Done 2026-08-08.
- [x] **Draft preview link** — a signed, temporary admin-only preview URL so a draft can be viewed exactly as a visitor would see it without publishing it first. New `backend/src/utils/previewToken.js` signs a short-lived (30 min) JWT carrying `{ purpose: 'preview', contentType, id }` off the same `JWT_SECRET` already used for admin/end-user auth, but with a payload shape that can never be replayed as an admin or user session token; a new admin-only endpoint (`GET /api/admin/preview-link`) mints one on demand. All 5 public "get by slug/id" controllers (Quiz, Post, Story, Puzzle, Friendship Quiz) now fetch the document unconditionally, compute `isLive` in JS, and only 404 if it's not live *and* the token doesn't validate against that exact resolved `_id`/`contentType` — so slug-based and id-based lookups share one scheme without extra bookkeeping. On the frontend, a new "👁 Preview" button (outside the existing `canWrite` gate, so every admin role can use it) sits on all 5 admin list pages, opens the real public page in a new tab with `?preview=<token>` appended; a new amber `PreviewBanner` renders on all 5 public view pages plus `Result.jsx` (reached after finishing a previewed quiz) whenever a valid preview token is present. Every real-visitor side effect a preview visit could otherwise pollute — view/share engagement recording, "already attempted" badge tracking, daily-streak completion, and quiz play-count recording — is explicitly skipped when a preview token is present, so previewing a draft never touches real analytics. Friendship Quiz previewing is deliberately fetch-only (an admin can see the draft template render correctly); the actual "create your own instance" submit flow was left untouched as a real content-creation action, not a preview concern. Verified end-to-end locally via direct API calls (guest request 404s on a draft, a tampered/garbage token still 404s, a valid token returns 200 with the draft content) and in the browser (preview banner renders, finishing a previewed quiz correctly forwards the token to the Result page and shows the banner there too, and a `PlaySession` count check before/after confirmed zero real play was recorded). Done 2026-08-08.
- [x] **Achievement levels + global leaderboard** — a 10-level progression ("🏆 My Achievements") layered on top of the existing 7 badges, requested directly with the concept refined together across several rounds: levels *are* the headline achievement system (not a separate progress bar), the old 7 badges stay fully separate as a smaller "🎁 Bonus Badges" section (no shared points), level names are fun/GenZ-styled with a plain-language subtitle explaining the requirement rather than a name tied to one activity (e.g. not "Quiz Curious"), and points come from everything combined (quizzes, puzzles, games, shares, reactions, streak weeks) — one single score, not per-category requirements, kept simple on purpose. `levels.js` (identical copy on frontend and backend, same reasoning as the existing `GAME_SLUGS` duplication) defines the 10 levels (Fresh Face → Twegle Legend) and the points formula. Reaching a level unlocks a toast with a "📸 Share this" button (same Canvas share-image pipeline quiz results use); a new public `GET /api/leaderboard/levels` computes every account's points server-side (never trusts a client-submitted score) and returns the top 100, shown on a new `/leaderboard` page — requires an account to appear on it, same reasoning as the existing per-game leaderboards. Verified in the browser: seeded a real points jump then triggered one genuine tracked action to confirm the level-up detection fires from the real code path (not just in isolation), confirmed the Badges page and toast both reflected the new level immediately, confirmed the leaderboard page correctly listed a real synced account ranked by server-computed points, and confirmed dark mode legibility on both new pages. Done 2026-08-08.
- [x] **Achievement levels points rebalance** — reported right after the levels feature shipped: the top level was reachable in one sitting since games and reactions are single-click, infinitely-repeatable actions with no natural cap (unlike quizzes/puzzles, which are limited by how much content exists), and it was never clear which activity earns how many points toward which level. Fixed in `levels.js` (both copies): `calculatePoints()` now caps counted game plays at 10 per game slug and counted reactions at 50 total before summing (quizzes/puzzles/shares stay uncapped — shares deliberately so, as the one behavior worth rewarding without limit); raised per-action point values and all 10 level thresholds so the top level still takes sustained, varied activity rather than one grind session. Added a "💡 How to earn points" table to the Badges page listing every scoring action's point value and cap, sourced directly from `levels.js`'s exported constants. Verified in the browser: seeded `localStorage` stats above both caps (20 plays each on two games, 200 reactions) and confirmed the displayed total matched the capped formula exactly (135 points), not the uncapped 325 it would have been without the caps. Done 2026-08-09.
- [x] **"📲 Add to Home Screen" button** — the site has been installable (manifest + service worker) since PWA support shipped, but nothing ever actually surfaced that to a visitor. New `useInstallPrompt.js` hook captures the browser's `beforeinstallprompt` event once on mount (suppressing the browser's own mini-infobar) and a small button in `Footer.jsx` only renders when that event actually fired and the site isn't already installed — clicking it shows the real native install dialog. Deliberately just a quiet, permanent footer link, never a banner or repeating popup: it's absent entirely on browsers that never offer installability (iOS Safari, or one that already decided not to), and disappears for good the moment it's used or the app gets installed, since a real prompt event is single-use either way. Verified in the browser: confirmed the button stays hidden under normal local conditions, then simulated the event to confirm it appears, the click correctly triggers the captured prompt, and it disappears afterward. **Follow-up the same day**: reported as too light/easy to miss — the initial outlined gray style matched the footer's muted social-icon row too closely. Restyled to the same solid violet-to-fuchsia gradient already used for the Achievements page's "Share my level" button, so it reads as a real call-to-action. Done 2026-08-09.
- [x] **Split the shared daily streak into independent Quiz and Puzzle streaks** — reported directly: doing only puzzles (or only quizzes) still advanced one combined "Day N" number with no way to tell which activity was actually keeping it alive, confusing after the two banners were built to look like separate mechanics. `dailyQuiz.js`'s single `recordDailyActivityCompletion`/`dailyQuizStreak` became two independent pairs — `recordQuizStreakCompletion`/`dailyQuizStreak` (kept the original key so nobody's existing streak reset) and `recordPuzzleStreakCompletion`/`dailyPuzzleStreak` (new, starts at 0 for everyone). `DailyQuizBanner`/`PuzzleOfTheDayBanner` now show "🔥 Quiz Streak"/"🔥 Puzzle Streak" with their own independent counts; My Activity (`Account.jsx`) gained a second streak tile and detail section; the Achievements page's "week-streak" badge and points-per-week now check the Quiz streak specifically (matching its own "Complete the Quiz of the Day 7 days in a row" description, a latent inconsistency from when the counter was shared); `calculatePoints()` (both frontend and backend copies) now sums a full-week bonus from each streak independently. Cross-device sync (`statsSync.js`, backend `leaderboardController.js`) treats any account's pre-split `streak` field as its legacy Quiz streak so no existing progress was lost; Puzzle streak simply starts fresh. Verified in the browser: seeded different Quiz/Puzzle streak counts and confirmed both homepage banners, the Achievements points total, and My Activity's two tiles/sections each showed the correct independent number; confirmed a legacy test account's old `streak` field still computed points correctly via the leaderboard endpoint. Done 2026-08-09.
- [x] **Shared images carried no link back to the site + stale share text** — two issues reported directly, one with a real shared level-up image as evidence. (1) The Canvas-drawn shareable images (level-ups, quiz/post results, compare cards) never rendered any URL at all — `navigator.share`'s accompanying caption text is frequently dropped entirely by the receiving app for a file share (Instagram/WhatsApp Stories post only the image), so a shared image had no way back to Twegle. Fixed by baking "twegle.in" directly into the image itself, in `shareImage.js`'s shared `drawLogo()` function (used by every card type), plus giving every caller's native-share caption text the actual page URL too, for platforms that do respect it. (2) The site-wide "Share Twegle" text in the footer and the floating `ShareSidebar` still only mentioned "quizzes, jokes, quotes," unchanged since before Puzzles and Games existed — updated to mention both. Verified in the browser: captured a real generated share image via a patched `canvas.toBlob`, rendered it back into an offscreen canvas, and confirmed pixels matching the "twegle.in" text actually render in the image (711 white-ish pixels in that region vs. the surrounding gradient); confirmed the WhatsApp share link's text now includes "puzzles, games." Done 2026-08-09.
- [x] **Footer mobile layout fixes** — two issues reported directly from a phone screenshot. (1) The "Share Twegle" buttons rendered centered on their own row, looking off against the left-aligned "SHARE TWEGLE" label above them — `ShareButtons.jsx` gained an optional `align` prop (defaults to the original centered behavior everywhere else it's used — Result/Post/Puzzle pages, etc.) and the footer passes `align="start"` to flush them left. (2) The "Explore" and "Company" footer columns stacked one below the other on mobile, leaving a lot of unused horizontal space next to "Explore" and making the page unnecessarily long — the outer 3-column grid only applied at `sm:` and up; changed it to a 2-column grid on mobile with the brand column spanning both, so Explore and Company now sit side by side in the row below it, while desktop (already 3 columns in one row) is unchanged. Verified in the browser at 375px and 1280px. Done 2026-08-09.
- [x] **Fixed the one seeded picture-puzzle's question/answer, which didn't match its own image** — reported directly: the puzzle's image looked like it already gave away the answer. Root cause was a content mismatch, not a code bug — the puzzle used a fixed Lorem Picsum placeholder photo (a person on Trolltunga, a famous Norwegian cliff), but its question ("Look closely at this picture. What everyday object is this a close-up of?") described a completely different kind of image (a zoomed-in rebus shot), and its answer was just a vague placeholder note ("It's a zoomed-in photo — take a guess... swap this for a real themed image any time") — left over from when this puzzle was first seeded, explicitly flagged in a code comment at the time as a stand-in never given real content. Rewrote the question/answer in `backend/src/scripts/seedPuzzles.js` to actually describe what the photo shows (a "name this famous cliff" riddle, answer: Trolltunga, Norway) so there's a genuine, honest puzzle again. Fixed directly on the live site via the admin panel's Puzzle editor (the seed script's upsert keys on question text, so simply re-running it would have created a second, duplicate puzzle instead of fixing the existing one). Verified live: the puzzle page now shows the corrected question, matching the actual image. Done 2026-08-09.
- [x] **Expanded the avatar picker from 14 to 28 presets** — requested directly (2026-08-10): 7 more "cute" emoji (🐹🐧🐢🦋🐥🐻🦉) and 7 more "gamer" emoji (🎮🕹️👾🤖⚡🎯🎧), on top of the original 14. Added to both `AVATAR_OPTIONS` copies that must stay identical — `frontend/src/pages/Account.jsx` (the picker UI) and `backend/src/utils/validators.js` (`isValidAvatar`, which rejects anything outside the list server-side regardless of what a tampered request sends). No layout change needed — the picker is a plain `flex flex-wrap`, so it just wraps into more rows automatically. Verified in the browser: all 28 render, selecting one of the new gamer emoji saves successfully via the real API with no errors. Done 2026-08-10.
- [x] **Reported back as still not the actual bug — fixed the real one: puzzle emoji spoiling the answer** — the report was about the small emoji shown next to *every* puzzle (not just the picture one), visible immediately alongside the question before "Reveal Answer" is ever tapped. Audited all 16 seeded puzzles and found 11 whose emoji was literally the answer object rendered as an icon — 🤧 next to "What can you catch but not throw?" (answer: a cold), 🎹 next to "What has many keys but can't open a single lock?" (answer: a piano), 👣 next to "The more you take, the more you leave behind" (answer: footsteps), plus 8 more of the same pattern (clocks, an egg, a candle, a towel, an envelope, and two Hindi riddles) — each one handed the answer away for free. Swapped all 11 to a neutral 🤔, or (for two — 🔑 for the piano/lock riddle, 🌍 for the stamp/travel riddle) to an emoji reflecting something already stated in the question text, not the hidden answer. Left the other 5 alone (💊 pills riddle, 🖼️ photo riddle, 🖼️ the Trolltunga picture puzzle, 🔊 the echo riddle, 🔤 the wordplay riddle) since each of those either matches the question's own stated premise rather than its answer, or (🔊 for "I speak... I hear...") adds nothing the riddle's own wording hadn't already implied. Added a permanent comment at the top of `seedPuzzles.js` warning against this for any future puzzle content. Fixed live via 11 direct `PUT /api/admin/puzzles/:id` calls through the admin session (re-running the seed script would have worked too, upserting by unchanged question text, but the direct-DB route used for the previous fix hit a persistent auth error from this network, so the admin API was used instead both times). Verified live: re-fetched all 16 puzzles via the admin API and confirmed every emoji matches the corrected seed file exactly; spot-checked the "catch a cold" puzzle page directly and confirmed it now shows 🤔, not 🤧. Done 2026-08-09.
- [x] **Admin-generated account recovery code for end-users** — requested 2026-08-10, built the same day. A fallback for when a user (mostly 8-18 year olds) is locked out and can't use the self-service `ForgotPassword.jsx` flow (lost their original code, no email/phone on file to recover it another way). From a user's row on the admin Members page (`/admin/end-users`), superadmin/editor can click "🔑 Recovery Code" to generate a fresh `TWEGLE-XXXX-XXXX` code that **expires in 10 minutes** (new `EndUser.recoveryCodeExpiresAt` field — only set for admin-issued codes; a user's own permanent code has none and never expires) — long enough to paste into WhatsApp/email and have the user act on it, short enough that an old message lying around isn't a standing risk. The resulting modal shows Username/Recovery Code/Expiry and a "Copy Username + Code" button that copies both as one ready-to-paste message. Every generation is written to the existing Activity Log for accountability (`resourceType: 'endUser'`, action `'update'`) — caught and fixed a real pre-existing bug in the process: `ActivityLog`'s `resourceType` enum didn't actually include `'endUser'` (or `'puzzle'`), so every existing admin end-user status-change/delete action, and any future puzzle-content log, was silently failing to write (the shared `logActivity()` helper swallows errors) — fixed by extending the enum, not just working around it for the new feature. Verified end-to-end via direct API calls against a local test account: code generation, Activity Log entry actually persisting, successful password reset with a valid code, and correct rejection of an expired one (`"This recovery code has expired. Ask an admin for a new one."`). Done 2026-08-10.
- [x] **Rematch button on both live 2-player games** — requested directly (2026-08-10): after Tic-Tac-Toe/Connect Four finishes, either player can click "🔁 Rematch" to instantly start a new round without sharing a new link — a `rematch` socket event resets the same game document in place (same code/room), and the loser of the last round starts the next one (a draw keeps the original starter) for fairness. No consent handshake needed, same trust model as every other move in these games. Verified end-to-end locally for Tic-Tac-Toe (win → rematch → board reset live on both tabs, correct starter); Connect Four uses the identical pattern.
- [x] **Converted Tic-Tac-Toe's two-player mode to live (socket.io) + added a Games "Single Player / 2 Player" filter** — requested directly right after Connect Four shipped. Reused Connect Four's exact socket pattern: `realtime/ticTacToeSocket.js` mirrors `realtime/connectFourSocket.js` (a `/tic-tac-toe` namespace, `joinRoom`/`makeMove` events), the REST `POST /:code/move` route was removed entirely (moves go over the socket now, matching Connect Four's split), and `joinGame` broadcasts over the socket the same way Connect Four's does. Frontend: `TicTacToeMultiplayer.jsx`'s 4-second poll was replaced with the same connect/joinRoom/gameState-listener effect `ConnectFourMultiplayer.jsx` already uses. Also added a `players` field (`['single']`/`['friend']`/both) to each entry in `registry.js` and a new "Single Player"/"2 Player" filter chip row on the homepage Games tab (client-side filter, since the registry isn't database content) — Tic-Tac-Toe now correctly shows under both filters, Connect Four only under "2 Player." Verified end-to-end locally: live join broadcast and live move sync both confirmed working for Tic-Tac-Toe exactly like Connect Four, and the category filter correctly includes/excludes games by their `players` field. Done 2026-08-10.
- [x] **Live two-player Connect Four (Twegle's first real-time feature)** — requested directly, after clarifying that "2-player game" meant genuinely live play (both screens updating instantly, like Ludo) rather than the existing async link-and-poll pattern Tic-Tac-Toe already had. Connect Four was picked as the first build over Ludo — simpler ruleset (one move per turn, no dice/tokens), proves the real-time infrastructure without also needing a complex ruleset, and doesn't create a second confusing "vs a friend" mode layered on top of Tic-Tac-Toe's existing one. Added `socket.io` (backend) and `socket.io-client` (frontend), both free/zero-cost. **Design: hybrid REST + socket.io, not pure in-memory game state** — room creation/joining still goes through REST and a `ConnectFourGame` Mongo document (mirroring `TicTacToeGame`'s pattern), but the actual disc-drop move happens over a socket.io connection and broadcasts to both players instantly instead of the 4-second poll Tic-Tac-Toe's page uses. Chosen specifically so a dropped connection (Render's free tier waking from a 15-minute idle sleep, a phone losing signal) never loses the game — a reconnecting client just re-joins and gets the real state from Mongo, same as a fresh page load. Caught and fixed one real bug during testing: joining a match happens over REST, so the creator's already-connected tab never found out a friend had joined until manually reloaded — fixed by having the join controller reach into the same `io` instance (stashed on `app.set('io', io)`) and broadcast the join too. Verified end-to-end locally with two separate browser profiles (shared localStorage across tabs in the same profile falsely appeared to duplicate the creator's role — not a real bug, just how localStorage works per-origin): live join broadcast, instant move sync, turn-flipping, vertical win detection, `ShareButtons` on finish, and reconnect resync via the `GET /:code` fallback all confirmed working. Done 2026-08-10.
- [x] **Single-player Connect Four (vs. "the house")** — requested 2026-08-12, built the same day. Connect Four previously only supported `players: ['friend']` in the game registry — it was built purely to prove the real-time `socket.io` infrastructure and never got an AI opponent, unlike Tic-Tac-Toe. Added a new client-side `ConnectFour.jsx` component mirroring `TicTacToe.jsx`'s pattern (human always red and goes first, AI is yellow, a short delay before the AI's move so it doesn't feel instant). The AI itself uses minimax with alpha-beta pruning at a fixed depth of 5, plus a standard windowed positional evaluation (scoring every possible 4-cell line by disc counts, with a center-column bias) for non-terminal positions at the depth cutoff — a well-documented, standard approach, since Connect Four's larger board (7×6 vs. Tic-Tac-Toe's 3×3) is too big to brute-force exhaustively the way Tic-Tac-Toe's minimax does. Wired into `Game.jsx` exactly like Tic-Tac-Toe: the challenge-a-friend form is no longer always shown for Connect Four (previously hardcoded on), single-player is now the default with "Challenge a friend" as a secondary link, and `?mode=friend` (from the homepage's "2 Player" filter) still opens straight to the challenge form. Also caught and fixed a real pre-existing bug in the process: `'connect-four'` was missing from the backend's `GAME_SLUGS` allowlist (`gameController.js`), which is also used by `engagementController.js` to validate `content_type: 'game'` — meaning Connect Four's page-view engagement tracking had been silently failing (404) ever since Connect Four shipped, for both single-player and the existing 2-player mode. Fixing the allowlist fixed both play-recording for the new single-player mode and the pre-existing broken view tracking. Verified end-to-end locally: AI responds and plays reasonably (contests the center, blocks/wins appropriately), turn-flipping and win/draw detection all correct, `/api/engagement` returns 201 instead of 404, homepage "Single Player" filter now lists Connect Four, "2 Player" filter still opens the challenge form directly.
- [x] **Snake and Ladder — single-player (vs. "the house") and live 2-player** — requested 2026-08-12, built the same day. A genuinely new game, not just an addition to an existing one, so both modes were built together mirroring the Connect Four/Tic-Tac-Toe patterns exactly: `SnakeLadderGame` model (positions per player, current turn, roundStarter for fair rematch alternation), REST create/join + a `/snake-ladder` socket.io namespace (`rollDice`/`rematch` events) for live 2-player, and a client-side-only `SnakeLadder.jsx` for single-player (the AI has no real decisions to make beyond rolling, so it just rolls randomly on a short delay, same spirit as the game itself). Board is the traditional numbered 10x10 zigzag layout with a generic, non-copyrighted set of ladder/snake jump squares — the exact same jump-square map is duplicated in a frontend util (`snakeLadderBoard.js`) and a backend util (`snakeLadder.js`), same reasoning Connect Four's win-check logic is already duplicated for its own client-only single-player mode. The die roll for live 2-player is always server-generated (`crypto.randomInt`), never trusted from the client, same "server is sole authority" rule every other live move in this codebase follows. Proactively added `'snake-ladder'` to the backend's `GAME_SLUGS` allowlist and a `shareSnakeLadder` preview handler up front this time, having just found the Connect Four bug where forgetting the allowlist entry silently broke view tracking. Verified end-to-end locally: single-player board renders with correct zigzag numbering and ladder/snake markers, AI rolls and moves correctly; live 2-player tested with two real socket connections — join broadcast, live roll sync, turn-flipping all confirmed, then scripted a full game to completion (23 rolls) confirming exact-100-to-win detection and rematch starter alternation (roundStarter flips correctly, same fix already applied to Connect Four/Tic-Tac-Toe). Homepage "Single Player" and "2 Player" filters both correctly list it.
- [x] **Snake and Ladder board-game visual pass** — reported directly as "not eye-catching," plain gray/white cells with tiny text. New shared components used by both single- and 2-player (so both upgrade together): `SnakeLadderBoard.jsx` (colorful gradient frame, warm checkered squares, bigger ladder/snake icons, ringed avatar tokens), `DiceDisplay.jsx` (an animated unicode die face that shuffles while rolling instead of a plain "Last roll: N" line), and `PlayerChip.jsx` (a status card per player showing name/position, highlighted when it's their turn, replacing the old plain text status line). Verified in the browser (light and dark mode, mobile viewport — no horizontal overflow, ladder-jump landing rendered correctly).
- [x] **"Surprise Me" button + "Continue where you left off" row on the homepage** — picked as the two highest-impact items from "Suggested, not started" when asked which would most help attract/retain visitors (pure engagement/retention plays, no backend work, both shippable in an afternoon, versus the bigger builds like Ludo/Chess or infra-dependent items like push notifications). **Surprise Me**: `SurpriseMeButton.jsx`, purely client-side — picks randomly from a pool built out of whatever's already loaded on the homepage (`allQuizzes`/`allPuzzles`, always populated; the static `GAMES` registry, no fetch needed; plus the active tab's own items opportunistically when it happens to be stories/posts/friendship quizzes) and navigates there via `useNavigate`, no new fetch just for this. **Continue where you left off**: a new `recentlyViewed.js` util (`frontend/src/utils/`) — a localStorage-only, capped-at-6, most-recent-first list, same anonymous-tracking spirit as `badges.js`'s streak/badges but a new capped-list pattern (badges.js's own lists grow unbounded, which wouldn't suit a short homepage strip). Hooked into the exact same `viewedRef`-guarded "record a view once per mount" `useEffect` every content page already has for `recordEngagement` (Quiz.jsx, StoryView.jsx, PuzzleView.jsx, Game.jsx, PostView.jsx) — reusing the existing "real view, not a preview" gate rather than building a second tracking mechanism. `RecentlyViewedRow.jsx` renders the strip (hidden entirely when empty) right below the hero, above the existing Daily Quiz/Puzzle banners. Verified end-to-end locally: visited a puzzle and a quiz, confirmed both correctly recorded to `localStorage['twegleRecentlyViewed']` and rendered back on the homepage strip; Surprise Me click correctly navigated to a random puzzle. **Two follow-up refinement rounds after mobile feedback**: (1) both were reported as not fitting well on mobile and taking up too much space — Surprise Me moved off the homepage entirely into `Header.jsx`'s icon row (rebuilt self-contained, fetching fresh on click instead of depending on a homepage-built pool) and `RecentlyViewedRow.jsx` shrunk from full gradient tiles down to small pill chips. (2) A second round of feedback reported the pill row showing a visible horizontal scrollbar with 2+ entries, the header icon still crowding the mobile search bar, and the 🎲 emoji not clearly reading as "Surprise Me" — fixed the scrollbar by adding the existing `.no-scrollbar` utility class, and hid the Surprise Me icon entirely behind a `SHOW_SURPRISE_ME = false` flag in `Header.jsx` per direct instruction to keep the feature/component intact for later re-enabling rather than removing it.
- [x] **Mobile fix for the "How to earn points" table + doc sweep** — reported directly, with a real screenshot: the merged streak row's longer note text and "Play a game"'s cap note had no room to sit next to the label on a narrow phone screen, forcing a horizontal scrollbar and a distorted-looking "Play a game" row — worse on mobile only, fine on desktop. First attempt (stacking every row's label above its value below `sm`) was reported as not looking as good as the original single-row layout, so reverted; fixed instead by touching only the two actual problem rows — merged the two separate streak rows into one ("🔥 Keep your Quiz/Puzzle streak — 25 pts each · per full week") and shortened "Play a game"'s note from "counts up to 10 plays per game" to "up to 10/game" so both fit on one line again, matching every other row's layout. Also swept `HOW_TWEGLE_WORKS.md` (hadn't been touched since the Achievement levels feature shipped — still described a single shared streak and had no mention of levels/points/leaderboard at all) and `FAQ.jsx` (the streak and badges Q&As were similarly out of date) to match current behavior. Verified in the browser: confirmed zero horizontal scroll and single-line rows at a 375px viewport, both locally and on production. Done 2026-08-09.
- [x] **Chess — single-player (vs. "the house") and live 2-player** — requested 2026-08-11, built 2026-08-13, the biggest game on the site by far: full legal-move generation (check/checkmate/stalemate, castling, en passant, promotion) isn't something worth hand-rolling the way Connect Four's "4 in a row" check was, so `chess.js` (MIT, the standard well-tested JS rules engine) was added as the legality/game-state authority on both frontend and backend, exactly as scoped in the original backlog item. Board is persisted as a FEN string (`ChessGame.fen`, plus `pgn` purely for the move-list UI) rather than a piece-array, since FEN is chess.js's native serialization and needs no custom parsing. **Backend**: `chessController.js`/`chessRoutes.js` mirror `connectFourController.js` exactly for create/join over REST; `chessSocket.js` (new `/chess` socket.io namespace) handles `joinRoom`/`makeMove`/`rematch`, re-deriving legality from the saved `fen` via chess.js on every move rather than trusting the client — same "server is sole authority" rule as every other live game. `'chess'` added to the `GAME_SLUGS` allowlist and a `shareChess` OG-preview route, both proactively (a past forgotten-allowlist bug silently broke Connect Four's view tracking). **Frontend**: a from-scratch `chessAI.js` (alpha-beta minimax, depth 2-3, material + standard piece-square-table evaluation, small mobility term, shuffled move order for natural-feeling ties) since chess.js is a rules engine, not an AI. One deliberate deviation from the site's usual per-game duplication: a shared presentational `ChessBoard.jsx` (8x8 grid, orientation flip for the second live player, legal-move dots, last-move highlight, check glow, Unicode piece glyphs styled with outline shadows for contrast in both themes) used by both `Chess.jsx` (single-player) and `ChessMultiplayer.jsx` (live) — an 8x8 board with a promotion picker and check/highlight state was judged too much shared complexity to safely duplicate twice the way Tic-Tac-Toe/Connect Four's much simpler boards are. Wired into `Game.jsx`/`registry.js`/`App.jsx`/`api.js` following the exact same pattern as Connect Four/Snake and Ladder. Verified end-to-end locally: single-player — legal-move dots exactly match chess.js's legal moves, AI responds within ~1s, promotion picker works; live 2-player — two real browser tabs, join broadcasts instantly to the waiting creator, a move (e4) synced live to the opponent's tab within under a second with the turn correctly flipping, board fits without horizontal overflow at a 375px mobile viewport, and square contrast (light/dark squares × white/black pieces) checked directly via computed styles in both light and dark mode.
- [x] **Enlarged Snake and Ladder's play area on mobile** — suggested directly as an idea to try, starting with one game before rolling out to the rest: the board/dice/player-chip components were sized identically at every screen width, which read as too small on a phone. Made `SnakeLadderBoard.jsx`/`DiceDisplay.jsx`/`PlayerChip.jsx` mobile-first *bigger* (the reverse of the usual pattern — unprefixed classes are the larger mobile size, `sm:` reverts to the original desktop size, so desktop is untouched) and had both `SnakeLadder.jsx` (single-player) and `SnakeLadderMultiplayer.jsx` (live) wrap the board in a `-mx-4 sm:mx-0` reclaim so it can actually use the extra width instead of staying squeezed by page padding meant for text. Since the board/dice/chip components are shared by both single- and 2-player, both got the upgrade from one change. Board cells grew from 28px to 36px (+28%) and the board itself now spans edge-to-edge on a 375px phone instead of sitting inset by 32px of padding. Verified in the browser: zero horizontal overflow at 375px, gameplay (rolling, turn-flipping) still works correctly, desktop cell size unchanged (still 36px, confirmed identical before/after).
- [x] **Rolled the mobile play-area enlargement out to every remaining game** — same day as Snake and Ladder's version, requested directly as "do this for all games." Applied the identical mobile-first-bigger pattern (unprefixed classes = bigger mobile size, `sm:` reverts to the original desktop size) to Tic-Tac-Toe, Connect Four, Chess, Rock Paper Scissors, Sudoku, 2048, Memory Match, Word Guess, Guess the Number, Simon Says, and Whack-a-Mole — boards/tiles/buttons/status text all sized up, single-player and (for Tic-Tac-Toe/Connect Four/Chess) their live 2-player pages both. Connect Four and Chess additionally reclaim their page's own `px-4` padding on mobile via a `-mx-4 sm:mx-0` wrapper (same trick as Snake and Ladder), since their frames were wide enough that the bigger discs/board needed the extra room to avoid overflowing. Verified every game individually in the browser at a 375px viewport — confirmed `document.body.scrollWidth` matched the viewport width (no horizontal overflow) on all 11 — and confirmed desktop (`sm`+) renders unchanged since every size bump has an `sm:` override reverting to the pre-existing value.
- [x] **Reordered homepage tabs: Games, Quizzes, Friendship Quiz first** — requested directly. Simple reorder of the `TABS` array in `Home.jsx`; every other tab's position afterward was left untouched — only the display order of the tab row changed. Verified in the browser: tab row reads Games → Quizzes → Friendship Quiz → Puzzles → Posts → Stories → Horoscope. **Follow-up same day**: made Games the actual *default landing tab* too (not just first in the row) — `activeTab`'s URL-param fallback and `setActiveTab`'s "omit from URL" default both switched from `'quizzes'` to `'games'`, plus the initial `content` state's placeholder `tab`. Verified a fresh visit to `/` with no `?tab=` query param renders the Games grid by default.
- [x] **Ludo — live 2-4 player game** — requested directly (2026-08-17), with an explicit scope decision up front: the full 2-4 player lobby experience, not a 2-player-only simplification (confirmed after clarifying that Ludo's real board supports up to 4). The biggest architectural step up from every prior live game: Tic-Tac-Toe/Connect Four/Chess/Snake and Ladder all hardcode exactly 2 roles as a fixed string enum baked into the schema, socket validation, and turn-flip logic — nothing in the codebase (including the async, fan-out `FriendshipQuiz` system) had an array-of-players/N-way-turn pattern to build on. `LudoGame.players` is an array of 2-4 `{role, name, tokens}` subdocuments (role assigned in join order from `['red','green','yellow','blue']`), with `currentTurnIndex` advanced via `(currentTurnIndex + 1) % players.length` instead of a binary flip. Standard rule set: 4 tokens/player, roll-a-6-to-leave-yard, capture-sends-back-to-yard (blocked on 8 standard safe squares), 3-sixes-in-a-row forfeits the extra roll, first to get all 4 tokens home wins **and ends the match** (2nd/3rd/4th aren't ranked, matching Snake and Ladder's "first to 100" simplicity). Two genuinely new capabilities no other game needed: a **lobby** (`status` stays `'waiting'` regardless of player count — only an explicit creator-only `startMatch` socket event, gated on `players.length >= 2`, flips it to `in_progress`, since a 3rd/4th friend may still be joining) and a **two-phase turn** (`rollDice` computes and persists `pendingRoll`/`movableTokenIndices`, a separate `moveToken` event with an explicit `tokenIndex` applies the chosen move — needed because one roll can have multiple legal token choices). `utils/ludo.js` (backend) does the board math with plain, well-known numbers (not copyrighted): a shared 52-square path, per-color offsets 13 apart (red 0/green 13/yellow 26/blue 39), 8 safe squares. Server is sole authority throughout (`crypto.randomInt` for rolls, every move re-validated server-side), same trust model as every prior live game. Deliberately out of scope: no single-player/AI mode, matching how Connect Four and Snake and Ladder each got single-player as a *separate later follow-up* rather than bundled in — Ludo's own backlog wording only ever asked for "a second real-time game." **Frontend board geometry note**: the "+"-shaped 15x15 board layout (`utils/ludoBoard.js`) is generated by an actual boundary-walk algorithm rather than hand-listed coordinates — a first hand-derivation attempt kept landing on a 56-cell ring instead of the standard 52 (a small extra notch of cells near one yard corner that doesn't affect gameplay, only the exact pixel shape), so the walk just stops at 52 cells once it's confirmed to align with each color's expected start square, with a runtime assertion that throws immediately if it ever doesn't — safer than trusting the geometry by inspection. Verified end-to-end locally with two real browser tabs (separate localStorage cleared per tab, matching how every prior live game's testing worked around shared-origin localStorage): lobby join broadcasting live to both tabs, creator-only "Start match" button gated correctly on player count, dice rolling and the auto-pass-on-no-legal-moves case (confirmed via direct socket calls logging the actual rolled value), tapping a movable token correctly leaving the yard and granting an extra roll on a 6 (confirmed via a direct API check of the persisted token position), and a mobile-viewport horizontal-overflow fix (the board's outer gradient padding was pushing 6px past a 375px viewport — shrunk the fixed board size to account for it). Dark mode spot-checked with no console errors.
- [x] **Streak-reminder toast + Activity Log filters** — requested directly (2026-08-17), both pulled forward from "Suggested." **Streak reminders**: scoped explicitly as an in-app toast ("like when a user achieves a badge/completes a level") rather than a real push notification, since that needs browser-push infrastructure (service worker subscriptions, a backend trigger) this site doesn't have — reuses the exact same mechanism as the existing badge/level-up toast. `dailyQuiz.js` gained `isStreakAtRisk(streak)` (true when the streak is real — count ≥2 — and was last extended yesterday, meaning today is the last chance before it resets); `badges.js` gained `checkStreakReminders()`, called once per page load from `App.jsx`'s `PublicSite`, which dispatches a `'twegle-streak-reminder'` event for each at-risk streak (quiz/puzzle independently) and self-limits to once per calendar day via a localStorage flag so it reads as a gentle nudge, not nagging. `BadgeToast.jsx` now also listens for that event and renders "🔥 N-day Quiz/Puzzle streak — play today to keep it going!" with a "▶️ Play now" link home. **Activity Log filters**: `activityLogController.js`'s `listActivity` now accepts `resourceType`/`action`/`admin` query params (validated against the model's own enums, plus an `ObjectId.isValid()` guard on `admin` so a malformed id 400s cleanly instead of a Mongoose CastError) and builds a Mongo filter from whichever are present. The admin-filter dropdown needed the existing `GET /api/admins` list, which was superadmin-only — split `adminRoutes.js`'s single router-wide role gate into per-route gates so listing (read-only, no `passwordHash`) is available to editor/analyst too, while create/delete stay superadmin-only. `Activity.jsx` gained 3 selects (admin/type/action) plus a "Clear filters" link, resetting to page 1 on any change. Verified end-to-end: backend — filtered by real admin id, by `resourceType=quiz`, combined filters, and confirmed an invalid `resourceType`/`admin` value is safely ignored rather than erroring; frontend — logged in as a real admin, confirmed the dropdown lists all 5 real admins and selecting "Quiz" live-narrows the table to quiz-only rows. Streak toast verified by seeding a fake at-risk streak in localStorage and confirming the exact toast text/link appeared on page load.
- [x] **Fixed Back button from a quiz Result page landing back in the finished quiz instead of home** — reported directly: clicking "← Back" on Result.jsx showed the last question of the quiz, and only a second Back actually reached the homepage. Root cause: `Quiz.jsx`'s `handleAnswer` never pushed a history entry per question (`questionIndex` is plain component state, not reflected in the URL), so the *quiz page itself* stayed the previous history entry the whole way through — the two `navigate(...)` calls into a result page (the normal result, and the "compare with a friend" result) both pushed a new entry on top of it. `BackButton.jsx`'s generic `navigate(-1)` therefore always landed back on the quiz first. Fixed by adding `replace: true` to both `navigate()` calls in `Quiz.jsx` — the quiz's history entry is replaced by the result page instead of stacked underneath it, so Back goes straight to wherever the player was before starting the quiz. Verified end-to-end in the browser: played a personality quiz to completion, confirmed Back from the result page landed directly on `/` in one click.
- [x] **Dice roll animation, confetti celebrations, extended sound design, and site-wide reactions** — 4 features requested together (2026-08-17), the top items pulled off the "Site Polish & Growth" list added earlier the same day. `DiceDisplay.jsx` gained real tumble/settle CSS animations on top of its existing face-cycling. A new dependency-free `utils/confetti.js` fires a canvas confetti burst (respecting `prefers-reduced-motion`) on badge unlock, level-up, and a trivia perfect score. `utils/sound.js` gained `roll`/`move` tones, extended from Ludo into the 4 other live-multiplayer game pages that had zero sound despite their single-player counterparts already having it via `Game.jsx`'s shared wiring. `Reaction.js` gained an optional `contentType` field (default `'post'`, no migration) and a new generic `/api/reactions/:contentType/:id` route reusing the existing controller; a generic `ContentReactions.jsx` component now appears on quiz results, stories, and post-game screens. Verified locally: a story reaction round-tripped through the new route, a game reaction persisted (confirmed via direct API check), the dice's computed animation name was confirmed correct after a live Ludo roll, and confetti's canvas/z-index/reduced-motion behavior was confirmed directly.
- [x] **Fixed Ludo's safe-square (star) positions being geometrically wrong for red/green/yellow** — reported directly ("star is wrong at every place... correct with one only which is just near to blue"). The board's `classifyCell` geometry naturally produces 56 connected ring cells, not the true 52-square Ludo path — the 4 excess cells are the diagonal corners of the center 3x3 block (pure connectivity waypoints where an arm turns 90°, not real squares). The old ring-order builder walked the ring and just stopped after 52 cells, arbitrarily chopping off whichever 4 were left at the end rather than these 4 specific corners — since the walk starts at blue's own square, that left blue correct while quietly shifting red/green/yellow's start squares (and every star, being fixed offsets from each start) off their true symmetric position. Fixed by walking all 56 cells for connectivity but only numbering the 52 that aren't one of the 4 corner cells — a purely geometric/rendering fix, `PATH_OFFSET`/`SAFE_SQUARES`/backend game logic untouched. Verified via a rotational-symmetry check (rotating index `i` 90° should equal index `i+13` — 0 mismatches after the fix, versus failing at nearly every index before) and by reading all 8 rendered stars' on-screen positions in a live match, confirming 4 exact mirror-pairs.
- [x] **Ludo yard tokens truly centered per quadrant + no dark-mode color shift on yard corners** — two more direct corrections against the same reference image (2026-08-17). Yard tokens moved from per-cell grid placement (which could only pin a token to one cell, not the true center of a 2x2-cell quadrant — that center sits on the line between cells) to a percentage-positioned absolute overlay, landing each token dead-center in its quadrant; a faint cross divider was added so the tray's 4 equal parts are visible, matching the reference. `YARD_BG`'s `dark:bg-*-900/80` overrides were removed so blue/red/green/yellow render identically in light and dark mode, matching how the home-stretch/token colors already worked. Verified locally: computed on-screen token centers matched the exact 25%/75% quadrant math for all 8 yard tokens, and a direct light/dark toggle confirmed all 4 corners compute to the byte-identical color.
- [x] **CSV export on Analytics tables + bulk publish/unpublish/delete on the 5 admin content lists** — both requested directly (2026-08-17), pulled forward from "Suggested." CSV export: a small "⬇ Export CSV" button beside each of the 8 tables on `/admin/analytics` (Quiz Plays, Post Engagement, and the 6 generic Engagement tables), entirely client-side via a new `admin/csvExport.js` — re-serializes the summary rows already loaded for display into a CSV `Blob` and clicks a throwaway `<a download>`, no backend endpoint needed since the data's already there. Disables itself while a table's data is null/still loading or genuinely empty. Bulk actions: QuizList/PostList/StoryList/PuzzleList/FriendshipQuizList all gained checkbox multi-select — a "Select all on this page" checkbox plus per-row checkboxes (both `canWrite`-gated, same as the existing per-row Delete button), backed by a shared `admin/useBulkSelection.js` hook (`{selected, toggle, toggleAll, clear}`, a plain `Set`) and a shared `admin/components/BulkActionsBar.jsx` toolbar (Publish/Unpublish/Delete/Clear), reused identically across all 5 pages rather than reimplementing selection bookkeeping 5 times. Publish/Unpublish loop sequentially over the selected ids calling each content type's *existing* single-item `PUT` endpoint with a status-only body (`{status: 'published'|'draft'}`) — every one of the 5 update controllers already treats every body field as optional (`if (x !== undefined) item.x = x`), so this is a safe partial update needing zero backend changes; Bulk Delete loops the existing single-delete endpoint behind the same `window.confirm(...)` the per-row Delete button already uses. This mirrors Bulk Import's own established pattern (`BulkImport.jsx` loops individual `createPost`/`createQuiz` calls client-side) rather than introducing a new bulk-request backend shape. Selection is deliberately page-local, cleared via an effect keyed to the same filter/page state each list already resets `page` on. Verified end-to-end in the browser: selected 2 quizzes on `/admin/quizzes`, bulk-Unpublished (both flipped to `draft`, bar disappeared, list reloaded), then bulk-Published the same 2 back (confirmed `published` again) to leave the database unchanged; confirmed all 8 Export CSV buttons render (7 enabled, the empty Horoscope table's correctly disabled) and that clicking one produces a real blob CSV download with the expected filename.
- [x] **"Your Twegle Wrapped" shareable recap image + homepage "🔥 X played today" counter + Quiz schema.org structured data** — all 3 requested together (2026-08-17), pulled straight off the "Site Polish & Growth" list. **Wrapped**: a new lightweight `utils/weeklyRecap.js` keeps a date-keyed local activity log (games/quizzes/puzzles/reactions/shares tallied per calendar day, pruned past 14 days) — needed because `badges.js`'s own stats are all-time cumulative and can't answer "what did you do *this week*." `badges.js`'s 5 existing `record*` functions each gained one `recordDailyActivity(kind)` call. `Badges.jsx` shows a "🎁 Your Twegle Wrapped" card (only once the week has any activity) summarizing the week's tallies with a "📸 Share my Wrapped" button that reuses the *existing* Canvas share-image pipeline (`shareOrDownloadImage` from `shareImage.js`, already built for quiz/post/compare results) rather than a new drawing routine. **Today counter**: a new `GET /api/stats/today` endpoint (`statsController.js`) reuses `digestController.js`'s existing `computeDigestForRange(since)` aggregate rather than writing a new query, returning today's total plays across every content type; `Home.jsx`'s hero stats row shows "🔥 X played today" once fetched, hidden when 0. **Schema.org**: `Quiz.jsx` now renders a `<script type="application/ld+json">` `Quiz` schema (name/description/url) using the exact JSON-LD pattern Home.jsx (WebSite) and Faq.jsx (FAQPage) already established — zero visible UI change, purely for richer Google search-result previews. Verified end-to-end locally: played a quiz to completion (confirmed no console errors and the JSON-LD tag parses correctly via direct DOM inspection), confirmed the homepage counter rendered "🔥 1 played today" from a real fetched value, and confirmed the Wrapped card appeared with the correct weekly tallies after that playthrough and its "Share my Wrapped" button completed without error.
- [x] **Fixed a broken daily streak displaying its old count instead of resetting to 0/Day-1** — reported directly ("if I missed any streak any day, next day it should automatically start with Day 0... currently it displays last Day, like Day 6 in puzzle, when I attempt it, it displays Day 1"). Root cause: `recordStreak()` in `dailyQuiz.js` only recalculates the count *at the moment a completion is recorded* — every display-only read (the homepage banners, Account page, badge/points checks) read the raw stored count directly, so a broken streak's stale number sat there unnoticed until the next play silently overwrote it. Fixed by decaying the count on every read instead of only on write: `readStreak()` now zeroes `count` (keeping `lastDate` for reference) unless it was last extended today or yesterday, via a new `decayIfBroken()` — a single change under the shared `getQuizStreak()`/`getPuzzleStreak()` getters that every screen already goes through. Confirmed there's no separate 7-day reset mechanic — the count climbs indefinitely until a day is actually missed; "7-Day Streak" is only a badge unlock threshold at 7, not a cycle. Verified locally: seeded a puzzle streak 3 days stale, confirmed the homepage banner's "Day N" badge correctly disappeared (reads as 0) instead of showing the stale number, while a same-day-completed streak still showed correctly.
- [x] **Fixed a real, major Ludo bug: rolling N sometimes visibly moved a token N+1 squares** — reported directly as "very major" ("suppose 4 occurs, the pawn walks 5 times, it should only 4 — this happening lots of time"). Root cause was the same 56-vs-52-cell board geometry the earlier star-position bug had already surfaced, but one layer deeper: the board's cross-shaped path genuinely has 56 physically-connected outer squares (confirmed by a fresh adjacency check — every one of the 56 has exactly 2 ring-neighbors, forming one unbroken loop with no dead ends), but the previous fix still only assigned a numbered index to 52 of them, treating the other 4 (the diagonal corners where an arm turns 90°) as unindexed "connectivity waypoints." That was the actual bug: a token's *stored* position always incremented by exactly the roll, but the *rendered* board silently skipped drawing an index at those 4 real squares, so a move crossing one of them visibly jumped 2 physical squares for a single index step. Fixed by indexing all 56 walked cells with no exclusions — every index-to-index step is now physically adjacent by construction, the only way to rule this class of bug out for good. Required renumbering the board's local-position scheme everywhere it's duplicated (`PATH_OFFSET` 14-apart on a 56-loop instead of 13-apart on 52, `SAFE_SQUARES` renumbered to the same 8 physical stars, home-entry moved from local 51 to 55, finish from 57 to 61) across both `utils/ludo.js` (backend) and all 3 frontend duplicates. Verified via an exhaustive offline simulation covering every possible roll (1-6) from every starting position (0-54) for every color — 0 mismatches between squares-crossed and the dice roll, versus a real, reproducible defect at exactly 4 points around the ring before the fix. Re-verified live in a vs-House match with no console errors.
- [x] **Fixed 3-/4-player Ludo matches being unable to start** — reported directly ("start a 3 player game, invite 2 friends, both joined, now I am unable to start the game — working fine with 2 player"). Root cause: `Game.jsx`'s Ludo challenge-form handler hardcoded the creator's own stored role as `'red'`, copying the same pattern every other live game correctly uses (Connect Four/Snake and Ladder/Chess/Tic-Tac-Toe really do assign a fixed first role regardless of player count) — but Ludo's `colorSequenceFor(maxPlayers)` only returns `['red','yellow']` for a 2-player match; 3-/4-player matches use the standard clockwise `['blue','red','green','yellow']`, making the true creator `'blue'`, not `'red'`. The hardcoded value only matched by coincidence in the 2-player case, exactly explaining "works fine with 2 player." With the wrong role stored, `isCreator` evaluated false for the real host, so the "▶️ Start match" button (creator-only) never appeared. Fixed by having `createGame`'s response include the server's actual assigned role (`role: players[0].role`, matching the shape `joinGame`'s response already returns for later joiners) and having `Game.jsx` store that real value instead of a hardcoded guess. Verified locally with a real 3-tab browser test: confirmed the creator's stored role now reads `'blue'`, confirmed the Start match button correctly appeared once both friends joined, and confirmed clicking it actually started the match with no errors; also confirmed via direct API check that a 4-player match's creator role matches the same way.
- [x] **Added a games count to the homepage hero stats row** — requested directly, since Games is now the site's top-priority tab but wasn't represented in the "🎯 X quizzes / 💬 X jokes & quotes / 🌐 English & हिंदी" row. "🎮 N games" now leads that row, reading `GAMES.length` from the already-imported static registry (no new fetch). Verified locally: renders as "🎮 13 games" first in the row.
- [x] **Fixed the hero heading collapsing into an unreadable one-word-per-line stack at in-between browser widths** — reported directly with screenshots (a resized desktop window, ~640-1000px, showed the title wrapped into 10+ tiny lines). Root cause: the title/stats hero row switched to a side-by-side two-column layout as soon as the `sm` breakpoint (640px) engaged, but the stats block was `shrink-0` (refuses to shrink) while the title had no width floor — at that in-between width there wasn't room for both, so the title got squeezed to a sliver and wrapped one word per line. Adding the games-count stat (a 5th item, from the entry above) widened the stats block and made this much more likely to hit. Fixed by moving the two-column split from `sm` to `lg` (1024px) — below that, the title takes a full row to itself and the stats wrap normally underneath, so there's no width to fight over until the screen is genuinely wide enough for both side by side. Verified locally at 700px (title renders as one normal-height line, no squeeze), 375px mobile (unaffected), and 1280px desktop (unaffected, unchanged side-by-side layout).
- [x] **Follow-up fix, same day: the hero stats row floated into the middle of the (legitimately 2-line) title instead of aligning with its top** — reported directly with another screenshot, this time at a wider desktop width (~1024-1366px) where the title has always wrapped to 2 lines. `items-center` was vertically centering the single-line stats block against that 2-line title, visually colliding it with the wrapped second line ("New") instead of reading as two clean columns. Changed to `items-start` so both columns top-align no matter how many lines either side wraps to. Verified at 1024px (title 2 lines, stats top exactly matches the "Where Fun Goes Viral" eyebrow line's top) and 1280px (title 1 line, same alignment holds).
- [x] **Third revision, same day: side-by-side hero restored (title left, stats right), fixed properly this time** — the stacked layout from the entry above wasn't actually what was wanted; the owner clarified the original side-by-side hero should come back, just with the stats side reorganized into 2 fixed rows instead of one wrapped strip. Both earlier squeeze/overlap bugs traced back to the stats side having an unpredictable width. Fixed at the source: each stats row is now `whitespace-nowrap` (2 short, stable-width lines instead of 5 wrappable items), so the title always gets the rest of the row with real room to spare, at any screen size. Verified at 700px (the earlier worst case — title 312px/2 lines, clean 16px gap to stats, no overlap), 1024px, 1366px, and mobile (stats still correctly hidden).
- [x] **[Attract] Public shareable profile page** (`twegle.in/u/<handle>`) — requested directly as the top pick from the Site Polish & Growth backlog. Opt-in only: an account picks a public `handle` (separate from the private login `username`, which stays exactly as private as before — see `EndUser.js`) and flips a "make my profile public" toggle in Account.jsx; nothing changes for any existing account until they do. The public page shows avatar, level (with progress to next), the 7 bonus badges (unlocked-only, so a profile doesn't broadcast what's *not* done), both streaks, and recently-completed quizzes (substituted for the backlog's "favorite quiz results," since no bookmarking feature exists yet — that's its own separate, unbuilt backlog item). Backend computes everything server-side from the synced `stats` blob (never trusts a client-submitted score), mirroring `leaderboardController.js`'s exact approach — a new `GET /api/users/:handle/public-profile` (public, no auth) plus a ported backend copy of the 7 bonus-badge unlock checks (`backend/src/utils/badges.js`, previously frontend/localStorage-only, kept in sync by hand the same way `levels.js` already is). Also added a share-preview route (`shareUserProfile`) for proper link previews. Verified locally: created two test accounts, set a handle + went public on the first, confirmed the profile renders correctly logged-out with the right avatar/level/badges/streaks, confirmed a made-up handle and a private profile both show the same "not found" state (can't tell "never existed" from "hidden"), confirmed trying to claim an already-taken handle shows a clean error instead of a crash, and confirmed dark mode + mobile (375px, no horizontal overflow) both work.
- [x] **Plain-English "what this does" comments across the whole codebase** — requested directly (2026-08-08), explicit green light given 2026-08-18 ("comment should be consistent across the application and easily understandable, so any developer can understand about the coding"). A comments-only pass over all 255 backend + frontend source files (~27,000 lines), adding beginner-friendly "what this does" explanations above every function/component/section, distinct from and additive to the codebase's existing "why" comments (all preserved byte-for-byte). Done in 11 batches (backend models/controllers/routes/middleware+utils+etc, frontend pages/components/games/utils/admin/root files) on a dedicated `add-plain-english-comments` branch, each batch independently verified before committing: a syntax/lint pass (`node --check` on the backend, `oxlint` on the frontend), manual diff review confirming every changed line was a pure comment addition (via a custom `git diff`-parsing script), and either a live backend boot + API smoke test or a `vite build` (bundle hash stayed byte-identical to before the pass — comments are stripped at compile time, strong proof of zero behavioral change) plus a browser check. Final full-project pass before merging to `main`: a syntax sweep across every backend file, a full frontend `oxlint` sweep (only pre-existing warnings/one pre-existing hooks error, confirmed unchanged from the `main` branch's own copy of `Game.jsx`), and a live smoke test covering the homepage, a quiz, the Tic-Tac-Toe game, and the admin login flow (401 on bad credentials rendered correctly) — all clean, zero functionality changed.
- [x] **Fixed a real cross-account data-leak bug: logging out didn't clear this browser's local badge/streak stats, so a different account logging in next could inherit them.** Found by direct questioning, not a bug report: the owner noticed Badges/My Achievements still showed real numbers right after logging out, and asked what would happen if a *different* account logged in next on the same browser. Root cause: `logout()` only ever removed the session token — the actual stats (`twegleStats`, streak keys, etc.) stayed in localStorage, so this browser kept looking like the just-logged-out account to every localStorage read, and `statsSync.js`'s merge-on-login (which takes whichever side of each stat is higher) would pull that leftover data into whichever account logged in next, on a shared device. Fixed by clearing local stats/streaks on logout instead of on login — new `clearLocalStats()` (badges.js) and `clearLocalStreaks()` (dailyQuiz.js), both called from `UserAuthContext.jsx`'s `logout()`. Deliberately *not* cleared on login, since that would have discarded legitimate guest progress earned between logging out and logging back in — clearing on logout means guest activity after that point starts from a clean 0 (no leftover Account A data to leak) while still correctly merging into whichever account logs in next, same as before. Verified locally: seeded one test account with 23 points, logged out, confirmed Badges/My Achievements immediately showed "Fresh Face, 0 points" with no reload needed, then logged into a completely different second account and confirmed it also showed 0 — no trace of the first account's 23 points.

---

*Last updated: 2026-08-18 (latest) — **Fixed 3-/4-player Ludo matches being unable to start**, reported directly ("invite 2 friends, both joined, unable to start the game — working fine with 2 player"). Root cause: `Game.jsx`'s Ludo challenge-form handler hardcoded the creator's own stored role as `'red'`, mirroring the pattern every other live game correctly uses (they all really do assign a fixed first role regardless of player count) — but Ludo's `colorSequenceFor(maxPlayers)` only returns `['red','yellow']` for a 2-player match; 3-/4-player matches use the standard clockwise `['blue','red','green','yellow']`, making the true creator `'blue'`. The hardcoded value only matched by coincidence at 2 players, exactly explaining why 2-player worked and 3/4 silently didn't — with the wrong role stored, the app didn't recognize the real host as the creator, so the "Start match" button (creator-only) never appeared. Fixed by having `createGame`'s response include the server's actual assigned role and having the frontend store that instead of a hardcoded guess. Verified with a real 3-tab browser test: creator's role correctly reads `'blue'`, Start match button appears once both friends join, and clicking it starts the match with no errors; also confirmed via direct API check for a 4-player match. See "Completed till date" for the full writeup. Earlier, 2026-08-17: **Fixed a real, major Ludo bug: rolling N sometimes visibly moved a token N+1 squares**, reported directly as "very major" and happening "lots of time." Root cause was one layer deeper than the earlier star-position fix: the board's path genuinely has 56 physically-connected squares (confirmed via a fresh adjacency check — every one of the 56 has exactly 2 ring-neighbors, one unbroken loop, no dead ends), but the previous fix still only numbered 52 of them, treating the other 4 (the diagonal corners where an arm turns) as unindexed waypoints. That was the real bug: the stored position always incremented by exactly the roll, but the rendered board silently skipped an index at those 4 real squares, so a move crossing one visibly jumped 2 squares for a 1-index step. Fixed by indexing all 56 cells with no exclusions, renumbering `PATH_OFFSET`/`SAFE_SQUARES`/home-entry/finish accordingly across the backend and all 3 frontend duplicates. Verified via an exhaustive simulation of every roll from every position for every color — 0 mismatches, versus a real defect at exactly 4 ring points before. See "Completed till date" for the full writeup. Earlier the same day: **Fixed a real streak bug: a broken daily streak kept displaying its old count instead of resetting to 0, only correcting itself after the next attempt (which then jumped straight to "Day 1").** Reported directly with a precise symptom (Puzzle streak showing "Day 6" after a miss, then "Day 1" once replayed). Root cause: `recordStreak()` in `dailyQuiz.js` only recalculated the count when a completion was actually recorded — every read-only display (homepage banners, Account page, badge/points checks) read the raw stored count straight from localStorage, so a broken streak's stale number lingered until the next play quietly overwrote it. Fixed by decaying the count on every *read* instead of only on write, via a new `decayIfBroken()` inside the shared `getQuizStreak()`/`getPuzzleStreak()` getters — zeroes the count (keeps `lastDate` for reference) unless it was last extended today or yesterday, so every screen reflects the same truth with no other file needing changes. Confirmed there's no separate 7-day reset — the count climbs indefinitely until an actual miss; "7-Day Streak" is only a badge threshold at 7, not a cycle. Verified locally by seeding a 3-day-stale streak and confirming the banner's "Day N" badge correctly vanished (0) instead of showing the stale number. See "Completed till date" for the full writeup. Earlier the same day: **Shipped 3 requested features together: "Your Twegle Wrapped" shareable recap image, a homepage "🔥 X played today" live counter, and Quiz schema.org structured data.** All 3 pulled straight off the "Site Polish & Growth" list. Wrapped reuses the existing Canvas share-image pipeline (`shareOrDownloadImage`) and a new lightweight date-keyed local activity log (`utils/weeklyRecap.js`) rather than a new stats system, since `badges.js`'s own stats are all-time cumulative and can't answer "what did you do this week." The today-counter reuses `digestController.js`'s existing `computeDigestForRange(since)` aggregate via a new `GET /api/stats/today` endpoint rather than a new query. The Quiz schema.org tag follows the exact JSON-LD pattern Home.jsx (WebSite) and Faq.jsx (FAQPage) already use — zero visible UI change. Verified end-to-end locally: played a quiz to completion (no console errors, JSON-LD parses correctly), confirmed the homepage counter rendered a real fetched value, and confirmed the Wrapped card appeared with correct weekly tallies and its share button completed without error. See "Completed till date" for the full writeup. Earlier the same day: **Shipped 4 requested features together: a better dice roll animation, confetti celebrations, sound extended to the live multiplayer games, and site-wide reactions.** Pulled the top items straight off the "Site Polish & Growth" list added earlier the same day. Dice: `DiceDisplay.jsx` gained real tumble/settle CSS animations layered on its existing face-cycling. Confetti: a new dependency-free `utils/confetti.js` (`fireConfetti()`, a one-shot canvas burst respecting `prefers-reduced-motion`) fires on badge unlock, level-up, and a trivia perfect score. Sound: `utils/sound.js` gained `roll`/`move` tones, extended from Ludo (which already had them) into the 4 other live-multiplayer pages (TicTacToe/Connect Four/Snake and Ladder/Chess) that had zero sound despite their single-player versions already having click/win/lose. Reactions: `Reaction.js` gained an optional `contentType` field (default `'post'`, no migration needed) and a new generic `/api/reactions/:contentType/:id` route pair reusing the existing controller; a generic `ContentReactions.jsx` component now appears on quiz results, stories, and post-game screens, extending the emoji-reaction pattern Posts already had. Verified locally: a story reaction round-tripped through the new route, a game reaction persisted and was confirmed via a direct API check, the dice's computed animation name was confirmed correct after a live Ludo roll, and confetti's canvas/z-index/reduced-motion behavior was confirmed directly (a trivia-quiz playthrough wasn't possible — none are currently seeded — so the perfect-score trigger was verified by code review of the call site rather than an end-to-end play). See "Completed till date" for the full writeup. Earlier the same day: **Fixed a real Ludo geometry bug: the safe-square (star) markers were in the wrong position for 3 of the 4 colors**, reported directly with a precise symptom ("correct with one only which is just near to blue"). Root cause: the board's cell-classification geometry naturally produces 56 connected ring cells, not the 52 a real Ludo path needs — the 4 excess cells are the diagonal corners of the center 3x3 block, pure connectivity waypoints where one arm turns 90° into the next, not real squares. The old ring-order builder just walked the ring and stopped after collecting 52 cells, chopping off whichever 4 happened to be left over rather than these specific 4 — since the walk starts at blue's own square, that left blue correct while silently shifting red/green/yellow (and every star, since stars are fixed offsets from each color's start) off their true symmetric spot. Fixed by walking through all 56 cells for connectivity but only numbering the 52 that aren't one of those 4 corner cells. Purely a rendering fix — `PATH_OFFSET`/`SAFE_SQUARES`/backend game logic untouched. Verified via a rotational-symmetry check script (0 mismatches across all 52 indices, versus failing at nearly every index before) and by reading all 8 rendered stars' on-screen positions in a live match, confirming 4 exact mirror-pairs. See "Completed till date" for the full writeup. Earlier the same day: **Added a new "Site Polish & Growth" backlog section**, at the owner's direct request for ideas on making the site look more attractive and attracting/retaining more visitors — 12 items, ordered highest-priority-first, each tagged [Look]/[Attract]/[Retain] for which goal it primarily serves. Placed ahead of Phase 2 in the doc (a separate bucket, not a Phase 2 replacement — Phase 2 is specifically what end-user accounts unlock, this is broader visual/growth/engagement polish, most of which doesn't need accounts to start). Top 3 by priority: a public shareable profile page (turns the existing badge/level system into something a friend can actually see), a "Your Twegle Wrapped" shareable weekly recap image (reusing the existing Canvas share-image pipeline), and referral rewards (turns the existing account system into an actual growth loop). Purely a planning-doc addition — no code changed, nothing to verify. Earlier the same day: **Two more direct Ludo corrections against the same reference image**: yard tokens now sit exactly centered within their quadrant of the white tray (previously pinned to a single grid cell, which can't represent a 2x2-cell quadrant's true center — fixed with a percentage-positioned overlay, same technique the center pinwheel already used, plus a faint cross divider making the 4 parts visible) and the 4 yard corner colors (blue/red/green/yellow) no longer shift to a darker tone in dark mode (removed the `dark:` color overrides on `YARD_BG` so they render byte-identical in both themes). Verified locally via exact on-screen token-center math and a direct light/dark computed-style comparison of all 4 corners. See "Completed till date" for the full writeup. Earlier the same day: **Reorganized the backlog: Phase 1 marked complete and removed as a section, its 4 still-pending items folded into Phase 2**, per the owner's direct confirmation that there's nothing left to pull forward from Phase 1. "Find the Difference" (user-side) and the automated test suite/Cloudinary uploads/plain-English comments (admin-side) moved into Phase 2's User-side/Admin-side lists respectively; the owner asked that each column be ordered highest-priority-first, so every item in both lists was re-ranked with a one-line rationale noting why it sits where it does (e.g. Bookmarks/Favorites now leads User-side as the smallest, least-blocked win; the automated test suite now leads Admin-side as the safety net protecting everything else; Cloudinary uploads sits below the codebase-comments pass since it's blocked on an owner action rather than something buildable end-to-end right now). No code changed, no functionality removed — this was a planning-doc reorganization only, so nothing needed local/production verification. Earlier the same day: **Shipped CSV export on Analytics tables and bulk publish/unpublish/delete on the 5 admin content lists**, both requested directly, pulled forward from "Suggested." CSV export is a small "⬇ Export CSV" button beside each of the 8 Analytics tables, entirely client-side (re-serializes the already-loaded summary rows into a CSV Blob, no backend endpoint) via a new `admin/csvExport.js`. Bulk actions add checkbox multi-select to QuizList/PostList/StoryList/PuzzleList/FriendshipQuizList — a shared `useBulkSelection` hook plus `BulkActionsBar.jsx` component reused identically across all 5 pages; Publish/Unpublish loop the existing single-item `PUT` endpoints with a status-only body (every update controller already treats fields as optional partial updates) and Delete loops the existing single-delete endpoint, mirroring Bulk Import's own client-side-loop pattern rather than adding new bulk backend routes. Verified locally: bulk-unpublished then bulk-republished 2 quizzes on `/admin/quizzes` (confirmed via rendered status text both times), and confirmed all 8 Export CSV buttons render correctly (7 enabled, the empty Horoscope table's disabled) with a real click producing a genuine blob CSV download. See "Completed till date" for the full writeup. Earlier the same day: **Redesigned Ludo's board a second time against a new reference image, requested directly ("delete previous UI, create new fresh UI")**: the new reference used a genuinely different standard color layout than the one matched earlier the same day — blue top-left, red top-right, green bottom-right, yellow bottom-left (a red↔blue and green↔yellow swap) — plus a different visual style (thin grid lines on every cell including the yard corners, not just the path). Swapped `PATH_OFFSET`/`YARD_BOUNDS`/home-stretch arms together again (same coordinated-swap approach as the earlier fix), reordered the pinwheel's conic-gradient stops to match, and flipped `LudoBoard.jsx`'s border logic so every cell gets a visible grid line except the yard's white tray (which stays a clean, borderless rounded panel). Verified via the board's own runtime geometry assertion (didn't throw) plus direct computed-style checks confirming all 4 corners' hues and the pinwheel's 4 gradient stops now match the new reference exactly, and a full gameplay pass (roll, auto-pass, tap-to-move) still works correctly with the new offsets. Earlier the same day: **Shipped a streak-reminder toast and Activity Log filters**, both requested directly, pulled forward from "Suggested." The streak reminder is scoped as an in-app toast (reusing the existing badge/level-up toast mechanism) rather than a real push notification, since push needs browser infrastructure this site doesn't have yet — it checks once per page load, self-limits to once/day, and fires independently for an at-risk Quiz or Puzzle streak. Activity Log gained admin/type/action filter dropdowns backed by real backend query-param filtering (with input validation so a bad value 400s or is safely ignored rather than erroring), and the admin-list endpoint powering the admin dropdown was opened up from superadmin-only to also cover editor/analyst (read-only). See "Completed till date" for the full writeup. Verified end-to-end: backend filter combinations via curl, frontend dropdown interaction live-narrowing the table, and the streak toast's exact text/link confirmed by seeding a fake at-risk streak. Earlier the same day: **Closed the remaining gap to the reference Ludo board, explicitly asked for "no compromise"**: the center square was still a diagonal gradient blur instead of the reference's true 4-triangle pinwheel (each color's triangle pointed toward its own approach direction — top=blue, right=yellow, bottom=green, left=red), and the home-stretch arms were a pale tint instead of the same bold color as the yards/tokens. Fixed with a `conic-gradient` overlay (4 exact 90°-wide color wedges, positioned over the board's true center) replacing the old gradient cell, and bumped the home-stretch color to match the yard/token saturation exactly (no dark-mode variant now, matching the reference's own theme-independent colors). Verified via computed styles: the overlay's gradient stops and position are exactly right, and home-stretch cells now compute to the same high-saturation color values as the yards. Earlier the same day: **Matched Ludo's board to a real reference screenshot, requested directly**: the color-to-corner layout had green/blue swapped from the standard (should be red top-left, blue top-right, yellow bottom-right, green bottom-left) — fixed by swapping green/blue's `PATH_OFFSET`, yard bounds, and home-stretch arms together in both `ludo.js` (backend) and `ludoBoard.js` (frontend), verified via the board's existing runtime geometry assertion plus direct position checks (`tokenCoords('blue', 0, 0)` now resolves next to the top-right yard). Also rounded the yard tray's outer corners and enlarged tokens while still in the yard, both matching the reference's cleaner look. Verified locally: corner colors now read red/blue/yellow/green clockwise matching the reference exactly (confirmed via computed hue values), no geometry assertion errors, and a fresh vs-House match still correctly assigns red/yellow. Earlier the same day: **Fixed 2 more Ludo issues reported directly**: (1) a 2-player match (including vs-House) was seating opponents in adjacent corners (red/green) — a real 2-handed Ludo game seats players diagonally opposite, so `ludoController.js` now assigns `red`/`yellow` specifically for 2-player matches (`colorSequenceFor()`), keeping the standard clockwise red→green→yellow→blue order for 3-/4-player matches; (2) every one of a yard's 36 cells had its own grid border, making each corner look like a busy checkerboard instead of one clean colored block with a single white panel holding the 4 tokens (the real Ludo look) — `LudoBoard.jsx` now drops the per-cell border entirely on yard cells and fills a 4x4 inner region white, so the yard reads as one solid block with a distinct tray, not a grid. Verified locally: a vs-House match now shows red facing yellow (not green), and the yard corners render as clean solid squares with a single 4-token white tray, no grid lines. Earlier the same day: **Fixed 3 issues in "Play vs House" reported directly with a screenshot**: (1) it required typing a name and was buried inside the friend-challenge form rather than being the default view — restructured so it's now the default landing view for Ludo (one click, no name field, matching Tic-Tac-Toe/Connect Four's zero-friction pattern), with "Challenge friends" demoted to a secondary link and the friend form's own max-players default changed from 4 to 2; (2) Ludo's registry entry had no `'single'` mode, so it never showed under the homepage's "Single Player" filter — added; (3) the board's green/blue/yellow yard corners read as gray/purple/orange in a real phone screenshot — the colors were technically correct and distinct (verified via computed styles) but too washed-out (`bg-*-200/70`) to tell apart at a glance, bumped to a solid `bg-*-300` matching the home-stretch stripes' own saturation. Verified locally: clicking Ludo's card lands straight on an in-progress "You vs. House" board with zero prompts, and the four yard colors are now clearly distinct in both themes. Earlier the same day: **Added "Play vs House" for Ludo's 2-player mode**, requested directly right after live Ludo shipped. Rather than a separate single-player component (every other game's pattern), this reuses the exact same live-match infrastructure: the challenge form's new "🏠 Play vs House instead" button creates a match with the second seat pre-filled by an AI named "House" and starts it immediately (no lobby to wait through), and a new `vsHouse` field on `LudoGame` marks it. The house has no server process of its own — the human's own connected browser drives its rollDice/moveToken events on a short delay (`ludoHouseAI.js`'s `pickHouseMove`, a plain heuristic: capture if possible, else leave the yard, else advance the furthest token), which is safe under the app's existing no-accounts trust model since roles are plain strings with no per-connection auth. Verified locally via direct API polling: a vs-House match skips straight to the board, and the house correctly auto-plays its full turn (including its extra roll on a 6) before handing control back. Earlier the same day: **Built Ludo, live 2-4 player** — the biggest architectural step up from every prior live game (Tic-Tac-Toe/Connect Four/Chess/Snake and Ladder all hardcode exactly 2 roles; Ludo needed an array-of-2-4-players/N-way-turn pattern built from scratch), with a full pre-game lobby (creator-gated "Start match" once ≥2 have joined) and a two-phase roll-then-choose-token turn, both genuinely new capabilities no other game on the site needed. Standard rules: 4 tokens/player, roll-a-6-to-leave-yard, capture-sends-back-to-yard (blocked on 8 safe squares), 3-sixes forfeits, first to get all 4 home wins and ends the match. No single-player/AI mode, matching how Connect Four and Snake and Ladder each got single-player as a separate later follow-up. See "Completed till date" for the full writeup, including a note on how the frontend's "+"-shaped board layout is generated by a self-verifying boundary-walk algorithm rather than hand-listed coordinates, after a first hand-derivation attempt kept landing on 4 extra cells. Verified end-to-end locally with two real browser tabs: lobby join/start, dice rolling (including the auto-pass-on-no-legal-moves case), moving a token out of the yard with the correct extra-roll-on-six, and a mobile horizontal-overflow fix. Earlier, 2026-08-13: **Fixed the quiz Result page's Back button landing back in the finished quiz instead of home** — `Quiz.jsx`'s navigation into a result page now uses `replace: true` since the quiz never had its own per-question history entry, so Back was always stepping into the quiz first before reaching home; now it's one click straight to wherever the player came from. Verified: Games-as-default-tab (reported as "still showing Quizzes") was checked directly on production and confirmed already live — likely a stale cache from testing right after the earlier deploy. Earlier the same day: **Made Games the default homepage landing tab**, a direct follow-up right after reordering the tab row — a fresh visit to `/` with no `?tab=` param now lands on Games instead of Quizzes. Earlier the same day: **Rolled the mobile play-area enlargement out to every game, and reordered the homepage tabs** — Tic-Tac-Toe/Connect Four/Chess/Rock Paper Scissors/Sudoku/2048/Memory Match/Word Guess/Guess the Number/Simon Says/Whack-a-Mole all got the same mobile-first-bigger sizing pass Snake and Ladder got earlier the same day (see below), and the homepage tab row now reads Games → Quizzes → Friendship Quiz → the rest, both requested directly. See "Completed till date" for the full writeup. Earlier the same day: **Enlarged Snake and Ladder's play area on mobile** — the board/dice/player-chip components now size mobile-first bigger (36px cells, up from 28px, spanning edge-to-edge on a phone) with desktop left untouched. Earlier the same day: **Pruned two low-value items from "Suggested"**: "Async multiplayer trivia" (overlaps heavily with the already-shipped "Compare with a friend," which covers the same core hook) and "A/B testing on quiz questions or ad placements" (needs real traffic volume to be meaningful and no one had actually asked for it — generic growth-playbook filler rather than a concrete Twegle need). Everything else in "Suggested" was reviewed and kept — each remaining item has a clear stated reason and defined scope. Earlier the same day: **Built Chess, single-player and live 2-player**, the last big item in "Suggested" and the biggest game on the site — see "Completed till date" for the full writeup (chess.js for legal-move authority, a from-scratch alpha-beta AI, a shared `ChessBoard.jsx` component used by both modes). Earlier the same day: **Second refinement round on Surprise Me / Continue where you left off after mobile feedback**: fixed a visible horizontal scrollbar on the "Jump back in" pill row (added the existing `.no-scrollbar` utility class from `index.css`) and hid the Surprise Me header icon behind a `SHOW_SURPRISE_ME = false` flag in `Header.jsx` per direct instruction — the icon was still crowding the mobile search bar and the 🎲 emoji wasn't a clear enough signal on its own, but the component/logic stay fully intact for a later re-enable. Verified locally: `.no-scrollbar`'s `scrollbar-width: none` confirmed applied via computed styles with real overflow (584px content in a 375px mobile viewport) and no Surprise Me icon rendered in the header a11y tree. Earlier, 2026-08-10: **Converted Tic-Tac-Toe's two-player mode to live (socket.io) and added a Games "Single Player / 2 Player" filter**, both requested directly right after Connect Four shipped. Tic-Tac-Toe's poll-based two-player match now uses the exact same socket.io pattern Connect Four introduced (own `/tic-tac-toe` namespace, REST move route removed in favor of a socket event); `registry.js` gained a `players` field per game, powering a new homepage filter chip row. Verified end-to-end locally (live join/move sync for Tic-Tac-Toe, correct filter behavior). Earlier the same day: **Built live two-player Connect Four, Twegle's first real-time feature**, after clarifying "2-player game" meant genuinely live play (both screens updating instantly) rather than the existing async Tic-Tac-Toe pattern. Picked Connect Four over Ludo as the first build (simpler ruleset, proves the real-time infra without also needing dice/tokens/capturing). Added `socket.io`/`socket.io-client` (both free); hybrid design keeps MongoDB as the source of truth for the match while socket.io only broadcasts live moves, so a dropped connection never loses the game, just the "instant" delivery. Caught and fixed a real bug during testing — joining happens over REST, so the creator's tab wasn't finding out about a joined friend until manual reload; fixed by having the join endpoint also broadcast over the socket. Verified end-to-end locally (live join, live moves, turn-flips, win detection, share buttons, reconnect resync). Earlier the same day: **Expanded the avatar picker from 14 to 28 presets** (7 "cute," 7 "gamer," requested directly), kept in sync across `Account.jsx` and `backend/src/utils/validators.js`. Also **picked Connect Four as the recommended first real-time two-player game** — simple, familiar rules, proves the WebSocket/room infrastructure without also needing a complex ruleset, and doesn't create a second confusing "vs a friend" mode on top of the existing async Tic-Tac-Toe. Earlier, 2026-08-09: **Fixed the real bug: puzzle emoji spoiling the answer.** The previous "fixed a mismatched picture puzzle" entry below turned out not to be what was reported — the actual issue was the small emoji shown next to *every* puzzle, visible before "Reveal Answer" is tapped; 11 of 16 puzzles used an emoji depicting the literal answer object (🤧 for "a cold," 🎹 for "a piano," 👣 for "footsteps," and 8 more of the same pattern), handing the answer away for free. Swapped all 11 to a neutral or question-safe emoji, and added a standing comment in `seedPuzzles.js` warning against this going forward. Fixed live via the admin API (11 direct `PUT` calls) and verified by re-fetching every puzzle and confirming the emoji now matches. Earlier the same day: **Fixed a mismatched picture puzzle** (its question described a "close-up rebus" image, but the actual seeded photo was a full landscape shot with nothing left to guess — rewritten to actually match the image, fixed directly on the live site via the admin panel since the seed script upserts by question text). Also **corrected the "two-player games" backlog item** — clarified as real live-play games (e.g. Ludo, both players' screens updating in real time) rather than the existing async link-and-poll pattern; genuinely needs new real-time infrastructure (WebSockets, rooms, live state sync), not just extending Tic-Tac-Toe's approach — captured as a real but unscoped direction, later expanding toward multiplayer. Earlier the same day: **Shared images now carry a link back to the site** (baked into the image itself, since native-share captions are often dropped by the receiving app for file shares) and **stale "Share Twegle" text updated** to mention Puzzles and Games. Earlier the same day: **Footer mobile layout fixes**: the "Share Twegle" buttons now flush left instead of centered (a new `align` prop on `ShareButtons`, defaulting to centered everywhere else), and the Explore/Company footer columns now sit side by side on mobile instead of stacking, closing up a lot of wasted horizontal space and page length. Earlier the same day: **Mobile fix for the "How to earn points" table**: a merged streak row and "Play a game"'s cap note didn't fit next to their labels on a narrow phone, forcing a horizontal scrollbar; fixed by shortening that one note and merging the two streak rows into one, after a first attempt (stacking every row) was reported as not looking as good as the original layout. Also swept `HOW_TWEGLE_WORKS.md` and the FAQ page, both stale since the Achievement levels feature shipped. Earlier the same day: **Split the shared daily streak into independent Quiz and Puzzle streaks** (reported as confusing when only one activity was actually keeping the counter alive) and **brightened the "Add to Home Screen" footer button** (reported as too light to notice). Earlier still: **Achievement levels points rebalance**: capped game-play and reaction points (10 plays/game, 50 reactions total) to close a grinding exploit, raised point values and level thresholds, and added a "How to earn points" table to the Badges page. See "Completed till date" for the full writeup. Earlier, 2026-08-05: **Search Console set up**, which turned up and fixed two real SEO bugs: `/sitemap.xml` was being served as the React app's HTML shell instead of real sitemap XML (Vercel's catch-all rewrite was intercepting it before the backend could handle it — fixed with a proxy rewrite ordered first), and `robots.txt` still pointed at a `yourdomain.com` placeholder instead of the real domain. Verified the fix directly, then resubmitted the sitemap — came back "Success, 140 pages discovered." Earlier the same day, **Google Analytics (GA4) wired in**: walked through the GA4 property setup together, then added the resulting Measurement ID (`G-KYTM35VQCT`) to `frontend/index.html`, loading only on the public site (explicitly skipped on `/admin/*`, same exclusion pattern as the dark-mode script, so CMS usage doesn't pollute visitor analytics). Verified `gtag.js`/`dataLayer` load on public pages and are absent on `/admin/login`. Split Search Console out into its own still-pending owner task, since it covers something GA4 doesn't (how people find the site via Google search). Fixed the admin login card being off-center with an unnecessary scrollbar on mobile — root cause was `min-h-screen` (100vh) sizing against the largest possible mobile viewport rather than what's actually visible with the address bar showing; switched to `min-h-dvh`. Earlier, 2026-08-04: a run of admin-panel work, all pushed to production. **Dashboard**: a new landing page after login (also reachable any time via the sidebar logo), with a Day/Week/Month/Year/All range picker controlling both a "plays/views/new content" summary card and 4 Top Content leaderboards (Quizzes, Friendship Quizzes, Games, Posts — previously quizzes-only), plus published-content counts, an unread-feedback callout, and recent activity. One new endpoint (`GET /api/admin/dashboard`) aggregates all of it server-side in one call. **Mobile fixes**: admin login/search/filter fields were invisible on phones with system dark mode on (the site's dark-mode script didn't know to skip `/admin` routes — fixed at the source, in 3 layers, across every admin input); the sidebar nav was always fully expanded on mobile, now hidden behind a hamburger toggle; Analytics/Activity tables were clipping instead of scrolling on narrow screens — Activity specifically (5 columns, the worst offender) now renders as stacked cards below the `sm` breakpoint instead of relying on an invisible horizontal scrollbar. **Public site**: Friendship Quiz results gained a "🏆 Challenge Your Friend" button that re-shares the *original* invite link (not just the score) so more friends can guess about the same subject; every quiz/post/story result page now nudges toward one other content type (a game, plus a post or quiz) via a new `CrossPromo` component, aimed at pages-per-visitor; fixed the Back button not scrolling to top after following one of those links (the browser's native scroll restoration was racing the app's own). Verified every change end-to-end in browser before pushing. Earlier the same day, shipped **Cross-content "next thing" recommendations**: a new `CrossPromo.jsx` pill strip on Result/Post/Story pages pointing at one other content type (a game, plus a post or quiz) below the existing same-type "You might also like" suggestions, aimed at pages-per-visitor. Verified end-to-end in browser on all three pages. Earlier the same day, considered 3 related ideas and skipped them: an admin "Top Content" table sorted by shares (duplicates data Analytics already shows across its 5 tables) and a custom pages-per-visitor metric (better answered by the already-planned Google Analytics setup than a custom build); also declined moving Friendship Quiz to the top nav position (Quizzes has far more content and drives most organic search traffic — Friendship Quiz already sits second). Restructured "Suggested, not started" into "Suggested (Phase 1)" and "Suggested (Phase 2)" per the owner's request, after confirming against `DEVELOPMENT_PLAN.md` §4 that Phase 1 (MVP) is fully complete — everything remaining is polish/growth on a finished foundation, not a gap. Added 2 new items: Google Analytics/Search Console setup (Phase 1, a business task not a code task, now that `twegle.in` is actually live) and migrating localStorage stats (streak/badges/leaderboard identity) to a real account on signup (Phase 2, a direct follow-up once end-user accounts exist). Earlier the same day: shipped **Shareable image for Compare results** (a side-by-side Canvas-drawn story image, same pipeline as quiz/post share images, new "📸 Share as Image" button on `CompareResult.jsx`) and made **My Badges more discoverable** (added a 🏆 header icon, not just a footer link). "Suggested, not started" is down to 4 lower-priority items. Earlier the same day: shipped **Achievement badges**: 7 localStorage-only badges (Beat the House 3x, Tried Every Game, Quiz Explorer, Perfect Score, 7-Day Streak, Reaction Fan, Super Sharer), a `/badges` page, and a bottom-corner unlock toast, hooked into existing action points rather than adding new ones. Verified end-to-end by actually earning a badge in browser and confirming it flipped to unlocked. Only "Shareable image for Compare results" and 4 lower-priority items remain in "Suggested, not started." Earlier the same day: shipped the last two items from "Suggested, not started" (at the time): a **"This week" digest** at the top of admin Analytics (3-line summary, one new endpoint counting across 9 models over the last 7 days) and **Bulk Import** (`/admin/bulk-import` — paste-a-list for posts, paste-JSON for quizzes, both reusing existing create endpoints looped from the frontend). Verified end-to-end in browser, including confirming bulk-imported items actually landed in the database. "Suggested, not started" now down to 5 items — everything admin-productivity-shaped from that list has shipped. Earlier the same day: shipped **Reactions inline on homepage post tiles** (😂🔥😭👍 directly on Jokes/Funny Lines/Quotes/Motivational/Memes tiles, no click-through needed), pulled forward from "Suggested, not started" per direct feedback. New batch endpoint (`GET /api/posts/reactions?ids=...`) avoids one request per tile; verified live in browser on both meme and text-post tile styles. Earlier the same day: shipped two admin-side items: **Content freshness flags** (a "⚠️ Needs a refresh" badge on Quizzes/Posts admin rows, entirely client-side from existing Analytics data) and **Mobile-friendly Quick Add** (`/admin/quick-add`, a one-textarea "type it, publish it" flow reusing the existing `createPost` endpoint). Earlier the same day: shipped **Daily streak / "Quiz of the Day"**, an entirely client-side Wordle-style habit hook (no backend changes): a date-derived deterministic pick (same pattern as Horoscope) plus a localStorage streak counter, verified end-to-end in browser. Earlier still: shipped **Reaction emojis on posts** and **Game leaderboards**, picked from "Suggested, not started" as the two features that reused the most existing backend infrastructure. Verified both end-to-end locally (reaction click updates counts live; leaderboard submit/sort/scoping all confirmed via direct API tests) before pushing. Previously, 2026-08-02: **deployed the site live** at `twegle.in`: MongoDB Atlas (database), Render (backend API), Vercel (frontend), all on free tiers, domain connected via DNS records at Namecheap. Seeded the production database with all existing content and created 3 admin accounts (superadmin/editor/analyst). All credentials/connection strings live in `credentials.txt` (gitignored, local-only — never in these docs). Marked "Hosting accounts" and "Domain name" done in "Your Tasks," and noted Google AdSense is now unblocked. Previously, 2026-07-31: built the **Horoscope feature** requested from "Suggested, not started": a "🔮 Horoscope" homepage tab (placed after Stories) with all 12 zodiac signs, each with a Day/Week/Month/Year horoscope in English and Hindi. Entertainment-only by design (small disclaimer, no medical/financial/legal claims), computed deterministically at request time from a date-derived index — no cron job, no AI/API cost, no database rows, matching the "compute at request time" pattern already used for scheduled publishing. Removed the item from "Suggested, not started" accordingly. Earlier the same session: fixed the header search box shifting position (stable 3-column grid layout) and moved Trending/Newest up next to the language switcher (it's a global filter, not tab-specific). Earlier still, fixed two bugs reported right after dark mode shipped: story read-aloud not stopping when switching stories via suggestions (React Router reuses the same component instance across story navigations, so the speech-cancel cleanup needed to key on the story text, not just mount/unmount), and the 7 game engines being unreadable in dark mode on phones with system dark mode enabled (the dark-mode pass covered every page but missed the actual game components under `src/games/`). Earlier the same session, shipped 4 more items: reworked the sticky-filter-sidebar scroll-jump fix from a clamped partial-scroll approach (inconsistent across tabs) to a simple, predictable full `scrollTo(0, 0)` reset per direct feedback; generalized the Trending/Newest sort toggle from Quizzes-only to every homepage tab; reordered the homepage tabs so Funny Lines follows Jokes; and built dark mode (manual toggle, persisted, no-flash, covering every public page). Moved "Dark mode toggle" from "Suggested, not started" to "Completed till date" accordingly. Earlier: added 11 items to "Suggested, not started": 10 GenZ/teen-appeal enhancement ideas spanning both the user side (dark mode, daily streak, game leaderboards, achievement badges, reaction emojis, a shareable compare-result image) and the admin side (bulk import, content freshness flags, mobile quick-add, a weekly Analytics digest), plus a "light/funny astrology" content-type idea (day/week/month/year, EN+HI) — recommended as legally safe (entertainment-only, no medical/financial claims) and auto-updating for free via date-based deterministic rotation through a pre-written snippet pool, no cron job or AI cost needed. None of these are built yet. Earlier the same day: added unified view/share engagement tracking to Quiz, Friendship Quiz, and Games, matching what Post already had, via one shared `Engagement` model rather than three more one-off ones; 4 new tables in admin Analytics. Earlier still: added Stories, a new content type with browser-based text-to-speech read-aloud (8 original stories seeded, full admin CRUD, homepage tab). Also removed the "Trending on Social Media" quiz category (added just one day earlier) at the owner's request; 29 → 25 quizzes live. Also removed the now-moot "refresh/retire Trending quizzes" item from "Suggested, not started." Prior update, same day as it was added (2026-07-30): Report (reusing the Feedback model), non-personality (trivia) quizzes, and true two-player async Tic-Tac-Toe — pulled forward from "Suggested, not started" — plus the "Trending on Social Media" quiz category and the reorg of this file into "Completed till date" as a full project history.*
